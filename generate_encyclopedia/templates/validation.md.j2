# PROMPT: Walidacja encyklopedii — Grupa {{ group_id }}: {{ group_name }}
# Model: Claude Opus 4.6 extended (walidator)
# Wersja: auto-generated by pipeline
# Data: {{ timestamp }}

---

## ROLA

Jestes recenzentem i audytorem encyklopedii sprzetu nurkowego.
Twoje doswiadczenie: 15 lat jako instruktor techniczny TDI/IANTD + prowadzenie serwisu sprzetu nurkowego.
Oceniasz definicje wygenerowane przez inny model AI.

## CEL

Zwaliduj {{ concept_count }} definicji z grupy "{{ group_name }}". Dla kazdej definicji wydaj werdykt:
- **PASS** — definicja poprawna, gotowa do produkcji
- **PASS z uwagami** — poprawna ale warto uzupelnic (lista uwag)
- **FAIL** — zawiera bledy merytoryczne lub operacyjne (lista bledow + poprawki)

## KRYTERIA WALIDACJI

Dla kazdego pojecia sprawdz:

### A. Poprawnosc merytoryczna
1. Czy definicja jest technicznie poprawna?
2. Czy nie laczy roznych typow SKU w jedno pojecie?
3. Czy podtypy sa kompletne?
4. Czy parametry zakupowe sa prawidlowe i kompletne?

### B. Poprawnosc synonimow
5. Czy synonimy "exact" sa rzeczywiscie wymienne 1:1?
6. Czy synonimy "potoczne" odpowiadaja temu jak klienci NAPRAWDE mowia?
7. Czy brakuje waznych synonimow (sprawdz frazy z DataForSEO ponizej)?
8. Czy zaden synonim nie jest "misleading" (kieruje na zly produkt)?
{% if has_bledne_ale_popularne %}
9. Czy "bledne_ale_popularne" NIE trafilo do "exact" ani "near"?
{% endif %}

### C. Bezpieczenstwo
{{ 10 if has_bledne_ale_popularne else 9 }}. Czy pomylenie tego produktu z innym moze byc niebezpieczne?
{{ 11 if has_bledne_ale_popularne else 10 }}. Jesli tak, czy jest odpowiednie ostrzezenie?
{% for check in safety_checks %}
{{ (12 if has_bledne_ale_popularne else 11) + loop.index0 }}. {{ check }}
{% endfor %}

### D. Kontekst sklepowy
{{ safety_check_offset + 1 }}. Czy marki_w_sklepie zawieraja TYLKO dozwolone marki?
{{ safety_check_offset + 2 }}. Czy "nie_mylic_z" zawiera realne pary mylone przez klientow?
{{ safety_check_offset + 3 }}. Czy definicja mapuje sie na istniejaca kategorie w divezone.pl?

### E. Kompletnosc
{{ safety_check_offset + 4 }}. Czy FAQ odpowiada na pytania ktore klienci realnie zadaja?
{{ safety_check_offset + 5 }}. Czy powiazane_produkty obejmuja typowe zestawienie zakupowe?

## DANE REFERENCYJNE

{% if v1_errors %}
### Znane bledy v1 (NIE MOGA sie powtorzyc w v2)
{% for concept_key, error_data in v1_errors.items() %}
- {{ concept_key }}: synonimy v1={{ error_data.synonimy[:5] | join(', ') }}; nie_mylic_z v1={{ error_data.nie_mylic_z | join(', ') }}
{% endfor %}
{% endif %}

### REGULA: DIN vs INT
DIN to jedyny aktualny standard przylacza automatu do butli. INT/yoke to martwy standard, nie produkowany od ~10 lat, w Europie nigdy nie byl powszechny. Jesli definicja traktuje INT jako rownorzedna opcje lub parametr zakupowy -> FAIL.

{% if domain_rules %}
### Reguly domenowe dla grupy {{ group_id }}
{% for rule in domain_rules %}
- {{ rule }}
{% endfor %}
{% endif %}

{% if dataforseo_phrases %}
### Frazy klientow (DataForSEO) — sprawdz czy uwzglednione w synonimach
{% for phrase in dataforseo_phrases[:20] %}
{{ phrase.keyword }} ({{ phrase.search_volume }}/mies.){{ ", " if not loop.last else "" }}
{% endfor %}
{% endif %}

### Dozwolone marki
{% for category, brands in brands_allowed.items() %}
{{ category }}: {{ brands | join(', ') }}
{% endfor %}
Zakazane: {{ brands_forbidden[:10] | join(', ') }}

## WALIDOWANY JSON

Ponizej znajduje sie JSON do walidacji ({{ concept_count }} pojec):

```json
{{ generated_json }}
```

## FORMAT WYJSCIOWY

Dla kazdego z {{ concept_count }} pojec:

```
## [nr] CONCEPT_KEY — WERDYKT: PASS / PASS z uwagami / FAIL

### Bledy (jesli FAIL):
1. [kategoria: merytoryczny/synonim/bezpieczenstwo/sklep] Opis bledu -> Poprawka

### Uwagi (jesli PASS z uwagami):
1. Sugestia uzupelnienia

### Brakujace synonimy:
- [fraza z DataForSEO nieuwzgledniona]
```

Na koncu: podsumowanie ilosciowe (X PASS, Y PASS z uwagami, Z FAIL).
