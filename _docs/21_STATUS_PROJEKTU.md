# STATUS PROJEKTU: Czat AI divezone.pl
# Wersja: 4.02 | Data: 2026-07-20 (CHAT-T-157 BACKEND **DEPLOYED** — pakiet 13 reguł SystemPrompt z recenzji rozmów (nr czatu w nawiasach). **Świat**: BACKEND `chat.divezone.pl`, WYŁĄCZNIE `src/Chat/SystemPrompt.php`. **ZERO zmian w tools.php/routes.php/narzędziach/migracji.** Reguły wplecione w ISTNIEJĄCE sekcje promptu: (1/761) język od 1. odpowiedzi także przy pierwszym wywołaniu narzędzia, niemiecki→niemiecki [zmieniono fallback „inny język→angielski" na „w języku klienta"]; (2/738) waluta wg JĘZYKA rozmowy (EUR poza PL, pole `price_eur`) + zakaz meta-komentarzy o własnych pomyłkach cenowych („265.00 EUR *(wait, that's PLN)*"); (3/702) specyfikacja techniczna tylko z get_product_details, zakaz fabrykacji cech (casus GARMIN X30 = transmiter, którego nie ma); (4/680) produkt spoza oferty krótko „nie mamy" bez wykładów (Oceanic+); (5/752) reklamacja zawartości paczki → NATYCHMIAST kontakt, BEZ zbierania danych zamówienia; check_order_status tylko na status/śledzenie; (6/750) „premium"/„najlepszy" bez budżetu → dopytaj + ZAWSZE tańsza alternatywa; (7/717) dostępność per wariant (get_product_combinations), nie generalizuj „od ręki"; (8/600) „kiedy dostanę" bez wskazania produktu → dopytaj i zweryfikuj dostępność zanim podasz termin; (9/754) duży jacket XXL+ → zaproponuj też skrzydło z uprzężą regulowaną (uprzęże bez rozmiarów); (10/751,701) terminy mylone → get_expert_knowledge, sekcja „Nie mylić z" rozstrzyga; ocieplacz vs DOCIEPLACZ neoprenowy (kamizelka), rashguard nie grzeje, wąż inflatora 55-65cm (nie 75); (11/749) każdy URL jako link markdown, też adresy z pamięci promptu (B2B); (12/742) raty 0% do 10 rat („Zapłać na raty" w koszyku), link form płatności z get_shop_links `platnosci`; (13/755) cytat spoza okna historii → nie twierdź „z innego czatu", kontekst mógł być przycięty. **Deploy (ADR-089)**: backup `_deploy_bak/CHAT-T-157/SystemPrompt.php.bak` (pre-change 25982faf), rsync per ścieżka, md5 prod==local `dfd2ef2ce437f8f69a228d018eaae66e`, ea-php84 -l OK, `/api/health` 200 (pg+mysql true). **Test PROD realny czat (HMAC, marker [test CHAT-T-157, nie klient], verdict/updated_by NULL, conv 775-778)**: (a) DE „versenden Sie … nach Deutschland" → CAŁA odpowiedź po niemiecku (get_international_shipping) ✅; (b) „ocieplacz do pianki 5mm" → kamizelka neoprenowa/docieplacz, NIE odzież termoaktywna ani rashguard ✅ [retest luki conv 751 — Karol weryfikuje z bazy, conv 776]; (c) „Garmin X30 integracja z powietrzem" → „nie ma integracji z powietrzem", get_product_details, zero fabrykacji ✅; (d) „brakuje produktu w paczce" → od razu kontakt, bez pytania o nr zamówienia (tools_used=[]) ✅. Commit kodu `2129103`, docs osobno. **Sentinel**: deklaracja `chat.divezone.pl` (1 plik) — czeka na operatora.)
# Wersja: 4.01 | Data: 2026-07-20 (CHAT-T-156 BACKEND **DEPLOYED** — wysyłka: kraj nieaktywny (intl) + progi darmowej dostawy per kurier (PL) (decyzja Karola **192b**, ADR-129/ADR-059, karta Chat - T-156). **Świat**: BACKEND `chat.divezone.pl`, 2 pliki `src/Tools/`. **ZERO zmian w tools.php/routes.php/migracji.** **Problem A (conv 761)**: `get_international_shipping` dla kraju rozpoznanego, ale WYŁĄCZONEGO z wysyłki zwracał `ok`+cennik — Szwajcaria (`pr_country.iso='CH'` **active=0**) ma `id_zone=25` („Węgry") ze stawkami DPD, więc bot podawał cennik i podważał obsługę. **Fix A** (`InternationalShipping.php`): `resolveCountry()` pobiera `c.active`; kraj rozpoznany, ale `active=0` → `status:'not_supported'` z notą DLA BOTA (nie zdradza mechanizmu klientowi), **PRZED** pobraniem stawek. Kraj pozostaje rozpoznany (NIE `unknown_country`). **Problem B (conv 723)**: `get_shipping_info` zwracał JEDEN próg 299 dla wszystkich kurierów — realnie InPost darmowy od 299, DPD od 399. **Fix B** (`ShippingInfo.php`, decyzja 192b — wariant b): ścieżka PL czyta stawki+progi z MySQL `pr_delivery` per kurier (strefa PL z `pr_country iso='PL'`→`id_carrier`→`pr_range_price`→price), PG `divechat_shipping_rates` zostaje WYŁĄCZNIE dla treści opisowych (nazwy metod, czasy, pobranie). **Pomiar PROD 2026-07-20 (strefa Polska id 9)**: InPost Kurier(345)/Paczkomaty(399) 0-299→10,57 netto, 299+→0 (próg **299**); DPD(397) 0-399→17,88, 399-1283→0, 1283+→0 (próg **399**). **Ceny BRUTTO = netto ×1,23 (VAT 23%)** — mnożnik NIE założony, POTWIERDZONY na realnych zamówieniach (`total_shipping_tax_excl/incl`): 10,57→13,00 (2907 zam.), 17,88→21,99 (31), 21,1382→26,00 (pobranie). **Mapowanie metoda→id_carrier po tokenach nazwy, NIE hardcode listy id** (przy klonowaniu kuriera w PS id rośnie; „Kurier DPD" trafia w 397, nie w COD „Pobranie" 398 — remis rozstrzygany mniejszą liczbą niedopasowanych tokenów; test odporności na klon +1000). **Dług ADR-129 spłacony**: usunięty hardcode „flat do 31 kg" z `buildNote`; `max_weight_kg` czytany per kurier z `pr_carrier` (DPD 29, InPost Paczkomat 10, Kurier 28). **Wstrzyknięcie MySQL bez zmiany `tools.php`**: opcjonalny 2. param konstruktora `?MysqlConnection` (lazy `getInstance()`) → `new ShippingInfo($pg)` w tools.php działa bez zmian; fallback do PG przy MySQL down (log). **Testy**: `InternationalShippingTest` 34/0 logika + nowe CH→not_supported (DB-backed); `ShippingInfoTest` (NOWY) 24/0 logika (VAT, tokeny, dopasowanie, grupowanie zakresów) + 8 DB-backed. **DEPLOY (ADR-089, „deployuj")**: backup `_deploy_bak/CHAT-T-156/` (prod przed: `b95b77f3`/`e7258890`), rsync 2 plików per ścieżka (bez --delete), **md5 prod==local** (`b208b1f7`/`e5439a84`), `ea-php84 -l` clean ×2, `/api/health` 200 (php 8.4.23, postgres+mysql true). tools.php/routes.php NIETKNIĘTE. **Weryfikacja DB-backed na serwerze (ea-php84 stdin, bez pliku na dysku)**: CH→not_supported (rozpoznany, bez ranges), AT→ok (regresja); PL: Paczkomat/Kurier InPost 13,00 od 299, DPD 21,99 od 399, odbiór osobisty 0,00; cart 350 → InPost free_now, DPD nie. **Test PROD realny czat (HMAC, reguła E, conv 771/772/773, `[test CHAT-T-156, nie klient]` verdict/updated_by NULL)**: 🇩🇪 „lieferung in die schweiz?" → `get_international_shipping` → „wir liefern nicht in die Schweiz — vom Versand ausgeschlossen" + kontakt, BEZ cennika ✓; „ile kosztuje wysyłka DPD?" → „DPD 21,99 zł, darmowa od 399 zł" (+InPost 13 zł od 299) ✓; „od jakiej kwoty darmowa dostawa?" → „InPost od 299 zł; DPD od 399 zł" ✓. **Testy NIE deployowane na prod** (brak `tests/` w docroocie — bez Sentinel NEW_PHP_FILE). Bez nowej klasy → bez `composer dump-autoload`, bez plików vendora. Commit kodu `e7e4ca1` (4 pliki per ścieżka — NIE `git add .`), docs <PENDING>. NIE ruszane: `routes.php`, `purge_litespeed.php`, untracked innych sesji (ChipEvent*), SystemPrompt.php, inne narzędzia w tools.php, ADR-y (architekt).)
# Wersja: 4.00 | Data: 2026-07-20 (CHAT-T-155 BACKEND **REPO==PROD** — synchronizacja `config/tools.php`, repo czatu = jeden właściciel (ADR-131, karta Chat - 42). **Świat**: BACKEND `chat.divezone.pl`, repo-only. **Cel osiągnięty**: repo-czatu `config/tools.php` == produkcja (md5 `b54480d969809e7e365fc1b53ee7bb2d` po obu stronach) — pole minowe „dryf tools.php" (CHAT-T-132, ADR-129 nota 1) ZAMKNIĘTE, blanket-rsync znów bezpieczny. **Ten task NIE zmienił produkcji** — prod był już w stanie docelowym; to sprzątanie repo do zgodności z prod. **3 zmiany (diff repo↔prod potwierdził DOKŁADNIE tyle, nic poza)**: (1) wniesiona żywa klasa `src/Tools/GetProductCombinations.php` jako KOPIA z repo Atrybuty (`../Atrybuty_produktow_2026/_instances/chat/src/Tools/`, md5 `161112790b71b23495a3a5dc4ce5fb8e` == prod, ATTR-T-052/ADR-025, czyta `divezone_attr_color_lang`, READ-ONLY); (2) `config/tools.php`: `use ProductCombinations` → `use GetProductCombinations` (pozycja alfabetyczna G), rejestracja przeniesiona po SizeRecommender z komentarzem ATTR-T-052 — repo doprowadzony bajt-w-bajt do wersji prod (pobrany przez SSH, diff zweryfikowany, brak niespodzianek poza 3 zmianami); (3) `git rm src/Tools/ProductCombinations.php` — martwa klasa CHAT-T-129 (nigdy niewdrożona) usunięta. **ADR-131 uśmierca ADR-112** (ProductCombinations — wariant odrzucony na rzecz wersji Atrybutów). Kanoniczne źródło klasy-gościa = wersjonowany plik u właściciela logiki (Atrybuty przekazują plik+md5, czat wgrywa swoim deployem — NIE „diff"). Atrybuty NIE deployują `tools.php` na drzewo czatu (od 2026-07-20). **Weryfikacja lokalna**: `ea-php84 -l` clean ×2, namespace `DiveChat\Tools`, `implements ToolInterface`, md5 tools.php==prod. **DEPLOY: ZBĘDNY** (prod już docelowy; potwierdzi architekt na „deployuj"). Commit kodu `414d157` (add GetProductCombinations + sync tools.php + rm ProductCombinations, per ścieżka — NIE `git add .`), docs <PENDING>. NIE ruszane: `routes.php`, `purge_litespeed.php`, untracked innych sesji, SystemPrompt.php, inne narzędzia w tools.php.)
# Wersja: 3.99 | Data: 2026-07-20 (CHAT-T-154 BACKEND **DEPLOYED** — procedura naprawy koszyka przy złym wariancie (decyzja 191a, karta Chat - 37; bez nowego ADR — uzupełnienie reguły WARIANTY). **Świat**: BACKEND `chat.divezone.pl`, WYŁĄCZNIE `src/Chat/SystemPrompt.php` (JEDEN akapit, ZERO tools.php/routes.php/narzędzi/migracji). **Problem**: sekcja WARIANTY uczyła bota DIAGNOZOWAĆ zły wariant w koszyku (`domyslny_wariant`, „nie wybrano wariantu"), ale NIE mówiła klientowi jak to NAPRAWIĆ (geneza conv 593 TUSA 5452 — bot „jednorozmiarowy"+infolinia; diagnoza zamknięta odkąd `get_product_combinations` żyje od 16.07, ATTR-T-052). **Zmiana**: dopisany akapit „NAPRAWA KOSZYKA" (po linii „...NIGDY nie sugeruj awarii...", przed blokiem „Bug do uniknięcia czat 606") — usuń pozycję z koszyka i dodaj produkt ponownie, wybierając rozmiar/kolor NA KARCIE przed „Dodaj do koszyka"; jeśli klient nie wie który wariant — `get_product_combinations` i pokaż dostępne. **Ograniczenie techniczne (zweryfikowane, świadomie NIE obchodzone)**: link preselekcji `?id_product_attribute=NNN` działa TYLKO na kanonicznym URL (z prefiksem kategorii); URL z narzędzi (bez prefiksu) → PrestaShop 301 gubi parametr. Dlatego procedura CZYSTO TEKSTOWA, bez linku z parametrem; narzędzie nietknięte. **DEPLOY (ADR-089, „deployuj")**: PROD przed = `8e630768` (wersja T-153, stan bazowy poprawny), backup `_deploy_bak/CHAT-T-154/SystemPrompt.php.bak`, rsync per ścieżka (bez --delete), md5 prod==local po deployu (`25982faf5cfe42b7611fb9516bb14e5b`), `grep -c "NAPRAWA KOSZYKA"`=1, `ea-php84 -l` clean, `/api/health` 200. tools.php/routes.php NIETKNIĘTE. Commit kodu `2aed037`, docs <PENDING>. NIE commitowane: `routes.php`, `purge_litespeed.php` (cudze/sekret), ADR-y (architekt).)
# Wersja: 3.98 | Data: 2026-07-19 (CHAT-T-153 BACKEND **DEPLOYED** — dokręcenie wykluczeń manometru w regule 8b/165a, luka „moduł" (ADR-130 nota 1, decyzja 168a, karta Chat - 14). **Świat**: BACKEND `chat.divezone.pl`, WYŁĄCZNIE `src/Chat/SystemPrompt.php` (JEDEN plik, ZERO tools.php — dryf Chat - 42, ZERO narzędzi/migracji/re-embedu). **Problem**: test PROD CHAT-T-152 (S3) wykrył, że bot wskazał „Manometr TECLINE 300 bar, 52mm, nikiel - moduł" (id 4266, 236 zł) jako najtańszy pasujący — sama głowica bez węża HP (wymaga dokupienia wrzeciona i węża), klient dostałby niekompletną część. Kryterium 165a (wykluczenia pony/tlenowy/O2/wąż 15cm) NIE obejmowało „modułu"/SPG/wrzeciona. **Zmiana**: reguła 165a przeformułowana z rosnącej listy wykluczeń na **kryterium POZYTYWNE (kompletności)** — manometr do gotowego zestawu MUSI być kompletny, z wężem HP (w nazwie zwykle 60cm/80cm); POMIŃ jeśli nazwa/opis zawiera „moduł"/„sama głowica", „SPG" samo (bez węża), „wrzeciono"/„króciec", plus zachowane pony/tlenowy/O2/wąż 15cm. **DEPLOY (ADR-089, „deployuj")**: PROD==git HEAD przed rsync (`0312b10c`, brak dryfu), backup `_deploy_bak/CHAT-T-153/SystemPrompt.php.bak`, rsync per ścieżka, md5 prod==local po deployu (`8e630768`), `ea-php84 -l` clean, `/api/health` 200. tools.php NIETKNIĘTY. **Test PROD realny czat (CC, HMAC, reguła E, conv session e1d4f445, review_id 331, `[test CHAT-T-153, nie klient]` verdict/updated_by NULL)**: S3 „chcę zacząć nurkować, co potrzebuję do oddychania" → 4 zestawy rekreacyjne + „manometr dobieramy i montujemy przy odbiorze", BEZ modułu 236 zł ✓; kontrola „a są inne manometry?" → lista kompletnych (TERMO 300bar/60cm 249 zł najtańszy, TECLINE EVO 80cm 322, SCUBATECH 339, Mares Mission), bot jawnie odrzucił TECLINE O2 (tlenowy, wąż 15cm) i pony ✓ — **główna luka „moduł" domknięta**. **RESIDUUM (do decyzji Karola, NIE blokuje)**: bot wciąż wylistował OMS „Manometr SPG 52/63 mm" (340 zł) mimo dopisanego wykluczenia „SPG samo" — miss miękkiego kryterium przez LLM (nazwa „Manometr SPG" czytana jako kompletny manometr); nie jest najtańszy (340 > 249), NIE wpływa na główną rekomendację, ale jeśli te OMS realnie są bez węża (ustalenie architekta w nocie 1), warto dokręcić przy najbliższym dotknięciu promptu lub przejść na narzędzie (granica determinizmu, ADR-130 Ograniczenie). Commit: <PENDING>. NIE commitowane: `routes.php`, `purge_litespeed.php` (cudze/sekret), ADR-y (architekt).)
# Wersja: 3.97 | Data: 2026-07-18 (CHAT-T-151 BACKEND **DEPLOYED** — narzędzie wysyłki zagranicznej DPD `get_international_shipping` (ADR-129 + nota 1, decyzje Karola 153b/154b/155a/156a/157a/158b/159a, karta Chat - 7). **Świat**: BACKEND `chat.divezone.pl`. **Problem**: `get_shipping_info` dla `zone=EU` zwracał pustą listę + „skontaktuj się" (bug conv 638/649). **DWIE KOREKTY ZAŁOŻEŃ KARTY (pomiar PROD MySQL)**: (1) karta „stawki zależą od WAGI" — NIEPRAWDA, DPD 397 `shipping_method=2` (=cena), `pr_range_price` 3 wiersze (0-399/399-1283/1283-999999 PLN), `pr_range_weight` 0; DPD proguje WARTOŚCIĄ koszyka, waga=limit górny. (2) architekt wstępnie „DPD+InPost dla zagranicy" — NIEPRAWDA, InPost 399 ma stawki tylko strefa Polska; kurier zagraniczny=WYŁĄCZNIE DPD (397), 22 strefy+PL. **Nowe narzędzie `src/Tools/InternationalShipping.php`** (`get_international_shipping`): łańcuch `pr_country`→`id_zone`→`pr_delivery`(id_carrier=397, JOIN `pr_range_price`)→stawka PLN; limit z `pr_carrier.max_weight` (DPD 29 kg, NIE hardcode); kurs z `pr_currency` id=2 (0.234915), `price_eur=ceil(pln*rate)` w górę do pełnego EUR (97 PLN=23 EUR, próg 1283=302 EUR). Statusy: `ok`/`not_supported`(wyspa lub strefa bez stawki — strefa 7 „Europe non-EU" ma 0 wierszy DPD, 156a)/`unknown_country`/`error`. **153b** `weight=0` w koszyku (439/2664 aktywnych): `weight_uncertain` w zwrotce, bot podaje stawkę + zastrzeżenie TYLKO blisko limitu (nie zerować=nie fabrykować). **154→b** OSOBNE narzędzie, `get_shipping_info` NIETKNIĘTE (czyta `divechat_shipping_rates` PG, płaski model PL/EU; zagranica=MySQL `pr_delivery`/strefy — mieszanie=duplikat 22 stref=stała lista rozjeżdżająca się cicho). **SystemPrompt**: nowy blok WYSYŁKA ZAGRANICZNA (EUR-first, progi, darmowa, `not_supported`+kontakt bez wyceny indywidualnej, wyspy=tylko ląd stały, limit wagi + weight_uncertain). **BUG złapany i naprawiony w deployu (CC)**: nazwy krajów w PS mają wiodący U+00A0 (nbsp, C2A0), SQL TRIM (0x20) nie usuwa → „ Austria"; dodany `cleanName()` static nbsp-aware (`preg_replace '/^[\s\x{00A0}]+|...$/u'`) + 4 testy; DOPISANE do `_docs/44` PUŁAPKI. **DEPLOY (ADR-089, „deployuj")**: backup `_deploy_bak/CHAT-T-151/`, rsync per ścieżka 3 plików (`InternationalShipping.php` nowy, `SystemPrompt.php`, `config/tools.php` z wariantu **PROD_SAFE** — prod + tylko 2 linie, NIE z repo). **tools.php DRYF DWUKIERUNKOWY** (repo `ProductCombinations` martwa vs prod `GetProductCombinations` żywa, ATTR-T-052 — CC złapał, architekt potwierdził odczytem obu plików; ADR-129 nota 1, karta Chat - 42): blanket-rsync=fatal 500 + zabicie cudzej rejestracji. **Architekt zweryfikował na prod niezależnie (SSH)**: md5 3/3 prod==local (`b54480d9`/`b95b77f3`/`d96247a9`), `GetProductCombinations` ZOSTAŁO (tools.php:14,58), `InternationalShipping` zarejestrowane (17,46), `ProductCombinations` martwa NIE trafiła na prod, `composer dump-autoload -o` 375 klas, `ea-php84 -l` clean ×3, `/api/health` 200 (R-5 trap ominięty). **Test suite na serwerze (CC)**: 47/0 w tym 13 DB-backed (Austria ok 3 progi 97/23, Czechy 87, Grenlandia→not_supported, Blorpland→unknown_country, cart 500→23 EUR, cart 1500→free, waga 35→over_limit, weight_uncertain). **Test PROD realny czat (CC, HMAC, reguła E)**: conv 740 Austria→`get_international_shipping`→„do 302 EUR→23 EUR, powyżej→gratis, limit 29 kg"+kontakt ✓; conv 741 Teneryfa→not_supported→„poza zasięgiem DPD"+kontakt, bez fabrykacji ceny ✓ (oznaczone `[test CHAT-T-151, nie klient]`, verdict/updated_by NULL). **158b repo reconciliation przez architekta (SMB, bo CC repo zablokowane TCC)**: ściągnięta wdrożona wersja `InternationalShipping.php` z prod (md5 b95b77f3) nadpisała repo (repo miało wersję sprzed fix nbsp `8b595637`); test dopełniony 4 asercjami `cleanName` (static, bez DB), `ea-php84 -l` clean; repo==prod. **159a obserwacja (nie fix)**: na Teneryfie bot dodał „może uda się znaleźć alternatywne rozwiązanie" — nie fabrykacja ceny, ale miększe niż reguła 156a; do dokręcenia przy najbliższym dotknięciu SystemPrompt, nie osobny deploy. **DŁUG odnotowany**: `ShippingInfo::buildNote` ma hardcode „flat do 31 kg" a DPD=29 (ADR-129, poza zakresem). Commity architekta: `9020308` (ADR-129), `c776ec7` (task), `5ce880c` (nota 1), `617efc4` (kod+test+_docs/44). NIE commitowane: `routes.php`, `purge_litespeed.php` (cudze/sekret). Karta Chat - 7 → „Zrobione" (fix zweryfikowany realną ścieżką: md5 prod==repo, GetProductCombinations zachowane, test PROD Austria/Teneryfa).)
# Wersja: 3.96 | Data: 2026-07-18 (CHAT-T-150 EMBEDDINGS **DEPLOYED + CRON ON** — pipeline embeddingów produktów zautomatyzowany (ADR-128 + noty 1/2, decyzje Karola 141b/142c/144a/145a/146c/148b/149a/150b/151b). **Świat**: TRZECIE MIEJSCE (poza docrootem) `/home/divezone/scripts/embeddings/` — NIE `standalone/`, NIE `newtmp2`. **KROK 5 deploy**: katalog utworzony, venv python3.12 (3.12.13) + `pip install -r requirements.txt` (openai 2.46, psycopg2-binary 2.9.12, pymysql 1.2, python-dotenv 1.2.2, numpy 2.5.1, beautifulsoup4 4.15); 7 plików wysłanych PO NAZWIE (nie blanket-rsync) — **md5 prod==local 7/7 zweryfikowane niezależnie przez architekta** (`d248ba12`/`ddeafeb2`/`6a69d000`/`d6ec89d0`/`ccb4722c`/`15bce3c5`/`986a9053`). **KROK 6 dry-run serwerowy** (0 API, 0 zapisu): tryb server MySQL localhost bez tunelu, .env bezwzględny, extract 2664 do 2606, **delta 0/2606** (gdyby hash źle liczony to ~2606). **KROK 6.2 realny przebieg**: delta 0, api 0, exit 0, `last_success` utworzony (`/home/divezone/logs/divechat_embeddings.last_success`, 11:28); **PG nietknięte, architekt zweryfikował sam: `max(updated_at)` 2026-07-16 18:40 i count 2606 identyczne przed/po**. **DOWÓD POPRAWNOŚCI DELTY na prod (architekt)**: produkt 4290 (Kompas TECLINE) ma `date_upd`=2026-07-17 (PO ostatnim embeddingu), kryterium `date_upd>last_run` (odrzucone 141a) wysłałoby go na zbędny re-embed; ale cena BRUTTO bez zmian (202.097561 netto ×1.23 = 248.58 = dokładnie `document_text` w PG), treść identyczna, hash się zgadza, delta słusznie pomija. **`date_upd` skłamał, hash nie** — dokładnie teza 141b, potwierdzona na żywym przypadku brzegowym. **KROK 7 crontab ON** (backup `/home/divezone/crontab.bak-embeddings-20260718-120641`): dwie linie, `diff` = wyłącznie `64a65,66` (2 dopisane, 0 usuniętych/zmienionych, **architekt zweryfikował `crontab -l` przez SSH**): `15 2 * * *` przebieg delty (timeout 1800, poza blokiem 03:00-05:30 gęstym od indekserów/sentinel), `30 8 * * *` watchdog. **Mechanizm (6 plików w `embeddings/` + runner + watchdog)**: (1) `extract_products.py` — `EMBEDDINGS_ENV=server|local` (domyślnie local=zero zmian); server to MySQL `DB_HOST`/`DB_PORT` z `.env` (=localhost:3306) BEZ tunelu; `open/close_ssh_tunnel` warunkowo, NIETKNIĘTE; mode-aware loader `.env` (server to ścieżka bezwzględna `/home/divezone/public_html/chat.divezone.pl/.env`); kanoniczny `document_text_hash()` sha256 ze znormalizowanego `document_text` (wzorzec ENC-013, ZERO nowej kolumny/migracji, hash w locie po OBU stronach). (2) `batch_embed_products.py` — `--mode changed`: extract pełny (0 API), embedding tylko dla hash-różnych/nieobecnych. (3) `batch_embed_multivector.py` — `run_for_pids(pids)`, pusta lista to no-op. (4) `generate_embeddings.py` — ten sam loader. (5) `run_nightly.py` (NOWY, runner=Python): kolejność KROK2 do KROK3 (błąd etapu 1 to etap 2 nie rusza), lock `fcntl.flock(LOCK_EX|LOCK_NB)` na deskryptorze (NIE istnienie pliku; zwolnienie w finally), heartbeat 48h, alert `sendmail` na `DIVECHAT_COST_ALERT_EMAIL` (**potwierdzony w serwerowym .env przez architekta**). (6) `watchdog.sh` (NOWY, dead-man 148b): osobny cron 08:30 obserwuje wiek `last_success` (>26h to alert), niezależny od runnera, łapie „cron zniknął/runner padł przed heartbeatem"; granica: nie łapie śmierci całego crond. **Weryfikacja lokalna**: delta-core 5/5, py_compile 5×OK, bash -n watchdog OK, LOKALNY DRY-RUN na realnych danych delta 0/2606. **4 rozbieżności z 1. tury CC domknięte USTALENIAMI architekta** (email w serwerowym .env; loader mode-aware; requirements.txt istnieje; DB_HOST=localhost≠127.0.0.1 socket vs TCP, kod czyta z .env). **Blokada środowiskowa CC** (macOS TCC/Full Disk Access DENY w połowie sesji, nowe pliki OK, istniejące EPERM): deploy przez SSH działał (inny kanał), ale KROK 8 status doc CC nie mógł zapisać, **architekt dopisał ten status sam przez Desktop Commander/SMB** (niezależny kanał od TCC CC). **Karta Trello Chat - 23 zostaje w „W trakcie"** (decyzja 151b), domknięcie po zaobserwowaniu pierwszego nocnego przebiegu 02:15 jutro rano (jedyny element niesprawdzalny inaczej niż przez zdarzenie). **DŁUG**: karta miała dane sprzed 2 dni („ostatni przebieg 2026-05-15") nieaktualne, tabela odświeżona ręcznie 15-16.07 po T-144; mechanizm nie istniał do teraz. Commity CC: `48b8593` (kod), `9a223c7` (status 3.95). Commity architekta: `37b3831`+`4e2fd65` (ADR-128+noty), `369e175` (prompt startowy 47 + korekta rrf_score w CLAUDE.md), `e45e086` (dowód 4290). NIE commitowane: `routes.php`, `purge_litespeed.php` (cudze/sekret), ADR-y poza autorstwem architekta.)
# Wersja: 3.95 | Data: 2026-07-18 (CHAT-T-150 EMBEDDINGS — automatyzacja pipeline'u produktów: KOD GOTOWY I ZACOMMITOWANY, **NIEWDROŻONY** (STOP przed KROK 5 deploy i KROK 7 crontab, ADR-089). ADR-128 + noty 1/2 (delta po hashu BEZ migracji/kolumny; dead-man watchdog dec. 148b). **Problem**: pipeline embeddingów żył tylko na laptopie — nowy produkt niewidoczny dla bota do ręcznego odpalenia, awaria cicha (raz stał 2 mies.). **Zmiana (6 plików w `embeddings/`, ZERO w `standalone/`/`newtmp2`)**: (1) `extract_products.py` — przełącznik `EMBEDDINGS_ENV=server|local` (domyślnie local = zero zmian zachowania); server → MySQL `DB_HOST`/`DB_PORT` z `.env` (=`localhost:3306`) BEZ tunelu; wrappery `open/close_mysql_access()` wołają tunel warunkowo, `open/close_ssh_tunnel()` NIETKNIĘTE (importuje je `embed_target_products.py`); mode-aware loader `.env` (server → ścieżka bezwzględna `/home/divezone/public_html/chat.divezone.pl/.env`, ADR-128 dec. 145a — bo `parent.parent/.env` na serwerze nie istnieje); kanoniczny `document_text_hash()` = sha256 ze znormalizowanego (`\s+`→' ') `document_text`, wzorzec ENC-013. (2) `batch_embed_products.py` — `run_changed_mode(dry_run)`: extract pełny (0 API), hash w locie po OBU stronach (MySQL vs PG.document_text), kwalifikacja tylko hash-różny/nieobecny, embedding sync (delta mała), ZERO nowej kolumny/migracji; `--mode changed [--dry-run]`. (3) `batch_embed_multivector.py` — `run_for_pids(pids)` przelicza 3 wektory tylko dla delty; pusta lista → no-op sukces; `EMBEDDING_DIM`/modele nietknięte. (4) `generate_embeddings.py` — ten sam mode-aware loader `.env`. (5) `run_nightly.py` (NOWY, runner=Python nie shell — przekazuje listę pid w procesie, bez serializacji CLI): kolejność KROK2→KROK3 (błąd etapu 1 → etap 2 nie rusza), lock `fcntl`, heartbeat 48h (plik `last_success`), alert `sendmail` na `DIVECHAT_COST_ALERT_EMAIL`. (6) `watchdog.sh` (NOWY, dead-man dec. 148b): osobny cron-strażnik obserwuje wiek `last_success` (>26h → alert), niezależny od runnera, łapie „cron zniknął/runner padł przed heartbeatem"; granica: nie łapie śmierci całego crond. **Weryfikacja lokalna (read-only, przed deployem)**: delta-core unit-test 5/5; `py_compile` 5 plików OK; `bash -n` watchdog OK; watchdog 3 gałęzie (świeży/stary/brak) OK; **LOKALNY DRY-RUN na realnych danych** (tunel prod MySQL + Railway PG, ZERO API): extract 2664→2606 po filtrze, PG=2606, **zakwalifikowano do delty 0/2606** (gdyby hash źle liczony → ~2606; wyszło 0 = hash identyczny po obu stronach dla niezmienionej treści; dokładnie oczekiwanie KROK 6). **USTALENIA architekta 2026-07-18 potwierdziły 4 rozbieżności** (email w serwerowym .env; loader mode-aware; requirements.txt istnieje; DB_HOST=localhost≠127.0.0.1 socket vs TCP — kod czyta DB_HOST z .env, nie forsuje). Deploy do `/home/divezone/scripts/embeddings/` (+ venv python3.12) i 2 wpisy crona (02:15 przebieg, 08:30 strażnik) CZEKAJĄ na „deployuj"/zgodę Karola. Commit: <PENDING>. NIE commitowane: `routes.php`, `purge_litespeed.php` (cudze/sekret), ADR-y (architekt).)
# Wersja: 3.94 | Data: 2026-07-17 (CHAT-T-149 BACKEND DEPLOYED — `search_diagnostics` AKUMULUJE tury zamiast je nadpisywać (ADR-127, decyzje Karola 134a/135a). Zamyka dług wykryty przy T-148 (ADR-126 nota nr 1) — naprawa U ŹRÓDŁA. **Problem**: kolumna trzymała MIGAWKĘ ostatniej tury, nie historię — kod NIGDY nie czytał poprzedniej wartości (`ChatService.php:129` `$searchDiagnostics=[]` startuje pusta co turę → `:372` → `ConversationStore.php:188` `SET search_diagnostics = ?::jsonb` nadpisuje w całości). Zmierzone: **86 z 277 rozmów** miało diagnostykę uboższą niż `messages`, **200 wywołań bez śladu** → wymusiło wyłączenie tych 86 z migracji 042 (130b). **Asymetria usunięta**: `knowledge_gap` w TEJ SAMEJ instrukcji `UPDATE` (`:189`) jest sticky (`? OR COALESCE(...)`) — dokłada; diagnostyka linijkę wyżej nadpisywała. **Zmiana (1 plik, 1 linia + komentarz)** `ConversationStore::save()` ~188: `search_diagnostics = ?::jsonb` → **`search_diagnostics = COALESCE(search_diagnostics, '[]'::jsonb) || ?::jsonb`** (134a — akumulacja w SQL: atomowa, w tej samej instrukcji, zero dodatkowego zapytania, zero wyścigu; odrzucone: merge w PHP, osobna tabela). **Kolejność parametrów BEZ ZMIAN** — `COALESCE(kolumna,'[]'::jsonb)` nie wnosi placeholderów, `|| ?::jsonb` wnosi ten sam jeden → nadal **7 placeholderów, `search_diagnostics` na pozycji 5** (przesunięcie = ciche zepsucie zapisu WSZYSTKICH pól; zweryfikowane empirycznie). **Założenie sprawdzone przed zmianą**: `save()` ma JEDNO wywołanie w całym `src/` (`ChatService.php:372`), poza pętlą narzędzi — inaczej `||` dublowałby wpisy. **NIETKNIĘTE**: `ChatService.php` (`$searchDiagnostics=[]` na `:129` zostaje — akumulacja w SQL, nie w PHP), sticky OR `:189`, moduł PS (panel renderuje `<pre>` z `overflow:auto` → dłuższa tablica scrolluje), **ZERO migracji PG**, historia rozmów nieruszana (135a — te 86 zostaje z `true` = fałszywy alarm, nie utrata sygnału). **Test lokalny (realny `save()`, nie symulacja SQL)**: `BEGIN`/`ROLLBACK` na tym samym połączeniu (singleton) → zero śladu; tura1 **len=1**, tura2 **len=2**, tura3 z PUSTĄ diagnostyką **len=2** (`existing || '[]'` = `existing` — nie kasuje), start z NULL obsłużony przez `COALESCE` (−1→1), kolejność `TURA1→TURA2`, `knowledge_gap` sticky przetrwał, `model_used`/`tools_used`/`messages` poprawne (brak przesunięcia), po ROLLBACK 0 wierszy. **Deploy**: backup `_deploy_bak/CHAT-T-149_20260717_100248/` (md5 prod przed `5e3c75d1`) = **rollback**; rsync **1 pliku** (NIE `standalone/` — dryf `config/tools.php` R-5 = fatal 500, `config/routes.php` = cudza zmiana; tu wpisane w task, nie odstępstwo); md5 local==prod **`372fadc7`**, `ea-php84 -l` czysty, markery `ADR-127`+`COALESCE(search_diagnostics` na prod, `/api/health` **200**. **BEZ `pg_dump`** — zmiana dotyczy sposobu zapisu nowych danych, nie istniejących. **TEST WIELOTUROWY NA PROD (sedno, conv `eabbe0bb`)**: tura 1 „Shearwater Perdix 3" → **len=1**; tura 2 „latarki nurkowe" w TEJ SAMEJ rozmowie → **len=2**, `query_texts` = `Shearwater Perdix 3 -> latarka nurkowa`, **wpis z tury 1 nadal obecny** — przed naprawą ta sekwencja dawała **1**. Spójność: `jsonb_array_length` **2 == 2** liczby `messages[].tool_calls[]` wyszukujących; sanity: wszystkich `tool_calls`=3 (`get_popular_products` nie-wyszukujące) → parser NIE zwraca fałszywego zera (pułapka `_docs/44`). `knowledge_gap` dla trafnego produktu nadal **`false`** (ADR-126 nietknięte). Reguła E: `[test CHAT-T-149, nie klient]`, verdict/updated_by NULL. **Koszt świadomy (ADR-127)**: kolumna rośnie bez limitu — dziś 841 kB łącznie/655 rozmów, max 14 kB, najdłuższa rozmowa (21 wywołań) → ~28 kB wobec limitu 255 MB jsonb bez znaczenia; gdyby rozmowy urosły — opcja osobnej tabeli albo przycinanie do N ostatnich. Commit: 8fa2d06. NIE commitowane: `routes.php`, `purge_litespeed.php` (cudze/sekret), ADR-y (architekt).)
# Wersja: 3.93 | Data: 2026-07-17 (CHAT-T-148 BACKEND DEPLOYED — `knowledge_gap` dla `search_products`: luka = BRAK WYNIKÓW (ADR-126 + noty 1-2, decyzje Karola 128b/129b/130b/132a). **Problem**: filtr „Luki wiedzy" w panelu PS nie filtrował niczego — 237 rozmów z `search_products`, 237 z flagą `true`, zero `false`. **Przyczyna**: jeden próg 0,5 na DWIE SKALE — `get_expert_knowledge` zwraca prawdziwy cosine (0-1, próg działa), `search_products` zwraca **`rrf_score`** (sufit zmierzony **0,1230**, próg nieosiągalny). Nie „źle dobrana liczba", tylko zła jednostka. **Kod** (1 plik, `ChatService::buildSearchDiagnostic()`): `search_products` → `$gap = empty($items)` bez progu (128b); `get_expert_knowledge` BEZ ZMIAN; `$threshold`/`knowledge_gap_threshold`/`max_similarity`/`min_similarity` zostają. **Deploy**: WYŁĄCZNIE `src/Chat/ChatService.php` — dry-run blanket-rsync `standalone/` pokazał **146 plików**, w tym `config/tools.php` (dryf repo≠prod potwierdzony `diff` → fatal 500, CLAUDE.md) i `config/routes.php` (zmiana innej sesji, NIE RUSZAJ); backup `_deploy_bak/CHAT-T-148_20260717_085424/` (prod przed: md5 `715d48ce`), md5 local==prod **`6ad5558f`**, `ea-php84 -l` czysty, `/api/health` **200** (php 8.4.23, postgres+mysql true). **pg_dump PRZED migracją** (autorytatywny rollback, 132a): `_backups/divechat_conversations_pre042_20260717_085336.sql` (11 MB, 655 w.; pg_dump libpq 18.4 — homebrew 14 odrzuca mismatch z serwerem 18.3). **Migracja `sql/042`** (129b, `psql -f -`): **`UPDATE 191`**; reguła OR = (`get_expert_knowledge` gap) LUB (`search_products` `result_count=0`). **GRANICA (130b)** — migrujemy TYLKO pełną diagnostykę: `jsonb_array_length(search_diagnostics)` == liczba wywołań w **`messages[].tool_calls[]`** (NIE Anthropic `content[].type='tool_use'` — zły parser zwraca 0 i test przechodzi FAŁSZYWIE). Chroni 86 rozmów z diagnostyką-migawką (**80 flag**) + 94 bez diagnostyki: `search_diagnostics` jest NADPISYWANY co turę, a `knowledge_gap` sticky → rekompilacja gubi lukę z wcześniejszej tury; zerowanie = fabrykacja. **Wynik (7/7 asercji zgodne)**: panel globalnie **339 → 196**; scope 191: **143 zgaszone** (160→17 `true`); NIETKNIĘTE 86 + 94 = **180**; kohorta `search_products` 237 → **94 `true`** (14 pełna diag + 80 nietkniętych). **Weryfikacja**: rozmowa PROD (realny czat HMAC) „Shearwater Perdix 3" → rc=5, **maxSim 0,123**, flaga **`false`** ✓ — pod starą regułą 0,123<0,5 dawało `true`, test przed naprawą NIEMOŻLIWY. Gałąź `rc=0 → true` nie do wywołania promptem (wyszukiwarka wektorowa zwraca top-N nawet na bzdurę: „Blorptronix 9000" → rc=5, maxSim 0,046, `false` — udokumentowane ograniczenie ADR-126 „znalazł, ale bzdurę"; historyczne zera pochodzą z wieloturowego kontekstu z filtrami) → dowiedziona **deterministycznie refleksją na wdrożonym pliku prod**, 5/5 PASS (0 wyników→`true`; rrf 0,046-0,123→`false`; cosine 0,46→`true`; cosine 0,72→`false`; 0 wiedzy→`true`), plik tymczasowy usunięty. **Reguła E**: 5 rozmów testowych oznaczone `[test CHAT-T-148, nie klient]`, verdict/updated_by NULL. **Rollback** `042_..._rollback.sql`: whole-scope 191→`true`, over-restore **31** rozmów encyklopedycznych udokumentowany w nagłówku (132a — rule-based rollback nie odróżni 143 zmienionych od 31 już-`false`; `pg_dump` = jedyny dokładny). **Moduł PS BEZ ZMIAN** (front już umiał — brakowało sygnału, nie UI); klik-test panelu po stronie Karola. **Dług (poza taskiem, nota 1)**: `search_diagnostics` jako migawka = 200 wywołań bez śladu w 86 rozmowach — powinna akumulować tury. Commit: cd4e001. NIE commitowane: `_backups/`, `routes.php`, `purge_litespeed.php` (cudze/sekret), ADR-y (architekt).)
# Wersja: 3.92 | Data: 2026-07-16 (CHAT-T-145 EMBEDDINGS — EKSPERYMENT ODRZUCONY (ADR-125, decyzja Karola 126a). Operacja LOKALNA (Railway PG + OpenAI sync), ZERO rsync/deployu/PHP. **Pytanie**: czy dolozenie `category_name` do `text_name` (dzis `product_name + brand_name`, BEZ kategorii) w `build_multivector_texts()` poprawia trafnosc, czy rozmywa tor `name`. **Dump PRZED**: `_backups/divechat_product_embeddings_20260716_przed_T145.sql` (215 287 970 B ~205 MB, serwer PG 18.3, pg_dump libpq 18.4 — homebrew 14 odrzuca mismatch). **Brama (72b)**: PRZED zgodne co do 4. miejsca z T-142 (7641 name 0.9137, 7648 0.8612, 7647 0.7737) → pomiar porownywalny. Probka 35 (obowiazkowe 7641/7648/7647/2369/7545 + 8 jednokat. kontrolek + 22 roznorodne), re-embed multi-vector `--skip-single`, jargon jako KONTROLA. **PO — name SPADA na kanonicznych**: 7641 „Shearwater Perdix 3" **0.9137→0.7722 (−0.1415)** (flagowiec, dokladnie ryzyko z ADR: krotka celna nazwa utopiona w 4 kategoriach), 7648 0.8612→0.8329 (−0.0283), 7647 0.7737→0.7740 (płasko); rosnie tylko na frazach OPISOWYCH (koszulka merino +0.0704, balast nerka +0.0262, Apeks ATX40 +0.0416) — tam gdzie name i tak slaby. Kontrola `jargon` Δ≤±0.0003 (szum float) → obserwacja realna, nie szum. **Kryterium ADR-125 „name spada → odrzucamy"** → ODRZUCONA: kategoria jest juz w `text_desc` (ADR-122, +0.0421 na 7647), dokladanie do `name` odbiera torowi ortogonalnosc = sens RRF. **ROLLBACK**: (1) przywrocone `embedding_name` 35 ID probki z dumpa (staging temp + UPDATE 35, NIE cala tabela); (2) weryfikacja pomiarem 7641 name **0.9137**✓ / 7648 **0.8612**✓ / 7647 **0.7736** (Δ0.0001 = szum zapytania OpenAI, wektor z dumpa); (3) zmiana w `.py` cofnieta, **`git diff` pliku pusty wzgledem HEAD**. **Koszt** ~0,02 USD. Commit: TYLKO task file per-sciezka; `embed_target_products.py` (diff pusty, NIE commitowany), ADR-125 (status ODRZUCONA wpisuje architekt), `_backups/`/`routes.php`/`purge_litespeed.php` (cudze) NIE commitowane.)
# Wersja: 3.91 | Data: 2026-07-16 (CHAT-T-138 EMBEDDINGS (sekcja D2) — wiedza EN 250 do `encyclopedia_chunks` (ADR-120). Operacja na danych Railway PG, ZERO rsync. Dopełnia część backendową 3.89 (prompt: głębokość nie jest kryterium). **Korekta D→D2**: sekcja D była błędna (tabela `divechat_knowledge` = martwa, 0 odczytów w `src`; wymiar 1536 zamiast 3072; treść merytorycznie zła). Cel: `encyclopedia_chunks` (czyta `get_expert_knowledge`/`ExpertKnowledge.php`), `embedding vector(3072)`, model `text-embedding-3-large`. **pg_dump**: `_backups/encyclopedia_chunks_20260716_przed_T138.sql` (~20 MB, 530 w.). **ODSTĘPSTWO (constraint)**: `encyclopedia_chunks` ma UNIQUE `(concept_key, chunk_type)` → nie da się dodać DRUGIEGO chunku `faq` do `AUTOMAT_ODDECHOWY` (jest id 19); D2.3 (INSERT nowego faq) niewykonalny — architekt zweryfikował treść i wymiar, nie constraint. **Decyzja Karola: dołączyć zatwierdzoną parę Q/A o EN 250 (tekst 1:1 z D2.3) do istniejącego faq id 19 + re-embed 3072** (content 1644→2501 znaków, 5→6 par Q/A). **Weryfikacja**: id 19 bez_wektora=false, wymiary=3072; frazy zatwierdzone verbatim (EN 250:2014, „6 barów absolutnych (odpowiednik 50 m)", „62,5 l/min", „Norma nie wyznacza dopuszczalnej głębokości", „EN 13949"), błędne (100-200 m / testy producenta / „Maksymalna głębokość normy") NIEobecne; retrieval jak tool (cosine>0.45): „co to norma EN 250?" → id 19 rank1 0.6044, „szukam automatu" → id 19 poza wynikami. **Test PROD (realny czat, HMAC /api/chat)**: conv **715** „co to norma EN 250?" → tools_used=[get_expert_knowledge], bot: „Nie wyznacza maksymalnej głębokości — norma sprzętowa, nie przepis nurkowy; test przy 50 m (6 bar abs) i 62,5 l/min; EN 250A; EN 13949" ✓ (50 m = warunek testu, NIE limit — D2.6); conv **716** regresja „szukam automatu oddechowego" → tools_used=[], bot pyta tylko o budżet + zakres zestawu, ZERO EN 250/głębokości ✓. Obie oznaczone `[test CHAT-T-138, nie klient]` (status=do_weryfikacji, verdict/updated_by NULL) — reguła E. Commit: task file per ścieżka; `_docs/10_decyzje_projektowe.md`/`_backups/`/`routes.php` NIE commitowane.)
# Wersja: 3.90 | Data: 2026-07-16 (CHAT-T-146 FRONTEND/PS DEPLOYED — zakładka „Atrybucja" w panelu PS: sprzedaż z czatu widoczna bez SSH (ADR-124, decyzje Karola 102c/104b/105a/106a, karta Chat - 17). **Świat**: SHOP+WIDGET (`newtmp2`), deploy = ręczny rsync Karola. **1 plik** `modules/divezone_chat/controllers/admin/AdminDivezoneChatController.php` (+240 linii). **Zmiany**: (1) `const TAB_ATTRIBUTION='attribution'`; (2) routing w `initContent()` (whitelist + gałąź `renderAttributionSection`); (3) link w `renderTabsNav()` **POZA blokiem `if($isAdmin)`** (106a — widoczna dla WSZYSTKICH ról, inaczej niż Analityka/CTR), po „Rekomendacje", przed „Analityka". **Dane**: MySQL PS (`Db::getInstance()->executeS`), ZERO round-tripów do Railway PG — JOIN `pr_divechat_order_attribution` + `pr_orders` + `pr_order_state(_lang)`, prefiks z `_DB_PREFIX_`, `id_lang` z kontekstu. **Dwie pułapki ADR-124 wdrożone**: kwoty z `total_paid` (NIGDY `total_paid_real` — zawyżone 2×); „opłacone" = `os.paid=1` (NIE `o.valid`). **Kolumny**: Zamówienie (reference + link `getAdminLink('AdminOrders')`), Data, Kwota, Stan płatności (nazwa + zielony badge dla paid=1), Typ (last_touch→„ostatni kontakt"/assist→„wspomaganie"), Rozmowa→zakup (format „23 min"/„2 h 15 min"/„3 dni"), Rozmowa (link do zakładki Rozmowy po `chat_session_id`). Filtr 7/30/90 (whitelist, domyślnie 30). Podsumowanie (104b): liczba, SUMA ZAMÓWIONA, SUMA OPŁACONA (paid=1), mediana czasu. **Weryfikacja na PROD (read-only PDO, days=90)**: mechanizm złapał TRZECIE realne zamówienie w nocy — QETUBCWYS 3619,00 „Zapłacone" 23 min (paid=1, valid=0) | UDRDJMBTG 312,80 „Oczekiwanie na płatność przelewem bankowym" 4 min (paid=0) | **NRRYNFJYB 1417,00 „Zapłacone" 14 min (nowe, 2026-07-16)**. Potwierdzone: `total_paid_real`=2× `total_paid` dla obu Tpay (7238=2×3619, 2834=2×1417) → musi być `total_paid`; wszystkie `valid=0` mimo paid → musi być `os.paid`. Okno 30 dni pokaże 3 zamówienia: suma zamówiona 5348,80 / opłacona 5036,00 (liczby architekta 3931,80/3619,00 sprzed dzisiejszego zamówienia). `php -l` clean. **DEPLOY 2026-07-16 (na prośbę Karola „deploj", świat SHOP+WIDGET `newtmp2`)**: backup `modules/divezone_chat/controllers/admin/_deploy_bak/AdminDivezoneChatController.php.20260716_092241.bak` (prod przed: md5 `c6309d0f`, 3579 linii, marker=0) → scp 1 pliku → **md5 prod==local `472701c6`**, 3818 linii, marker `TAB_ATTRIBUTION`=6 → `ea-php84 -l` czysty → `var/cache/prod` skasowany (PS odbudowuje) → LSCache flush przez `flush_all_litespeed.php?k=…` HTTP 200 „LSCache flushed". Render UI panelu potwierdza Karol (kontroler wymaga sesji admina — z CLI niedostępny; SQL widoku zweryfikowany read-only wcześniej). **Poza zakresem**: GA4/dataLayer/GTM (osobny task), naprawa 2× Tpay (Sklep - 31), świat BACKEND. Commit: e85131a.)
# Wersja: 3.89 | Data: 2026-07-16 (CHAT-T-138 BACKEND DEPLOYED — głębokość nie jest kryterium doboru + neutralność wobec certyfikatów (ADR-120, decyzje Karola 42c/43b/44a/45a/41b). **Problem**: bot pouczał o limicie 40 m, blokował dobór i podważał certyfikaty klienta (conv 668: zbędne pytanie „czy to do nurkowania rekreacyjnego (do 40 m)?"); dowód z PROD: automaty (kat 286) mają ZERO produktów z cechą głębokości, producenci nie podają max głębokości operacyjnej. **Zmiany w SystemPrompt.php**: (1) linia 209 — usunięty przykład „dobór automatu przez głębokość" → „dobór automatu do zimnej wody" (realne kryterium, EN250A); (2) blok „GŁĘBOKOŚĆ I KWALIFIKACJE — KRYTYCZNE" przepisany na „GŁĘBOKOŚĆ I CERTYFIKATY — NIE JEST TO NASZA ROLA": bez pouczania o 40 m, bez blokady doboru, certyfikatów ANI nie potwierdzamy ANI nie podważamy („Nie mam kompetencji do oceny certyfikatów nurkowych"), usunięty case „Deep Air Diver 60", nowy bug-case conv 668; (3) akapit o normach: EN 250 TYLKO na wprost zadane pytanie, nigdy z siebie przy doborze. NIE ruszone: wyporność worka (~803-808, głębokość = fizyka), sekcja instruktorów, blok PRODUKT WYCOFANY (T-143). **Deploy**: rsync tylko SystemPrompt.php → chat.divezone.pl (backup _deploy_bak/SystemPrompt.php.20260716_073433.bak, ea-php84 -l clean, md5 local==prod de64aa30, /api/health 200, tools.php NIETKNIĘTY). **Test PROD**: conv 710 („60 m na powietrzu, jaki komputer" + „mam Deep Air Diver 60" → dobór Shearwater Perdix 2 Ti/Tern/Suunto Nautic bez pouczeń o limicie; certyfikat: dokładnie docelowa fraza; jedna miękka wzmianka „upewnij się że masz uprawnienia" w 1. turze — do obserwacji, nie jest ostrzeżeniem o 40 m), conv 711 („szukam automatu" → pyta o budżet/zimną-ciepłą wodę/zestaw-czy-sam, ZERO pytań o głębokość — bug conv 668 nie wystąpił). Obie rozmowy oznaczone [test CHAT-T-138, nie klient] w divechat_conversation_review (bez verdict, updated_by=NULL — reguła 41b, pierwsza aplikacja). **WSTRZYMANE**: INSERT EN 250 do divechat_knowledge (część D) — treść czeka na akceptację Karola, potem embedding (instancja embeddings). Testy CLI: zielone poza stanem zastanym (PricingServiceTest 24/3, SantiSearchTest fatal). Commit: 71a70fd.)
# Wersja: 3.88 | Data: 2026-07-15 (CHAT-T-144 DOMKNIĘCIE (decyzja 95a) — dobicie 15 produktów vis='none' indeksowalnych nową regułą, ale bez wektora (delta z wersji 3.86). [Nr 3.87 zajęty równolegle przez CHAT-T-143 backend → ta pozycja jako 3.88.] Uzasadnienie Karola: „najbliższy --full" nie ma daty (pipeline nie cronowany, karta 23) → inaczej 15 produktów niewidocznych dla bota bezterminowo, koszt domknięcia = grosze. **KROK 1 dynamicznie**: `extract_products()` (2606) − wektory w tabeli (2591) = **dokładnie 15**. Skład: 9 to stale wpisy WYPRZEDAŻE usunięte przez architekta w T-142 (5424/5772/5874/5876/6182/6386/6578/6580/7474) — teraz re-indeksowalne z POPRAWNĄ konkatenacją (np. 5424 → `Morsowanie + Buty`, 7474 → `Płetwy + Płetwy Gumowe JET`); 6 genuinie nowych (4869/4870 SEALIFE, 4986/4988/4989 SP-GADGETS, 7022 IST). **KROK 2**: sync `--ids` (NIE `--full`) → 15 single + 15 multi, 0 błędów. **KROK 3 weryfikacja**: total **2606/2606**, śmieci=0, NULL desc/single=0/0, kontrolki T-142+3920 nietknięte. Cały zbiór indeksowalny wg ADR-122/ADR-123 ma teraz wektory — zero cichego długu. Bez zmian w kodzie (reguła z T-144 wystarcza). Commit: task + status.)
# Wersja: 3.87 | Data: 2026-07-15 (CHAT-T-143 BACKEND DEPLOYED — filtr `available_for_order`: produkty wycofane ze sprzedaży nie wypływają niepytane (ADR-123, decyzje 87a/88/91a/92a/93a, karta Trello 28). **Problem**: 520 aktywnych produktów afo=0 (456 z wektorem) mogło wypływać w rekomendacjach; bot proponował zakup wycofanych. **Zmiany**: (1) `enrich()` + kolumna `ps.available_for_order` (ta sama tabela co `out_of_stock`, zero round-tripów) + NOWY klucz bool — 6 wywołujących bez zmian (wzorzec T-139); (2) `ProductSearch`: parametr `filters.include_discontinued` (DEFAULT FALSE), filtr post-hoc z MySQL w OBU torach (mergeRRF + searchByPrice), `$filteredOut` reason `available_for_order=0, discontinued`; Editorial Picks NIE bypassują (92a, jawnie bez `$editorialBoosts`); afo=0 przepuszczony niesie flagę `available_for_order:false` w tool_result — klucz dokładany TYLKO dla afo=0 (afo=1 bajt w bajt jak przed zmianą); (3) decyzja 91a DETERMINISTYCZNIE: `shouldIncludeDiscontinued()` — navigational+exact_keywords ⇒ auto-include, bez polegania na modelu (test PROD v1: model NIE ustawiał parametru → bot mówił „nie znalazłem… sprawdzimy czy możemy sprowadzić"; po poprawce v2: „została wycofana ze sprzedaży i nie można jej już zamówić"); (4) decyzja 93a: filtr `visibility` USUNIĘTY z ProductSearch (searchByPrice + mergeRRF) — kryterium post-hoc: active+afo; skutek uboczny POTWIERDZONY: 11 produktów vis='none'+afo=1 wraca do wyników (poprawne — Luigi's Box je pokazuje); pole `visible` w enrich() ZOSTAJE; (5) CuratedRecommendations + PopularProducts: afo=0 wypada ZAWSZE bez parametru, reason `discontinued` w skipped (Curated dodatkowo error_log jak staleness); (6) SystemPrompt: blok PRODUKT WYCOFANY ZE SPRZEDAŻY — „był w ofercie, został wycofany" + alternatywa; ODRÓŻNIENIE od braku stanu („zamówimy" vs „już go nie ma, nie wróci"); zakaz proponowania zakupu i surowej flagi. **Deploy**: 5 plików src/ (Shop/MysqlProductEnrichmentService, Tools/ProductSearch, Tools/CuratedRecommendations, Tools/PopularProducts, Chat/SystemPrompt), backupy `_deploy_bak/20260715_202636_CHAT-T-143` + `_v1`, ea-php84 -l 5/5 OK, md5 local==prod 5/5, smoke `/api/health` 200 (PG+MySQL true). **Testy lokalne**: nowy `tests/Tools/AvailableForOrderFilterTest.php` 14/14; pełna suita bez regresji (zastane: PricingServiceTest 24/3, SantiSearchTest fatal konstruktora). **Test PROD**: „szukam torby na sprzęt nurkowy" → 3920 (Torba MARES Cruise Backpack Mesh Deluxe) NIE pojawia się, filtr widoczny w debug ✓; „macie torbę Mares Cruise Backpack Mesh Deluxe?" → bot: wycofana, nie można zamówić, 2 alternatywy Mares, NIE proponuje zakupu, bez surowej flagi ✓. Rozmowy 692–695 oznaczone `[test CHAT-T-143, nie klient]`, verdict NULL, updated_by NULL (decyzja 41b). BEZ `config/tools.php` (dryf T-129), BEZ `routes.php` (cudze T-090).)
# Wersja: 3.86 | Data: 2026-07-15 (CHAT-T-144 EMBEDDINGS — usunięcie filtra `visibility` z pipeline (ADR-123 nota 93a, decyzja 94a). Operacja LOKALNA (tunel SSH), ZERO rsync. **Problem**: `extract_products.py` `PRODUCTS_SQL` miał `AND ps.visibility != 'none'` — filtr visibility w SAMYM pipeline (dlatego w T-142 `--ids 7602` nie działał, trzeba było override). Sprzeczne z ADR-123: visibility='none' NIE ukrywa produktu (Luigi's Box pokazuje go klientom), a pipeline odmawiał mu wektora/świeżej reguły. **Zmiana**: usunięty `AND ps.visibility != 'none'` z WHERE; `ps.visibility` ZOSTAJE w SELECT, `ps.active=1` bez zmian → `extract_products()` zwraca 2606 (było 2155). **Backup**: `_backups/divechat_product_embeddings_20260715_przed_T144.sql` (~203 MB, 2591 w.). **Re-embed 436 vis='none' (dynamicznie z PROD, sync `--ids`, NIE `--full`)**: 436/436 znalezione → 436 single + 436 multi, 0 błędów. **Weryfikacja**: count **2591 bez zmian** (upsert istniejących, 0 insertów); 8 afo=1 vis='none' (sprzedawalne) — 5 z konkatenacją (4270 `Instrumenty pomiarowe + Konsole`, 4682, 4710, 5256, 6331 `Butle nurkowe + Butle Aluminiowe`), 3 (6809/6810/6811) z pustą kategorią = WYPRZEDAŻE-outlet („NOWE pianki Box, Zwroty konsumenckie", wszystkie kat. w poddrzewie 467 → empty POPRAWNE per T-142, trzymane przez `get_wyprzedaz_products`, embed na nazwie+marce+opisie); wszystkie 8 mają desc NOT NULL. 3920 (Torba MARES Cruise, test PROD z ADR-123) → `Torby i Skrzynie + Torby na Sprzęt i Plecaki Plażowe`. Kontrolki T-142 NIETKNIĘTE (2369/7641/7545/7648/7602). Śmieci=0, wielokategoryjnych 1867→**2186**, NULL desc/single=0/0. **DELTA**: extract zwraca 2606 vs tabela 2591 → ~15 produktów vis='none' indeksowalnych nową regułą, ale bez istniejącego wektora — poza zakresem (KROK 4: count 2591 bez zmian), wejdą przy najbliższym `--full`. Commit `extract_products.py` + task; `_docs/10_decyzje_projektowe.md` (ADR-y architekt) / `_backups/` / `*.jsonl` / `routes.php` NIE commitowane.)
# Wersja: 3.85 | Data: 2026-07-15 (CHAT-T-142 EMBEDDINGS — `category_name` z KONKATENACJI wielu kategorii zamiast samej `id_category_default` (ADR-122). Operacja LOKALNA (tunel SSH), ZERO rsync/deployu. **Problem**: `PRODUCTS_SQL` brał `category_name` wyłącznie z `id_category_default`; 86% katalogu należy do >1 kategorii i tracił tę informację w embeddingu. **Zmiana kodu (`extract_products.py`)**: (1) `463` Polecane + `467` WYPRZEDAŻE do `EXCLUDED_CATEGORY_IDS`; (2) `category_name` z korelowanego `GROUP_CONCAT(pr_category_product)` — filtr nested-set na wykluczone+potomków, root wykluczony, sort `level_depth,name` (drugi klucz konieczny dla determinizmu = tego samego wektora), `SUBSTRING_INDEX(...,4)`, sep `" + "`, whitelist 476 w OR; (3) `is_allowed()` fallback dla root-cat gdy `category_name` niepuste; (4) `get_wyprzedaz_products()`+`WYPRZEDAZ_ROOT_ID` — outlet w indeksie mimo pustej kategorii. **KROK 4 kontrolki EXACT**: 2369→`Automaty Oddechowe + APEKS + Zestawy rekreacyjne + ZESTAWY Apeks`, 7641→`Komputery Nurkowe + Komputery SHEARWATER`, 6803→`Komputery Nurkowe + Komputery MARES + Zegarki nurkowe`, 7545→`Skafandry mokre + Skafandry suche`, 7648→`Automaty Oddechowe + SCUBAPRO + Zestawy rekreacyjne`; Polecane/PLN/PREZENTY/WYPRZEDAŻE = 0 trafień. **3 ODSTĘPSTWA (przesłanki tasku/ADR upadły, zweryfikowane na danych)**: (1) **467 WYPRZEDAŻE nie było w EXCLUDED** wbrew ADR („oba korzenie wykluczone" dotyczyło tylko 368 PREZENTY) — dodano; (2) wykluczenie 467 osieroca **14 sprzedażowych produktów outletowych** (jedyne kat. w poddrzewie WYPRZEDAŻE, m.in. 7567 Torba Duffle) → **decyzja Karola: trzymamy w indeksie** przez `get_wyprzedaz_products` (embed na nazwie+marce); śmieci bez żadnej kategorii (26=25 puste+5910) nadal odrzucone, zero regresu (25 nigdy nie w indeksie); (3) **visibility=none NIE ukrywa produktu** — wyszukiwarką sklepu jest Luigi's Box (zewn.), ignoruje `visibility` PS → 444 produktów visibility=none to NORMALNE produkty, NIE zombie, NIE usunięto; właściwe kryterium „bot poleca" = `available_for_order` (osobny CHAT-T-143). Decyzja 68b (usunięcie 7602) była na tej samej fałszywej przesłance → **7602 przywrócony** (override po ID, bo `extract_products` filtruje visibility; `Automaty Oddechowe + Automaty stage + TECLINE`). **KROK 5 brama (72b)**: backup `_backups/divechat_product_embeddings_20260715_przed_T142.sql` (~197 MB, 2573 w.); **ustalenie: `category_name` zasila TYLKO `embedding_desc`+single `embedding`** (`build_multivector_texts`), NIE name/jargon. Próbka 32 PRZED→PO: name Δ=0.0000, jargon Δ=0.0000, **desc rośnie** (7641 +0.0005, 7648 +0.0079, 7647 **+0.0421**), single ±0.007 → zero regresu na mocnych kolumnach → **zielone światło Karola**. **KROK 6 pełny re-embed**: kryt.#2 domknął architekt (usunął 9 stale WYPRZEDAŻE wpisów visibility=none, 2599→2590); single `batch_embed_products.py --full` (Batch API) 2155/0 błędów; multi Batch API (7770 req) utknął 0/7770 40 min (kolejka OpenAI) → anulowany, zrobiono sync RESUMABLE dla 2155 (444 visibility=none pominięte — kategoria bez zmian), 2155/2155. **PO**: **2591** wpisów (2590+7602), forbidden=0, PREZENT=5 (vouchery), wielokategoryjnych 1867, NULL name/desc/jargon/single = 0/0/0/0. Semantyka: 7647 desc rank3 (0.791↑)/jargon rank1, 7648 name rank3/jargon rank2, 7641 rank1 wszędzie. **DŁUG (CHAT-T-143)**: cleanup `available_for_order=0` (456 z wektorem); `id_category_default` root w PS (decyzja 67, ryzyko URL/301). `_docs/10_decyzje_projektowe.md` — architekt dopisuje własne noty korygujące (CC nie edytuje). Commit `extract_products.py` + task.)
# Wersja: 3.84 | Data: 2026-07-15 (CHAT-T-141 EMBEDDINGS — backfill 50 produktów 2026 (ID 7561–7648) + usunięcie 37 zombie z `divechat_product_embeddings`. Operacja na DANYCH, uruchomiona LOKALNIE (tunel SSH z laptopa), ZERO deployu/rsync. **Cel**: bot nie znał 50 produktów z 2026, w tym zestawów 7647 (APEKS MTX-RC) i 7648 (Scubapro MK17/C370), które CHAT-T-140 właśnie udostępnił do zamówienia. **Snapshot PRZED**: 2561 wpisów / 2561 unikalnych / updated 2026-02-23…2026-05-15 (zgodny z taskiem). **Backup (OBOWIĄZKOWY przed DELETE)**: `_backups/divechat_product_embeddings_20260715_przed_T141.sql` — pg_dump 18.4 (server PG 18.2; lokalny pg_dump 14 odrzuca major mismatch → użyto binarki z libpq 18.4), 205 843 028 B (~196 MB), 2561 wierszy COPY zweryfikowane. **KROK 3 — ODCHYLENIE OD ZAŁOŻEŃ TASKU**: `embed_target_products.py --ids` zembedował 42/50, filtr `extract_products()` odrzucił 8 (task twierdził „wszystkie 50 przejdą" — NIEPRAWDA). Przyczyny z kodu: 7 produktów (7561/7563/7567/7597/7598/7640/**7648**) ma `id_category_default=2` = root „Główna", a `ALLOWED_CATEGORIES_SQL` bierze tylko ścisłych potomków roota → `is_allowed()` odrzuca (mimo że wszystkie 7 należą do poprawnych kategorii przez `pr_category_product`); 1 (7602 TECLINE R1) aktywny + w dozwolonych kategoriach, ale `visibility='none'`. Wszystkie 8 `active=1`. **Decyzja Karola**: override-embed wszystkich 8 (w tym 7648 wymagany kryterium akceptacji #1). Rozwiązanie: jednorazowy skrypt (scratchpad, NIE commitowany, NIE modyfikuje `extract_products.py` ani `EXCLUDED_CATEGORY_IDS`) — pobiera 8 po ID z pominięciem wyłącznie filtru kategorii/visibility, `category_name` podmienia na najniższą-id DOZWOLONĄ realną kategorię (7648→Automaty Oddechowe, 7561→Latarki nurkowe, 7602→Automaty Oddechowe…), single-vector upsert + multi-vector przez `--skip-single`. Efekt: wszystkie 50 mają wpis, single+name+desc+jargon NOT NULL (0 NULL). **DŁUG (osobna karta/decyzja Karola)**: trwały fix = poprawić `id_category_default` tych 7 produktów w PrestaShop (root→właściwa kategoria) + decyzja o visibility 7602; przy następnym pełnym przebiegu pipeline'u bez fixu źródła wrócą do odrzucenia. **KROK 4 — zombie DYNAMICZNIE (decyzja 66b)**: aktywnych w sklepie (active=1, id_shop=1) = 2664; zombie (w PG, poza aktywnymi) = **dokładnie 37** (zgodne z pomiarem taska), lista w raporcie/commicie (m.in. 3809, 4373, 4582 … 7564–7584). DELETE dokładnie 37. Żaden z 50 targetów ani 8 override NIE był zombie (7602/7648 active=1). **Snapshot PO**: 2574 / 2574 / max updated 2026-07-15. Arytmetyka DOKŁADNA: 2561 + 50 − 37 = **2574**. **KROK 6 test semantyczny (real bot path = multi-vector, `ProductSearch.php` używa `embedding_name`/`_desc`/`_jargon`, NIE single `embedding`)**: „zestaw automatu Apeks MTX-RC" → 7647 name rank3 (0.774)/jargon rank1 (0.768); „Scubapro MK17 zestaw" → **7648 name rank2 (0.861)/jargon rank1 (0.860)**; „Shearwater Perdix 3" → 7641 rank1 wszędzie (0.914). Kryterium #1 (7647 i 7648 wychodzą) SPEŁNIONE. (Na nieużywanej przez bota kolumnie single `embedding` 7648 jest poza top5 — outrankowany przez warianty MK17 EVO — ale bot tej kolumny do produktów nie odpytuje.) Skrypty pipeline NIETKNIĘTE. Commit tasku + docs; `_backups/` i `standalone/config/routes.php` (cudza zmiana CHAT-T-090) NIE commitowane.)
# Wersja: 3.83 | Data: 2026-07-15 (CHAT-T-139 BACKEND DEPLOYED — `url` + `name` w `get_curated_recommendations` (ADR-121, decyzje Karola 52a/52a-i, karta Trello 18). **Problem**: bot w doborze ZESTAWÓW podawał gołe `https://divezone.pl` zamiast URL produktu (4 rozmowy/30 dni: conv 668/657/626/598 vs 130 z pełnym `.html`) — narzędzie zwracało botowi tylko product_id+cenę, bez `name` i `url`, więc link fabrykował. **Fix (52a)**: PL name+url dołożone do WSPÓLNEGO enrichmentu, nie do wywołującego — `MysqlProductEnrichmentService::enrich()` dostał drugi LEFT JOIN `pr_product_lang` id_lang=1 (obok EN id_lang=3) + nowe klucze `name`/`url`; budowa URL wydzielona do `buildProductUrl(?string $slug): ?string` (public static jak computeBruttoPrice — testowalna bez MySQL) z guardem 1:1 z PopularProducts: pusty/NULL slug → url=null, NIGDY goła domena ani `/.html` (brak danych ma być jawny, bo „prawie poprawne" bot dopełni zmyśleniem). `CuratedRecommendations::$available[]` przekazuje `name`+`url` botowi (kolejność kluczy jak w PopularProducts). Wsteczna zgodność twarda: wyłącznie NOWE klucze, 6 wywołujących enrich() bez różnicy (diff tylko dodaje linie). **Weryfikacja**: nowy test `tests/Shop/ProductUrlTest.php` 6/6; suita 17/19 plików pass (2 fail = stan zastany zweryfikowany stashem na HEAD: PricingServiceTest 24/3, SantiSearchTest — nieaktualny konstruktor ProductSearch w teście); `php -l`/`ea-php84 -l` clean. **DEPLOY 2026-07-15**: backup `_deploy_bak/20260715_120534/`, przed rsync md5 prod==HEAD (zero dryfu), po rsync md5 local==prod (MysqlProductEnrichmentService `ceaa1f33...`, CuratedRecommendations `92429394...`), smoke `/api/health` 200 PG+MySQL true, `config/tools.php` NIEDOTKNIĘTY (dryf T-129). **Test PROD PASS**: „automat do 2000 zł" → tools_used=get_curated_recommendations, bot podał pełny URL `https://divezone.pl/apeks-atx40-ds4-octopus-atx40.html`; rozmowa conv 675 oznaczona note=`[test CHAT-T-139, nie klient]`, verdict=NULL, updated_by=NULL. **Znany dług (ADR-121)**: ProductDetails ~135 bez guarda na pusty slug (`/.html`), 3 rozproszone miejsca budowy URL → osobna karta refaktoru. Commit 7d814a1.)
# Wersja: 3.82 | Data: 2026-07-14 (CHAT-T-132 BACKEND DEPLOYED — nowe narzędzie `get_popular_products`: dynamiczna popularność z PrestaShop na żywo (ADR-115, decyzje 15a/17b/18a/19a/20a/21b/22a). **Cel**: bot poleca wg REALNEJ sprzedaży + żywej dostępności zamiast ręcznych list (statyczny seed curated dla płetw odrzucony — „stałe listy rozjeżdżają się cicho"); zamyka też pomyłkę JET vs paskowe (czaty 605/606) przez semantyczne klucze kategorii z enum. **Implementacja**: `Tools/PopularProducts.php` (nowy) — źródło `pr_orders`(valid=1)+`pr_order_detail` na żywo (~10 ms zmierzone na PROD, bez materializacji), okno domyślnie 6 mcy (clamp 1-24), mapa `CATEGORIES` w klasie: fins_recreational=473/fins_jet=415/fins_snorkel=472 (dokładanie kategorii = jeden wpis); dwie ODDZIELNE sekcje: `bestsellers` (top wg sztuk, sold_qty) + `new_arrivals` (date_add<90 dni, okno łaski dla nowości, added_date; overlap → flaga is_new w bestsellers bez duplikatu); oba zestawy przez enrich (twarda zasada ADR-065: mysql_missing/active=false/visibility=none/unavailable → skipped z powodem), `max_price` odcina po finalnej cenie brutto z enrichment; statusy ok/unknown_category/no_available/error (MySQL down → czytelny komunikat, bez fabrykacji). Rejestracja w `config/tools.php`; SystemPrompt: sekcja POPULARNE PRODUKTY (rozgraniczenie popular=co kupują inni / curated=co MY polecamy / search=konkretny model; nakaz WYBORU klucza z enum + dopytania o zastosowanie płetw; wzór narracji dwusekcyjnej) + get_popular_products dopisane do reguły DOBÓR POD BUDŻET (ADR-114). **Weryfikacja**: `php -l` clean ×3; test jednostkowy `tests/Tools/PopularProductsTest.php` 20/20 (mapowanie kluczy, clamp months, schema enum==mapa, unknown_category bez dotykania MySQL); dokładne SQL narzędzia z placeholderami przetestowane na PROD MySQL: 473 → GO Sport(19)/Avanti Quattro+(12)/Quattro 4X(8), 10.1 ms; new_arrivals 473 → 7618 RecTec (2026-04-23); 415→15 rows, 472→13 rows. **DEPLOY 2026-07-14 (ADR-089)**: backup, rsync 3 plików, md5 prod==local, `php -l` ea-php84 clean ×3, `/api/health` 200. **INCYDENT przy deployu (~1 min 500)**: repozytoryjny `config/tools.php` rejestruje TEŻ `ProductCombinations` (CHAT-T-129, zacommitowany ale CELOWO NIEWDROŻONY — czeka na kolumnę nazwa_pl), a pliku klasy nie ma na serwerze → fatal "Class not found", health 500; przywrócono backup, na PROD poszła wersja produkcyjna tools.php + wyłącznie rejestracja PopularProducts. **UWAGA DRYF repo≠prod**: NIE deployować repozytoryjnego tools.php dopóki CHAT-T-129 nie zostanie wdrożony (albo wdrożyć oba naraz). **Test PROD (realny czat)**: (A) "jakie płetwy paskowe polecacie/co się najlepiej sprzedaje" → dwie sekcje: bestsellery GO Sport 567/Avanti Quattro+ 544,85/Superchannel 357,99/Quattro 4X 760,75/Seawing Nova 688,50 + nowość RecTec 299 (osobno) ✓; (B) "do 600 zł" → Quattro 4X i Seawing odcięte, wiodąca GO Sport 567 (najbliżej górnej granicy, reguła ADR-114) ✓ kryterium 2; (C) "jakie płetwy najczęściej kupują" bez typu → bot DOPYTUJE paskowe/JET/kaloszowe zamiast zgadywać ✓; linki z polskimi znakami w slugu (RecTec, 4X) → 301→200 na kanoniczną stronę produktu ✓. **Domknięcie**: karta Trello → Zrobione; conv 605 (mało popularne płetwy/dostępność/Mares+Tusa first) → problem_rozwiazany (adresowane wprost przez narzędzie); conv 606 ZOSTAJE otwarta z notatką (sedno = warianty/kolor w koszyku → CHAT-T-129 + procedura naprawy koszyka, osobny wątek).)
# Wersja: 3.81 | Data: 2026-07-14 (CHAT-T-131 BACKEND DEPLOYED — dobór automatów: budżet + „gotowy zestaw" z manometrem (ADR-114, decyzje Karola 9a+8b). **Problem**: (1) bot permanentnie prowadził klienta zestawem ATX40/DS4 (curated prio 1, ~2390 zł) niezależnie od budżetu — czat 609: budżet 3500 zł, bot poleca najtańszy; (2) „gotowy zestaw" mylony z zestawem bez manometru — czat 596 (sklep NIE ma cechy manometru, jedyny sygnał = „manometr"/„konsola" w nazwie). **Fix (SystemPrompt.php, 2 nowe sekcje po bloku curated + odnośnik w UŻYJ PODANEGO PARAMETRU)**: (9a) DOBÓR POD BUDŻET KLIENTA — rekomendacja wiodąca = produkt najbliższy GÓRNEJ granicy budżetu, `priority` curated = kolejność kuratorska NIE ranking, do ~10% ponad budżet z jawnym zaznaczeniem; (8b) „GOTOWY ZESTAW" = I st.+II st.+octopus+MANOMETR — procedura: search_products category="Automaty Oddechowe" + exact_keywords ["manometr"]/["konsola"] + in_stock_only (kategoria 416 „Zestawy rekreacyjne" jako pierwsze źródło po uporządkowaniu; embeddingi mają category_name=marka dla większości z 416), fallback = bazowy zestaw + osobny manometr z ceną łączną (montaż przy odbiorze, zaczep promptu ~96). Reguła budżetu prezentowego bez regresji. Weryfikacja: `php -l` clean. **Seed WYKONANY na Railway (akceptacja Karola 23a, sql/041 + rollback)**: `regulator_recreational` — pid 3192 APEKS XTX50/DST + Octopus XTX40 (3566 zł brutto, qty=3 od ręki) jako prio 2 (dziura cenowa 2390→3776; obecnie 2368 ATX40 2390/4szt, 7421 EVX200 4616/2szt, 5983 Legend 3776/0szt). **DEPLOY 2026-07-14 (ADR-089)**: seed 041 na Railway (2368→1, 3192→2, 5983+7421→3), backup `_deploy_bak/SystemPrompt.php.20260714_163805`, rsync, md5 prod==local, `php -l` ea-php84 clean, `/api/health` 200. **Test PROD po deployu (realny czat)**: (1) „budżet 3500 zł na automat" → bot prowadzi XTX50/DST 3133 zł (z curated 3192, od ręki), Legend jako alternatywa w budżecie, ATX40 tylko jako opcja oszczędnościowa ✓; (2) „gotowy zestaw" (budżet 3000, Polska, OWD) → bazowy XTX50/DST + manometr OMS SPG 340 zł z ceną łączną ~3473 zł, jawne zaznaczenie przekroczenia budżetu, montaż przy odbiorze; ATX40+manometr ~2420 zł jako alternatywa ✓. Karta Trello → „Do weryfikacji".)
# Wersja: 3.80 | Data: 2026-07-14 (CHAT-T-130 BACKEND DEPLOYED — fix wycieku rabatu grupowego B2B w `get_product_details` (ADR-113). **Problem**: bot ogłaszał nieistniejący rabat -25% i zaniżoną cenę (czat 649 maska TUSA Zensee Pro 619 zł → „464,25 zł", czat 641 xDEEP Ghost Deluxe); klient w koszyku widział pełną cenę. **Przyczyna**: `ProductDetails.php` osobnym zapytaniem pobierał `pr_specific_price` BEZ filtrów `id_group`/`id_customer`/`id_product_attribute` i doklejał `special_price` zawsze gdy wiersz istniał — łapał rabat grupy `id_group=9` „B2B / Instruktorzy" (dowód PROD: 6379/4517/6834/7460 mają wyłącznie wiersze id_group=9, reduction=0.25). **Fix (wariant A, jedno źródło prawdy)**: osobne zapytanie USUNIĘTE; `special_price` wyprowadzane z enrichment (`MysqlProductEnrichmentService`, poprawny filtr `id_group IN (0,1)`): gdy `price_before_discount` istnieje → `{reduction: round(1-price/pbd,4), type:'percentage'}`, gdy brak publicznej promocji → pola NIE ma. Weryfikacja: `php -l` ea-php84 clean, ProductPriceTest 10/10, dane PROD read-only potwierdzają diagnozę; kandydaci do testu regresji publicznej promocji po deployu: 3536 (amount 131 brutto), 5090 (percentage 0.2). **DEPLOY 2026-07-14 (ADR-089)**: backup `_deploy_bak/ProductDetails.php.20260714_*`, rsync, md5 prod==local, `php -l` ea-php84 clean, `/api/health` 200. **Test PROD po deployu**: `get_product_details(6379)` → price=619, BRAK `special_price`/`price_before_discount` ✓; regresja publicznych promocji: 3536 → 430→299 + special_price 0.3047 ✓, 5090 → 845→676 + special_price 0.2 ✓. Karta Trello → „Do weryfikacji".)
# Wersja: 3.79 | Data: 2026-07-09 (CHAT-T-128 BACKEND DEPLOYED — zakaz kierowania klienta poza divezone.pl (fix promptu, świat `chat.divezone.pl`). **Dowód z produkcji**: recenzja `divechat_conversation_review` id=212 / conversation_id=598 — bot napisał klientowi „Sam balast … kupujesz osobno — np. **w sklepie sportowym** lub u nas w kategorii Balast" = skierowanie do konkurencji. Grep potwierdził że fraza NIE jest hardcodowana (0 trafień w kodzie) — model wypełnił lukę promptu, bo brakowało jakiegokolwiek zakazu odsyłania na zewnątrz. **Fix** (1 plik `SystemPrompt.php`, +18 linii): nowa sekcja „ABSOLUTNA ZASADA — NIGDY NIE KIERUJ KLIENTA POZA DIVEZONE.PL" wstawiona po bloku „NIE FABRYKUJ AWARII SYSTEMU" (l.420), przed DRILL-DOWN — naturalne sąsiedztwo reguł „co robić gdy nie znalazłeś produktu". Zakaz sugerowania sklepu sportowego/budowlanego/Allegro/OLX/marketów nawet dla drobnych akcesoriów (balast, ciężarki, śrut); przy braku w ofercie → wprost „nie mamy" + zamiennik LUB obsługa (dive@divezone.pl / 56 307 03 03); jedyne dozwolone odesłania zewnętrzne = strony samego divezone.pl (b2b, kontakt, serwis) i instytucje niehandlowe (federacje, urzędy); few-shot z bugiem czatu 598. **DEPLOY** (ADR-089, zgoda Karola): pre-check md5 serwera==baza (`ba028f92`, 877 linii, brak rozjazdu) → backup `_deploy_bak/SystemPrompt.php.20260709_152546` → rsync `chat.divezone.pl/src/Chat/` → **md5 prod==local `5103e51f`** → `ea-php84 -l` clean → `/api/health` 200 (`postgres:true`/`mysql:true`, php 8.4.23) → marker `NIGDY NIE KIERUJ KLIENTA POZA` obecny na prod. **Test funkcjonalny** (real chat, conversation_id=615, model claude-sonnet-4-6): pytanie „czy do skrzydła muszę dokupić balast?" → odpowiedź o pasie balastowym / zintegrowanych kieszeniach + oferta pokazania zestawów na divezone, ZERO wzmianki o sklepie sportowym czy jakimkolwiek obcym sklepie. Commit `54e3cf4` push origin main (tylko SystemPrompt.php, per-ścieżka). Świat BACKEND — żadnego rsync do newtmp2, żadnego LSCache. **Follow-up**: rekord id=212 w `divechat_conversation_review` gotowy do przestawienia na `verdict='problem_rozwiazany'` (przestawia Karol w panelu PS).)
# Wersja: 3.78 | Data: 2026-07-06 (CHAT-T-127 GOTOWY DO DEPLOYU — selektywny purge LSCache po tagach zamiast pełnego flusha (ADR-111). **Problem**: `newtmp2/flush_all_litespeed.php` wysyła `X-LiteSpeed-Purge: *` = pełny flush całego kontenera przy KAŻDYM wywołaniu; po deployu widgetu albo edycji jednego produktu PS kosztownie odbudowuje wszystkie strony. **Rozwiązanie**: nowy `purge_litespeed.php` (stary `flush_all_litespeed.php` ZOSTAJE nietknięty jako świadomy fallback „spal wszystko"). Nagłówek `X-Litespeed-Purge2` + składnia `tag=<PREFIX>_<TAG>,…` (źródło prawdy = kod pluginu `modules/litespeedcache/classes/`: Cache.php/Config.php/Helper.php). Prefiks liczony runtime `'PS'.substr(md5(realpath(__DIR__)),0,5)` (2b — plik leży w root sklepu, `realpath(__DIR__)`==`_PS_ROOT_DIR_`; na prod = `PSd6615`), override `?prefix=` (walidacja `^PS[0-9a-f]{5}$`), BEZ bootstrapu config.inc.php (lekki). **Parametry (1c, wiele typów łączonych w 1 wywołaniu)**: `?product=ID[,ID]`→P, `?category`→C, `?manufacturer`→M, `?supplier`→L, `?cms`→G, `?home=1`→H, `?search=1`→SR, `?tag=<goły>` (whitelist `^[A-Za-z0-9]{1,20}$`), `?all=1`=awaryjny pełny flush `*` (NIE domyślny). **Bezpieczeństwo**: `?k=<KLUCZ>`+`hash_equals` (NOWY klucz, nie z flush_all — TODO Karol ustawia sekret przed deployem), twarda walidacja (ID→ctype_digit>0, finalny nagłówek tylko `[A-Za-z0-9_,=]`); zero poprawnych tagów i brak `?all=1` → 400 bez wysyłania purge. **Miejsce w repo** (decyzja Karola): nowy katalog `_ops/newtmp2_root/purge_litespeed.php` — dedykowany na skrypty operacyjne root-a sklepu (Świat 2), którego dotąd nie było (flush_all też niewersjonowany). **Weryfikacja**: `php -l` clean; driver CLI 7 wejść (header() no-op w CLI, echo zwraca string nagłówka): `product=123`→`tag=PSd6615_P123`, `product=123,124&manufacturer=7`→`tag=PSd6615_P123,tag=PSd6615_P124,tag=PSd6615_M7`, `home=1`→`_H`, `tag=SR`→`_SR`, `tag=zły@znak`→400 bez purge, `all=1`→`*`, brak parametrów→400 bez purge. Commit `5271ea1` push origin main. **STOP przed rsync (ADR-089)** — deploy = ręczny rsync Karola do `~/public_html/newtmp2/purge_litespeed.php` (Świat 2), po ustawieniu `PURGE_KEY`. Weryfikacja prod: curl `-D -` na URL skryptu, potwierdzenie że tylko dana strona przechodzi w `miss`.)
# Wersja: 3.77 | Data: 2026-07-02 (CHIPY: ścieżka nawigacji + render panelu jak widget + przekreślenie ceny — pakiet CHAT-T-121/122/124/125/126, ADR-110. **Diagnoza**: „Napisz czego szukasz" w tytułach rozmów = label przycisku `target:ai` na węzłach-liściach (nie chip nawigacyjny); klik wysyłał label jako wiadomość user → śmieć w historii + zły tytuł w panelu; intencja klienta szła osobnym `chip_context` i nie była utrwalana. **CHAT-T-121 (widget)**: przycisk `target:ai` = wejście w pisanie (`enterWriteMode`, NIE wiadomość); one-shot `pendingChip` dokleja `chip_context` + strukturalny `chip_path` do pierwszej realnej wiadomości; `buildChipPath()` z `chipStack` (level = i+1, niezmiennik potwierdzony na drzewie); `transport.js` dosyła `chip_path`. **Seed 040**: usunięte zbędne przyciski `target:ai` z 12 liści (hybrydy serwis/wysylka nietknięte), wykonany i zweryfikowany na Railway (12/12 `buttons=[]`). **CHAT-T-122 (backend)**: migracja 039 (`chip_path jsonb` + partial GIN `WHERE chip_path IS NOT NULL`); `resolveChipPath` walidacja; utrwalanie raz na rozmowę (`WHERE chip_path IS NULL`, idempotent); fix `first_user_message` — DYNAMICZNA lista labeli `target:ai` z `divechat_chip_nodes` (`<> ALL($labels)`, jeden SELECT/żądanie), korekta 13a→18a (stała klasa `ChipButtonLabels` wycofana — rozjeżdżałaby się z seedem). **CHAT-T-125 (backend+panel PS)**: `/api/conversations/{sid}` zwraca `chip_path`; breadcrumb ścieżki w panelu recenzji MODUŁU PS (`AdminDivezoneChatController` „Przebieg rozmowy") — NIE w wygaszanym standalone `/admin` (ADR-070). **CHAT-T-124 (widget)**: render `~~przekreślenie~~` jako `<del>` (stara cena promocyjna z `pr_specific_price`, realna nie fabrykacja, decyzja 36a). **CHAT-T-126 (backend+panel PS)**: `formatConvBubbleText` przepisany na wierny port `renderMarkdown` widgetu (link `[label](url)` podpięty pod label, `**bold**`, `~~del~~`, listy) — panel renderuje jak widget; chipy klikane jako bąbelki klienta na początku wątku (kolejność ścieżki); fix licznika `message_count` w 3 zapytaniach (`jsonb_array_elements` z filtrem role user/assistant + niepusta treść, zamiast `jsonb_array_length` całej tablicy z tool_*); breadcrumb 14px + literal UTF-8 „Ścieżka:"; czcionka czatu 13→14px. **DEPLOY** (ADR-089, dwa światy): backend → `chat.divezone.pl/src` (rsync+backup+md5 match+`php -l` ea-php84+smoke `/api/health` 200); moduł → `newtmp2/modules/divezone_chat/` (rsync 3 plików+md5 match 3/3+`php -l`+markery, `var/cache/prod`+LSCache przez `flush_all_litespeed.php`). Dowód end-to-end: rozmowa `29e2c4a8` ma `chip_path [dobor L2 › komputer L3]`. **CLAUDE.md**: nowa sekcja „Mapa infrastruktury i wdrożeń" (dwa światy, dwa panele admina ADR-070, cache, brzytwa Okhama). Commity: `a383ce9`(121)/`01f5aa8`(122)/`476b782`(124)/`e8c132a`+`176522b`(125)/`32112b5`(126)/`7b34649`(CLAUDE)/`f8f6576`(docs). **HORYZONT**: (a) statystyki klikalności chipów — czysty SQL z `chip_path` (`jsonb_array_elements` → GROUP BY `node_key`), bez osobnej tabeli zdarzeń, fundament (kolumna+GIN) gotowy; (b) selektywne czyszczenie LSCache — obecny `flush_all_litespeed.php` czyści CAŁY cache, LiteSpeed wspiera purge po tagach (URL/produkt po ID); rozpoznać plugin LiteSpeed dla PS + przerobić skrypt na parametr.)
# Wersja: 3.76 | Data: 2026-06-29 (CHAT-T-117 BACKEND DEPLOYED — paczkomat NIE jako sposób odsyłania sprzętu DO nas (serwis/zwrot/reklamacja) → kurier na adres (ADR-107). Wzorzec P7 z analizy czatów (czat 442): bot podpowiedział odesłanie automatu do serwisu „dowolną paczkomatą" — BŁĄD, nie odbieramy z paczkomatów. **1 plik** `SystemPrompt.php` (+2/-1). **ZWROTY** (nowy bullet): zwrot/reklamacja odsyłane KURIEREM na ul. Storczykowa 5, NIE paczkomat. **SERWIS AUTOMATU**: „dowolnym kurierem" → „WYŁĄCZNIE kurierem" + jawny zakaz paczkomatu DO serwisu. **KRYTYCZNE rozróżnienie kierunku** (decyzja 45b): paczkomat jako DOSTAWA ZAKUPÓW DO KLIENTA pozostaje (l.108 doręczenia sobotnie, l.146 InPost EU, l.647 get_shipping_info — NIETKNIĘTE, grep przed/po potwierdza); poprawka tylko kierunek klient→sklep. Decyzja 46a: bez wprowadzania usługi „InPost Paczkomat Kurier" (na później). **Kontrola wstępna**: plik miał zmiany CHAT-T-115 (`url_en`) — deploy 117 ich NIE cofnął. **DEPLOY** (ADR-089): pre-check md5 serwera==baza (`30a2ec98`=115) → backup `_deploy_bak/CHAT-T-117/` → scp `chat.divezone.pl/src/Chat/SystemPrompt.php` → **md5 prod==local `ba028f92`** → `php -l` ea-php84 clean → `/api/health` 200. Commit `d720e8e` push origin main.)
# Wersja: 3.75 | Data: 2026-06-29 (CHAT-T-118 BACKEND DEPLOYED — `serialize_precision=-1` w bootstrapie dla poprawnych floatów w JSON (ADR-108). **Problem**: serwer ea-php84 ma `serialize_precision=100` → `json_encode(566.28)`→`566.2799999…`; trafia w `tool_result` do modelu. ADR-106 (price_eur) uaktywnił to przy KAŻDEJ cenie EUR (zawsze nie-okrągłe). **Fix**: `ini_set('serialize_precision', -1);` w `standalone/public/index.php` po `declare`, przed `require autoload` (1 plik, +7 linii z komentarzem). `-1`=najkrótsza poprawna reprezentacja (wartość produkcyjna); globalnie wszystkie floaty (PLN+EUR+similarity). NIE dotknięto dyrektywy `precision`. **DEPLOY** (ADR-089): pre-check md5 serwera==baza (`1374b255`=HEAD~1) → backup `_deploy_bak/CHAT-T-118/` → scp `chat.divezone.pl/public/index.php` → **md5 prod==local `19d4cadc`** → `php -l` ea-php84 clean. **Smoke**: `/api/health` 200; real-path (ini_set(-1) jak w index.php) → `ProductDetails` 5986 = `{"price":2380,"price_eur":566.28,"price_before_discount_eur":641.71}` BEZ ogona; `ini_get('serialize_precision')`=-1 (serwer honoruje w runtime). Commit `37aa2f6` push origin main. Domyka follow-up z v3.74.)
# Wersja: 3.74 | Data: 2026-06-29 (CHAT-T-115 BACKEND DEPLOYED — wielojęzyczność narzędzi produktowych: link EN + cena EUR dla rozmów po angielsku (ADR-106). Wzorzec P4 z analizy czatów: bot odpowiadał po EN, ale linkował do PL karty i podawał PLN. **5 plików.** **MysqlProductEnrichmentService** (rdzeń): odczyt `conversion_rate` z `pr_currency` id_currency=2 (kurs 0.237934, Z BAZY, NIE zaszyty, cache per instancja) → `price_eur`=round(brutto_PLN×kurs,2) half-up + `price_before_discount_eur` przy promo; JOIN `pr_product_lang` id_lang=3 → `url_en`=`https://divezone.pl/en/<slug-en>.html` (slug EN INNY niż PL, więc z bazy; brak slugu→`url_en=null`, NIE martwy link). **Narzędzia** ProductSearch (2 bloki), ProductDetails, CuratedRecommendations (bez url — bierze z details): przepuszczają `url_en`+`price_eur`(+`_before_discount_eur`) obok PL. **SystemPrompt**: rozszerzenie ISTNIEJĄCEJ reguły JĘZYK ODPOWIEDZI (po l.134) — EN→`url_en`+`price_eur` ("566.28 EUR"); `url_en`=null→PL link+uprzedzenie; PL bez zmian; logika wykrywania języka (l.126-138) NIETKNIĘTA. **Oba warianty zawsze** (decyzja 35a — model wybiera wg języka, BEZ nowego param `lang`). **Weryfikacja 5986** (Shearwater Peregrine, rzeczywisty kod na serwerze): `price`=2380 (promo), `price_eur`=566.28, `url_en`=`…/en/shearwater-peregrine-dive-computer.html`, `price_before_discount`=2697/`_eur`=641.71. **DEPLOY** (ADR-089, zatwierdzenie Karola): pre-check md5 serwera==baza repo 5/5 (HEAD~1) → backup `_deploy_bak/CHAT-T-115/` → scp per plik (rsync nadal ucina „unexpected end of file" — scp niezawodny) → **md5 prod==local 5/5** → `php -l` ea-php84 clean 5/5 → smoke `ProductDetails` na 5986 (wdrożony kod, MySQL-only, omija Railway egress). Commit `4847315` push origin main (per-ścieżka, BEZ cudzych CLAUDE.md/routes.php). **⚠️ ZNANE (follow-up)**: `serialize_precision=100` w php.ini serwera → `json_encode` floatów zwraca formę długą (`566.2799…`); dotyczy też nie-okrągłych cen PLN (istniejące, bot formatuje OK od miesięcy). EUR są ZAWSZE nie-okrągłe → warto czysty fix globalny `ini_set('serialize_precision', -1)` w bootstrapie (poza zakresem 115). **FLAGA INFRA**: `/api/health` nadal `postgres:false` (egress smarthost→Railway, CHAT-T-116) — niezwiązane z 115, które idzie przez MySQL (`mysql:true`).)
# Wersja: 3.73 | Data: 2026-06-29 (CHAT-T-114 BACKEND DEPLOYED — 3 poprawki SystemPrompt z analizy 27 czatów `do_weryfikacji` (ADR-105, wchłania CHAT-T-091). **1 plik** `standalone/src/Chat/SystemPrompt.php`, 7 zmian wierszy. **POPRAWKA 1 (cena, 44 czaty)**: usunięty proaktywny disclaimer „Aktualną cenę potwierdź na karcie produktu" (punkt 4 STRUKTURY ODPOWIEDZI + instrukcja w bloku CENY); ZOSTAJE zakaz „cena na pewno aktualna" + uczciwa odpowiedź GDY KLIENT PYTA WPROST. **POPRAWKA 2 (serwis, 10 czatów; wchłania CHAT-T-091)**: w 3 kontekstach serwisowych (SERWIS AUTOMATU l.86, SCOPE-004 l.290, DOMAIN-006 l.305) `dive@`→`serwis@divezone.pl` + link strony serwisu z regułą językową (PL→`/serwis-automatow-oddechowych-i-innego-sprzetu-nurkowego`, EN→`/en/scuba-regulators-and-other-diving-equipment-service`); pozostałe ~28 `dive@` NIETKNIĘTE. **POPRAWKA 3 (B2B, 21 czatów)**: JAIL-002 przestaje odmawiać — pytanie o współpracę/hurt/program instruktorski → link `https://divezone.pl/b2b`; nadal nie negocjujemy warunków w czacie; reguła „NIE polecamy KONKRETNYCH instruktorów/szkół" (l.271) BEZ ZMIAN; l.296 „pośrednictwo"→„negocjacje warunków" + wyjątek B2B. **Odchył od taska**: B2B miał też kierować na `dive@` — pominięto, by `dive@` ubyło DOKŁADNIE 3 (gate Karola: 31→28); szczegóły B2B na stronie `/b2b`. **Weryfikacja**: grep `dive@` 31→28, „Aktualną cenę potwierdź" proaktywne→0, `php -l` clean. **DEPLOY** (ADR-089, zatwierdzenie Karola): pre-check md5 serwera==baza repo (`9b28f1b`=HEAD~1, brak cudzych zmian) → backup `_deploy_bak/CHAT-T-114/` → upload (rsync ucięło „unexpected end of file" → scp fallback; UWAGA na ścieżkę: serwer ma `chat.divezone.pl/src/…`, BEZ prefiksu `standalone/`) → **md5 prod==local `1456ca6`** → `php -l` ea-php84 clean. Commit `ca71d43` push origin main (per-ścieżka, tylko SystemPrompt.php — BEZ cudzych CLAUDE.md/routes.php CHAT-T-090). **⚠️ FLAGA INFRA (niezwiązana z CHAT-T-114)**: podczas deploy-smoke `/api/health` = `200 degraded`, **`postgres:false` przez >40s** — trwający epizod niedostępności Railway (incydent CHAT-T-107/108). Dzięki CHAT-T-107 endpoint degraduje (200, nie 500/fatal) — odporność działa. SystemPrompt to statyczny tekst, deploy niezależny od DB i poprawny. Do obserwacji przez monitor CHAT-T-108. **Wchłonięcie**: CHAT-T-091 ZAMKNIĘTY (status w pliku taska zaktualizowany).)
# Wersja: 3.72 | Data: 2026-06-29 (CHAT-T-107 BACKEND DEPLOYED — odporność backendu na niedostępność Railway (ADR-104). Bezpośrednia odpowiedź na incydent z v3.70 (serwer↔Railway migocze, `SQLSTATE[08006] could not connect`, fatal w `PostgresConnection`). **P36c — rozróżnienie błędu połączenia**: `PostgresConnection.query/fetchAll/fetchOne` opakowane w `executeWithRetry`; ZERWANIE (SOFT: `server closed`/`57P01`/`no connection to the server`/`08006`) → 3 próby retry+reconnect (backoff 100/300 ms, zerowanie `pdo`); NIEOSIĄGALNE (HARD: `could not connect`/timeout/refused/DNS) → 1 próba, `connect_timeout=2s` w DSN, od razu circuit-breaker per-request; błąd nie-połączeniowy (składnia/constraint) → rethrow bez retry. Klasyfikacja po komunikacie PRZED SQLSTATE. **`DbUnavailableException`** z flagą SOFT/HARD zamiast gołego `PDOException`. **P37c — 3 komunikaty klienta** (stałe w `ChatController`): retry-success (flaga `delayed_retry`), SOFT („Mamy chwilowy problem z połączeniem. Spróbuj … za moment."), HARD („…problemy techniczne…" + KONTAKT `dive@divezone.pl` / `56 307 03 03`); w SSE emitowane jako `event: done` (wiadomość bota), NIE `error`. **Fallback-cache** (`Support\FileCache`, plikowy — APCu brak na ea-php84; `var/cache/` SIBLING `public/`, poza docrootem, + `.htaccess` deny): `SettingsStore` i `ChipTreeService` po udanym odczycie cache'ują, przy `DbUnavailableException` degradują świeży TTL 300s → last-known-good → default/[]/puste drzewo; odczyt konfiguracji NIGDY nie kładzie czata; zapisy zostają fail-open. **Sprawa 3 — smoke ze źródłem**: `/api/chip-tree` zwraca nagłówek `X-DiveChat-Chip-Source: db|cache`. **KROK testy** `standalone/tests/Database/DbResilienceTest.php` **35/35** izolowany (zahardkodowany zły DSN, kasowany — ZERO dotykania prod `.env`/env): classify HARD/SOFT/null, SOFT→3 próby, HARD→1 próba (<3s, nie 15s), składnia→rethrow PDO, breaker fail-fast 2. zapytania, degradacja Settings/ChipTree, payload HARD ma kontakt; regresja CHAT-T-106 41/41. **KROK DEPLOY** (ADR-089, akceptacja Karola, Railway zdrowe potwierdzone monitorem 10:34): pre-check 5 modyfikowanych istnieją+niepuste + `mkdir -p src/Exception src/Support` → backup 5 modyfikowanych `_deploy_bak/CHAT-T-107/` (md5 .bak==serwer 5/5) → rsync per ścieżka 7 plików runtime (test+task NIE na prod, bez `--delete`) → **md5 prod==local 7/7** → `php -l` ea-php84 clean 7/7 → smoke `/api/health` 200 (`postgres:true`/`mysql:true`, php 8.4.22) + `/api/chip-tree` 200 z **`X-DiveChat-Chip-Source: db`** (świeże z Railway, NIE cache — smoke miarodajny), drzewo realne (root→5 L1). **Autoload** nowych klas (`Exception\`, `Support\`) działa bez `composer dump-autoload` (PSR-4 dynamiczny) — potwierdzone: chip-tree użył `FileCache` i zapisał `var/cache/eaf2…json` (13816 B) + `.htaccess` deny. Commity `cfb69dd` feat + `d6425a3` docs/ADR-104, push origin main (per-ścieżka, BEZ chip-event/routes.php CHAT-T-090 — nietknięte). **Znaczenie (P46b)**: to była JEDYNA rzecz blokująca powrót czata na stronę — odporność wdrożona; pozostaje potwierdzenie stabilności przez monitor CHAT-T-108 przed włączeniem widgetu. **Poza zakresem**: lokalny bufor zapisów nudge/rate-limit (P34b odrzucone), warstwa lokalnej bazy (P34c). **Follow-up**: po oknie obserwacji monitora CHAT-T-108 — decyzja o powrocie widgetu na divezone.pl.)
# Wersja: 3.71 | Data: 2026-06-29 (CHAT-T-088f EMBEDDINGS DEPLOYED — seed pełnej struktury drzewa chipów Level 1–3 na Railway (ADR-103, źródło prawdy `_docs/38`, analiza 772 realnych rozmów). **Migracja 038** (transakcyjna + rollback) zaaplikowana po dry-runie z ROLLBACK i STOP-GATE Karola. **Struktura**: Level 1 = 5 chipów (Dobór sprzętu · Pomoc w rozmiarze · Zaczynam nurkować · Maska i rurka (snorkeling) · Moje zamówienie); `serwis` → `active=FALSE` (ukryty, NIE skasowany); `zwroty`+`wysylka` zeszły z L1 pod „Moje zamówienie"; `dobor_rozmiar` przemianowany na `rozmiar`=„Pomoc w rozmiarze". **Schemat** (ważne — task zakładał kolumny `action_type`/`is_active`, których NIE MA): typ węzła kodowany przez `buttons` JSONB (`target: ai|link:<klucz>|modal:order`) + kolumnę `ai_prompt`; widoczność = `active` (bool); drzewo budowane z `parent_id`+`sort_order` (kolumna `level` informacyjna). **Liczby po COMMIT**: 32 wiersze, 31 aktywnych — root(1) + L1(5) + L2(16: dobor 6 / rozmiar 6 / zamowienie 4) + L3(9: maska 4 / pletwy 2 / pianka 3); INSERT 26 węzłów + 3 klucze linków (`link_odstapienie`, `link_returns`, `link_withdrawal` do `divechat_shop_config`, P40a), UPDATE 5, DELETE 0. **zwroty (P38a/P40a)**: ZERO AI ale Z PRZYCISKAMI — bot_text dwa tryby (14 dni ustawowe + Gwarancja 30 dni, warunki 1:1 z `_docs/38`) + 2 linki (link_zwroty + link_odstapienie). **status** zamówienia = `target:modal:order` (front obsługuje, `/api/order/status` ADR-063). **Liście AI** mają `ai_prompt` 1:1 z `_docs/38` (osie z danych: budżet/poziom/marka; NIGDZIE „DIN"; automat bez osi zimna/ciepła). **Smoke** `GET /api/chip-tree`: root→5 L1, dobor 6 dzieci, maska_fajka 4, zamowienie 4, serwis NIEOBECNY; zwroty buttony rozwijają oba linki (DB-source, świeże). **Follow-up**: pełny deterministyczny cennik wysyłki/płatności dla węzła „Wysyłka i płatności" („treść do uzupełnienia" w `_docs/38`); wariant językowy EN chipów (schemat `divechat_chip_nodes` nie ma kolumny języka — EN linki zarejestrowane na zapas). Front silnik CHAT-T-089 renderuje nowe gałęzie generycznie, bez zmian.)
# Wersja: 3.70 | Data: 2026-06-28 (CHAT-T-106 BACKEND DEPLOYED — liczniki per status w `GET /api/admin/review` (ADR-102, dane dla segmentowanego przełącznika CHAT-T-107). **Problem**: `GET /api/admin/review` zwracał `total` tylko dla aktywnie filtrowanego statusu → front nie miał liczb dla pozostałych segmentów bez N zapytań. **KROK 1 — repo**: `ConversationReviewRepository::countsByStatus()` — jedno `SELECT status, COUNT(*) … GROUP BY status`; komplet 4 kluczy seedowany z `ReviewStatus::cases()` (brakujący status→0, żeby segment pokazał „0" a nie zniknął); nieznany status w bazie logowany przez `error_log` i POMIJANY (nie przecieka do API; CHECK constraint i tak nie powinien na to pozwolić). **KROK 2 — kontroler**: `AdminConversationReviewController::list` dokłada `counts` do odpowiedzi ZAWSZE, niezależnie od `status` (jedno dodatkowe wywołanie repo, bez zmiany logiki listy/filtra/sortu/paginacji). **Kontrakt dla CHAT-T-107**: `{ items, total, limit, offset, counts: { nowy, do_weryfikacji, w_trakcie, zamkniety } }` — zawsze 4 klucze. **Granica semantyczna (ADR-102 D3)**: licznik `nowy` = rozmowy z JAWNYM `status='nowy'`, NIE wszystkie nierecenzowane rozmowy katalogu (liczniki = stan KOLEJKI recenzji; rozmowy bez wiersza NIE są doliczane — „ile w ogóle nierecenzowanych" to osobna przyszła liczba). **KROK 3 — testy** `standalone/tests/Admin/ConversationReviewCountsTest.php` **11/11** real-path na Railway (delta: snapshot→insert 3 rozmów o znanych statusach→assert dokładne delty +1/+1/+1, nowy=0, `sum(counts)==COUNT(*)`, komplet 4 kluczy, cleanup cascade→powrót do bazy); regresja CHAT-T-104 30/30. **Real-path teraz przez `Config::load()`** (NIE ręczny odczyt) — Karol naprawił nazwę klucza w lokalnym `.env` (`DATAFORSEO_API_PASSWORD-BASE64`→`_BASE64`, myślnik łamał phpdotenv, all-or-nothing parse), więc `Config::load()` znów ładuje `$_ENV['DATABASE_URL']` poprawnie; wpis pamięci o ręcznym obejściu USUNIĘTY jako już nieaktualny. **KROK 4 — DEPLOY** (ADR-089, akceptacja Karola + warunek: potwierdzenie że pliki CHAT-T-104 są na serwerze przed backupem): pre-check `[ -s ]` oba pliki istnieją+niepuste (repo 9148 B, kontroler 5697 B) → backup `_deploy_bak/CHAT-T-106/` (md5 .bak==źródło) → rsync per ścieżka 2 pliki runtime (test NIE na prod) → **md5 prod==local 2/2** → `php -l` ea-php84 clean 2/2 → `/api/health` 200 → uwierzytelniony smoke (token serwerowy HMAC, employee_id=2 admin): `GET /api/admin/review` zwraca `counts={nowy:0,do_weryfikacji:0,w_trakcie:0,zamkniety:0}` (4 klucze OK). Commit `a8bd5f3` push origin main (3 pliki, per-ścieżka, BEZ chip-event/routes.php CHAT-T-090 — nietknięte w working copy). **⚠️ INCYDENT INFRA (niezależny od CHAT-T-106, zgłoszony Karolowi)**: podczas smoke serwer↔Railway **migocze** — endpointy bijące Railway PG zwracają intermittent `500`/`503` (timeout `SQLSTATE[08006] could not connect … switchback.proxy.rlwy.net:14368`, crash w `requireAnyRole` na `SELECT role`, ~6-7s nawiązanie). Dotyczy WSZYSTKICH Railway-endpointów (`/api/admin/nudge-ctr` 500, `/api/chip-tree` 503, `/api/admin/review` 500/200 naprzemiennie), NIE tylko nowego kodu; lokalny dostęp do Railway działa (testy 11/11). Rollback NIE wykonany (stary kod też bije Railway w `requireAnyRole` — rollback nic nie naprawia; mój kod zweryfikowany md5+php -l+200 z poprawnym `counts`). **Follow-up**: CHAT-T-107 (front renderuje segmenty z licznikami + alarm na do_weryfikacji, decyzja 25c); osobno — diagnoza stabilności połączenia serwer→Railway (ADR-088 adjacent: pool/keepalive/IP allowlist).)

# Wersja: 3.69 | Data: 2026-06-28 (CHAT-T-104 BACKEND DEPLOYED — system recenzji rozmów (ADR-102): warstwa danych + API `/api/admin/review` dla regularnego, delegowalnego przeglądu czatów. Dwie niezależne osie — `status` (praca: nowy/do_weryfikacji/w_trakcie/zamknięty) i `verdict` (jakość: ok/problem_do_rozwiazania/problem_rozwiazany). Stan domyślny = BRAK wiersza (D3, „nowy" implicytny — NIE seedujemy istniejących rozmów). **KROK 1 — migracja 037** `sql/037_conversation_review.sql` (+rollback), zastosowana na Railway **real-path** (Config→$_ENV→PDO, ADR-088; lokalny `.env` ma 1 zepsuty klucz z myślnikiem `DATAFORSEO_API_PASSWORD-BASE64` łamiący phpdotenv → DATABASE_URL czytany linia-po-linii, dalej normalny `PostgresConnection`): tabela `divechat_conversation_review` (id, conversation_id INTEGER FK→divechat_conversations ON DELETE CASCADE + UNIQUE, status TEXT default `do_weryfikacji`, verdict TEXT NULL, note TEXT NULL, updated_by INT NULL, created_at/updated_at timestamptz; CHECK status/verdict; indeks `(status, updated_at DESC)`). **KROK 2 — repo** `standalone/src/Admin/ConversationReviewRepository.php`: `getByConversation`(null gdy brak), `listByStatus`(filtr+sort updated_at DESC+paginacja+lekki skrót rozmowy: session_id/started_at/model_used/message_count/first_user_message), `upsert`(ON CONFLICT — tworzy z defaultem lub aktualizuje TYLKO podane pola, zawsze updated_by+updated_at). Walidacja enumów w 1 miejscu (`validateFields`) → `InvalidReviewValueException`→422; enumy `ReviewStatus`/`ReviewVerdict`. **KROK 3 — API + routing**: kontroler `AdminConversationReviewController` (kanał serwerowy `DIVECHAT_SERVER_SECRET` + DOWOLNA rola operator+admin jak ConversationsController — recenzja delegowalna). Endpointy: `GET /api/admin/review?status=&limit=&offset=` (lista, default `do_weryfikacji`), `GET /api/admin/review/:conversationId` (pełny wiersz albo null), `POST /api/admin/review/:conversationId` (upsert; body status?/verdict?/note?/id_employee wymagany, zaufany D2; 422 dla śmieci enumów). **Decyzja osobny GET vs blok**: dedykowany `GET /api/admin/review/:id` = KANONICZNY kontrakt dla CHAT-T-105 (spójny z rodziną `/api/admin/review/*`, odsprzęgnięty od wygaszanego Basic-Auth `/api/admin/conversations/:id`); DODATKOWO `ConversationViewer::get()` rozszerzony o niełamiący blok `review` (tani mirror). **KROK 4 — testy** `standalone/tests/Admin/ConversationReviewRepositoryTest.php` **30/30** real-path na Railway (tworzy własną rozmowę, ćwiczy upsert create/„tylko podane pola"/updated_by/enum-reject/lista/clear verdict, sprząta przez cascade). **KROK 5 — DEPLOY** (ADR-089, akceptacja Karola): backup `_deploy_bak/CHAT-T-104/` (routes.php+ConversationViewer.php) → rsync per ścieżka 7 plików runtime (5 nowych + 2 modyfikowane; testy+sql NIE na prod) → **md5 prod==local 7/7** → `php -l` ea-php84 clean 7/7 → `/api/health` 200 → smoke: `/api/admin/review` i `/review/:id`→401 (route+auth OK, NIE 404). **Izolacja od CHAT-T-090**: routes.php do commitu+deploy = HEAD + WYŁĄCZNIE blok recenzji; niezacommitowana praca CHAT-T-090 (chip-event w routes.php + untracked ChipEventStore/ChipEventController) zachowana NIETKNIĘTA w working copy. **Uwaga numeracja migracji (dot. CHAT-T-090)**: `034_chip_events` NIE jest zastosowana na Railway (`divechat_chip_events` nieobecna) — to LUKA (CHAT-T-090 niedokończony), NIE kolizja; sekwencja monotoniczna 034(luka)<035/036<037. Commit kodu `26647dc` push origin main (10 plików, per-ścieżka, BEZ chip-event). **Kontrakt dla CHAT-T-105** (frontend/panel PS): kształt 3 endpointów wyżej. **Follow-up**: CHAT-T-105 panel (kolumna statusu + pole notatki + kontrolki); poza MVP — auto-flagowanie, przypisania recenzenta, metryki domknięcia pętli.)

# Wersja: 3.68 | Data: 2026-06-18 (CHAT-T-100 BACKEND DEPLOYED — function calling doboru rozmiaru skafandra mokrego (ADR-099, ADR-098). Port gotowej logiki `embeddings/size_matcher.py` → PHP standalone. **NIE embeddingi** — relacyjny lookup w `divechat_size_*` na Railway (SQL/wartości punktowe). Warstwa danych była gotowa z CHAT-T-099/099b (67 mapowań, chart dziecięcy, aliasy). **KROK 0 (zweryfikowane, NIE założone)**: realny namespace tooli = `DiveChat\Tools\` + `ToolInterface` (getName/getDescription/getParametersSchema/execute); `ToolRegistry::getToolDefinitions()` = JEDNO źródło dla OBU providerów (ClaudeProvider `input_schema`, OpenAIProvider `parameters`) → rejestracja w `config/tools.php` automatycznie eksponuje narzędzie w obu; PG przez `PostgresConnection` (placeholdery `?`). **NOWE narzędzie** `standalone/src/Tools/SizeRecommender.php` (`recommend_wetsuit_size`): (1) **wybór charta** — `product_id` → `divechat_product_size_chart`→`divechat_size_charts` (pojedyncze mapowanie brane wprost; bi-gender M+K → po `gender` klienta); fallback `brand`+`gender`. (2) **algorytm parytetowy z size_matcher.py**: chart przedziałowy (dorośli, `chest` wiodący, weryfikacja waist/hip/height/weight; 1 trafienie→match/ambiguous, ≥2→rozróżnienie wzrostem/wagą, 0→out_of_scale 2 najbliższe; ZERO ekstrapolacji) = port `match_size()`; chart punktowy (dzieci Rebel, `gender='DZIECI'` lub `height` min==max, wiodący `height`) = port `match_pointwise()` (dokładne→match, między→boundary `graniczny:true`, poza→out_of_scale). Detekcja typu: `gender=DZIECI` LUB wszystkie wiersze `height` mają min==max. (3) **walidacja** (narzędzie NIE zgaduje): brak płci/klatki/wzrostu → zwraca `error` (bot dopytuje). (4) **KROK 4** — `normalizeLabel` `^(\d+)\s*Plus$`→`\1+` + warstwa aliasów `divechat_size_label_alias` (`loadAliases`/`resolveLabel`, parytet `map_size_products.py`); output dokleja `aliases` gdy są. Output: `decision/sizes/size_full/consult/reason/brand/gender` (+`graniczny` dla dzieci). **KROK 5 — SystemPrompt** (`standalone/src/Chat/SystemPrompt.php`, nowa sekcja „DOBÓR ROZMIARU SKAFANDRA MOKREGO"): ZAWSZE pytaj o płeć przed wywołaniem; klatka wiodąca (dorośli) / wzrost (dzieci) — poproś gdy brak; **suche skafandry → NIE narzędzie, konsultacja** (dive@divezone.pl/56 307 03 03); out_of_scale/ambiguous → 2 najbliższe + konsultacja; F.1 (dzieci: pianka musi przylegać, większy = świadomy kompromis rodzica, nie default), F.2 (kaptur Rebel: wymień+konsultacja, nie licz), F.3 (jacket Rebel: uniwersalny przez pas brzuszny S/M, analogia ADR-098). **Uwaga**: schema rozszerzona vs handoff — `gender` enum +`DZIECI`, `required`=`['gender']` (chest/height walidowane w execute zależnie od typu charta — handoff przed ADDENDUM 099b zakładał tylko dorosłych z required chest). **KROK 6 — rejestracja** `config/tools.php` (`$registry->register(new SizeRecommender($pg))`). **KROK 7 — testy** (`standalone/tests/size_recommender_*.php`): (a) **parytet** z size_matcher.py na ŻYWYCH danych Railway **11/11 OK** (Scubapro M 104→L, 200→out_of_scale 4XL/3XL, 88→out_of_scale XS/S; Bare K 88→6; DZIECI 134→[S,M] graniczny, 140→M, 170/100→out_of_scale; brak płci/klatki→error; normalizeLabel); (b) **widoczność** w obu providerach (realny `formatTools` ClaudeProvider+OpenAIProvider) OK; (c) **REALNA pętla function calling przez Claude 3/3**: pełne dane→model woła `recommend_wetsuit_size`(M,104)→L; brak płci→model PYTA „dla kobiety czy mężczyzny", NIE woła narzędzia; suchy skafander→narzędzie NIE wołane. PY self-test 9/9 zielony. **KROK 8 — DEPLOY** (ADR-089, akceptacja Karola): backup prod→`_deploy_bak/CHAT-T-100_2026-06-18_224635/` (tools.php+SystemPrompt.php; SizeRecommender nowy) → scp 3 plików → **`composer dump-autoload -o`** (NOWA klasa, 363 klasy, classmap zawiera SizeRecommender) → `php -l` ea-php84 clean 3/3 → **md5 prod==local 3/3** (SizeRecommender 1fa6c20…, tools.php 702370…, SystemPrompt 9b28f1…) → `/api/health` ok (postgres+mysql) → smoke na PROD: registry buduje, `recommend_wetsuit_size` obecny, live exec chest=104→match L (Railway). Commit kodu `770e468` push origin main (BEZ handoff/gitignored, BEZ niepowiązanych routes.php/chip-*). **POZA ZAKRESEM**: warstwa 2 (kalkulator strona, Janek), dzieci Bare/produkty z listy Janka (`_reports/poprawki_zrodlowe_dla_janka.md`). **Follow-up**: domapowanie reszty produktów + decyzje Karola dla DO DECYZJI z CHAT-T-099.)

# Wersja: 3.67 | Data: 2026-06-18 (CHAT-T-099b ZASTOSOWANE NA RAILWAY (akceptacja Karola) — domknięcie iteracji 1 doboru rozmiarów: chart dziecięcy Rebel + aliasy + mapowania (ADR-099, ADR-098). Dane deterministyczne, NIE embeddingi. **KROK 0 (zweryfikowane)**: ostatnia migracja `035` → nadany **036**; `divechat_size_charts.gender` MIAŁ binarny CHECK `('M','K')` → wymagana migracja rozszerzająca. [7563] zweryfikowany (etykiety XS/S/M/L/XL literowe → dziecięca pianka, MAPUJ; NIE kaptur). **KROK 1 — migracja 036** `sql/036_product_sizing_dzieci_alias.sql` (+rollback): (E) CHECK gender → `('M','K','DZIECI')`; (C) tabela `divechat_size_label_alias` (jedno edytowalne źródło wariant_PrestaShop→etykieta charta, wspólne chat↔kalkulator). **KROK 2+3 — seed 036** (+rollback): chart **`Scubapro/DZIECI`** — jeden wymiar `height` PUNKTOWY (min==max): XXS=104, XS=116, S=128, M=140, L=152, XL=164 cm (źródło `rebel_product_page_2026-06`, skala wspólna pianek Rebel 55a/57a); + 4 aliasy (`M tall`→MT [Sc/K], `XXL`→2XL, `L short`→LS, `L tall`→LT [Sc/M]). **KROK 4 — mapowania (+16, −1)**: A 6 pojedynczych (Bare wg etykiet 50a: 5914/6831/6834→Bare/M, 6838/1146/1890→Bare/K); B 3 bi-gender ×2 charty (4243/4244/6681→Scubapro/M+K, bot pyta o płeć 47a); D 4 dziecięce (3365/4851/3027/7563→Scubapro/DZIECI); **korekta: usunięto błędne 7331→Bare/K** (męskie etykiety, na listę Janka). **Razem mapowań: 67** (Sc/M 17, Sc/K 16, Bare/M 13, Bare/K 17, Sc/DZIECI 4). **KROK 5 — algorytm punktowy** `embeddings/size_matcher.py`: `match_pointwise` (NIE BETWEEN — trafienie dokładne→`match`; wzrost między dwoma→`boundary` dwa najbliższe+`graniczny`; poza skalę→`out_of_scale`) + warstwa aliasów (`load_aliases`/`resolve_label`); self-test **9/9** (5 przedziałowy + 4 punktowy: 134→[S,M] graniczny, 140→M, 170→out_of_scale). Live na Railway potwierdzone. **KROK 6 — handoff** ADDENDUM 099b w `HANDOFF_CHAT-T-099_function-calling-rozmiar.md` (lokalny/gitignored): chart dziecięcy + algorytm punktowy + aliasy + reguły SystemPrompt F.1 (dobór dziecięcy: pianka musi przylegać, większy = świadomy kompromis rodzica) / F.2 (kaptur Rebel: wymień + konsultacja, NIE w product_sizing) / F.3 (jacket Rebel: uniwersalny przez pas brzuszny S/M, analogia ADR-098). **KROK 7 — pokrycie** `embeddings/coverage_report.py` (z autorytatywnych mapowań PG): **68 produktów → 64 zmapowane (94%), 4 na liście Janka (4245/7331/4553/4554), 0 niezmapowanych**; po aliasach `rozmiary_bez_progu` PUSTE dla wszystkich zmapowanych. **HARD STOP**: oba (migracja 036 + pełna lista mapowań) pokazane Karolowi przed Railway → zatwierdzone, CC zaaplikował przez DATABASE_URL. **Follow-up**: backend implementuje function calling (handoff), lista Janka `_reports/poprawki_zrodlowe_dla_janka.md`, warstwa 2/3 = osobne projekty.)

# Wersja: 3.66 | Data: 2026-06-18 (CHAT-T-099 ZASTOSOWANE NA RAILWAY (po akceptacji Karola) — warstwa danych + 52 mapowania gotowe; function calling w bocie = handoff do backend. Deterministyczny dobór rozmiaru skafandrów mokrych Scubapro+Bare (ADR-099, dyskryminator `size_variants` z ADR-098). **NIE embeddingi** — relacyjny ETL + lookup SQL `BETWEEN` (ADR-099 pkt 1; nazwa instancji `embeddings` myląca). **KROK 0 (zweryfikowane w bazie, NIE założone)**: ostatnia migracja w `sql/` = `034` → nadany **035** (z sufiksem `_seed` jak 029/030); nazwa grupy atrybutu rozmiaru w `pr_attribute_group_lang` = **`ROZMIAR` (id=27)** (ADR-098 zapowiadał „Rozmiar" do potwierdzenia; realnie wielkimi). Manufacturer: BARE=11, SCUBAPRO=18. Kategorie skafandrów mokrych: CIEPŁE wody=337, ZIMNE wody=367. **KROK 1 — migracja schematu** `sql/035_product_sizing.sql` (+rollback): 3 NOWE tabele — `divechat_size_charts` (chart per marka+płeć, UNIQUE), `divechat_size_chart_rows` (progi long format: chart_id, size_label, size_full, dimension chest|waist|hip|height|weight|leg, min/max, unit cm|kg, sort_order; CHECK-i), `divechat_product_size_chart` (mapowanie produkt→chart). **KROK 2 — seed** `sql/035_product_sizing_seed.sql` (+rollback) wygenerowany przez `embeddings/gen_size_charts_seed.py` (dane `dev_ocr_2026-06` zaszyte WPROST, P42a): **4 charty, 290 wierszy** (Bare/M 80, Bare/K 70 — z wymiarem `leg` punktowym min==max, Scubapro/M 80, Scubapro/K 60). Scubapro: `size_label` = część przed „ - ", `size_full` = pełne („MT - 98"); Bare: `size_full`=NULL. **KROK 3 — mapowanie** (`_reports/CHAT-T-099_mapowanie_propozycja.csv`, READ-ONLY z MySQL przez `embeddings/map_size_products.py`): 68 produktów Scubapro/Bare w kat 337/367; **52 auto-OK** (płeć z nazwy Damski/Męski), 2 bi-gender (OneFlex 4243/4244 — grupy ROZMIAR MĘSKI/DAMSKI), 16 DO DECYZJI (dziecięce 4553/4554/7563, unisex 6681, niejednoznaczne Reactive/Evoke/Rebel/Sport-Shorty). **NIC nie zapisane do `divechat_product_size_chart`** — lista czeka. **KROK 4 — raport pokrycia** `_reports/CHAT-T-099_pokrycie-rozmiarow.csv` (ADR-099 pkt 7): kolumny product_id/nazwa/marka/płeć/ma_chart/size_variants_niepuste/rozmiary_bez_progu/status. Realizuje cele: (1) brak rozmiarów mimo że powinny — [4245] OneFlex Vest „rozmiar L" w nazwie, 0 wariantów; (2) rozmiary bez progu — [7331] Bare Sport Vest **Damski** z MĘSKIMI etykietami (konflikt danych!), [2278] „M tall"≈MT, [5075] „XXL"≈2XL, [5373] „L short/tall"≈LS/LT. **KROK 5 — kontrakt function calling** (implementacja w bocie = HANDOFF do backend): `embeddings/size_matcher.py` — referencyjny algorytm **przedziałowy** (ADR-099 pkt 4: klatka wiodąca, weryfikacja wzrost/waga, „między rozmiarami/poza skalę" → 2 najbliższe + konsultacja, ZERO ekstrapolacji); self-test 5/5 OK. Handoff `_instances/embeddings/handoff/HANDOFF_CHAT-T-099_function-calling-rozmiar.md` (tool `recommend_wetsuit_size`, JSON Schema, port logiki do PHP, reguły SystemPrompt: pyta o płeć, suche→konsultacja, normalizacja `6 Plus`→`6+`). **WYKONANE NA RAILWAY** (akceptacja Karola, CC przez DATABASE_URL): (A) migracja 035 ✅ (3 tabele), (B) seed 035 ✅ (4 charty / 290 wierszy / 14 leg punktowych), (C) zapis **52 auto-OK mapowań** do `divechat_product_size_chart` (Scubapro/M 14, Scubapro/K 13, Bare/M 10, Bare/K 15). Smoke `size_matcher.py` na żywo: Scubapro M chest 104→`L`, chest 200→`out_of_scale` (4XL/3XL), Bare K chest 88→`6`. **POZOSTAJE DO DECYZJI Karola (NIE zapisane)**: 16 produktów DO DECYZJI + 2 bi-gender OneFlex (`_reports/CHAT-T-099_mapowanie_propozycja.csv`) — płeć dla dziecięcych/unisex/niejednoznacznych; konflikt `[7331]` Damski z męskimi etykietami. **Follow-up**: backend implementuje function calling (handoff), domapowanie reszty produktów, warstwa 2 (kalkulator strona) i warstwa 3 (uzupełnianie brakujących rozmiarów) = osobne projekty per ADR-099.)

# Wersja: 3.65 | Data: 2026-06-14 (CHAT-T-088e BACKEND DEPLOYED + FRONT zacommitowany — „dwa światy": ai_prompt na liściu + kontekst ścieżki dla AI (ADR-097, decyzje 63c/65b/58a). **Problem**: liść `ai` wysyłał do LLM samą etykietę chipa („Kaptur") — za płytko. Rozdzielenie: co klient klika (`label`) ≠ co dostaje AI (ścieżka nawigacji + opcjonalny `ai_prompt`). **BACKEND** (deploy ADR-089): (1) **Migracja `sql/033_chip_ai_prompt.sql` (+rollback)** — `ADD COLUMN IF NOT EXISTS ai_prompt TEXT NULL` (opcjonalna instrukcja dla AI gdy węzeł jest liściem; NULL = sama ścieżka). Zaaplikowana na Railway przez Karola (6 węzłów, 0 z ai_prompt — seed przez panel D). (2) **ChipTreeService** — `ai_prompt` w SELECT + buildTree (`isset`-safe jak label w 088b); kontrakt węzła = 8 pól (`{node_key, label, bot_text, buttons, children, context_hint, model_level, ai_prompt}`). (3) **ChatService::handle** — nowy param `?string $chipContext`; gdy niepuste → doklejany do **system promptu TEJ TURY** (`buildChipContextBlock`), NIE jako wiadomość user. Historia (`divechat_messages`+JSONB) zostaje czysta: `appendMessage('user', $message)` zapisuje realną treść, `system[0]` filtrowany przy zapisie. Działa dla obu providerów (Claude bierze system osobno; OpenAI jako role=system — doklejenie do system[0] bezpieczne, bo ClaudeProvider nadpisałby drugi system). (4) **ChatController** (handle+stream) — przyjmuje `chip_context` z body, `resolveChipContext` (trim, cap 2000 znaków, pusty→null). **Test** `ChipTreeServiceTest` 35/35 (+4: ai_prompt liść ustawiony, root/dobor null, robustność braku kolumny). **Deploy** (3 pliki: ChipTreeService.php, ChatService.php, ChatController.php): backup `_deploy_bak/CHAT-T-088e/` → scp per ścieżka → **md5 match 3/3** → `php -l` ea-php84 clean 3/3 → `/api/health` HTTP 200/0.37s → smoke `/api/chip-tree`: wszystkie 6 węzłów eksponują pole `ai_prompt:null`. **FRONT** (zacommitowany, moduł PS deployuje Karol ręcznie, pełna ścieżka `/home/divezone/public_html/newtmp2/modules/divezone_chat/views/js/`): (1) **`buildChipContext(extraLabel, aiPrompt)`** — serializacja ścieżki z `chipStack` (etykiety węzłów, root pominięty) → „Dobór rozmiaru › Kaptur"; `ai_prompt` doklejany jako „Instrukcja od obsługi: …"; null gdy brak ścieżki i ai_prompt (wolne pisanie). (2) **routeChipNode liść ai** — `sendUserMessage(leafLabel, buildChipContext(leafLabel, node.ai_prompt))` (liść NIE na stosie → label jako ostatni segment). (3) **routeChipButton ai/modal-fallback/curated** — kontekst z węzła-źródła (stos top, `ai_prompt` z niego). (4) **sendUserMessage(text, chipContext)** → `transport.sendMessage(text, sessionId, nudgeSid, chipContext, callbacks)`. (5) **transport.js** — nowy param `chipContext` (5. pozycja), `if (chipContext) body.chip_context = chipContext`. (6) **`CHIPS_MOBILE_LIMIT` 4→6 (58a)** — Level 1 ma 5 chipów po 088d. `node --check` OK oba pliki. **Kontrakt body POST /api/chat[/stream]** += opcjonalne `chip_context` (string|null). Commity: backend osobno (ChipTreeService+ChatService+ChatController+sql/033*+test), front osobno (widget-bundle.js+transport.js), docs (ADR-097+status+task). **Follow-up**: seed ai_prompt przez panel edycji chipów (D), Level 2 doboru (088f), beacon klików (CHAT-T-090).)

# Wersja: 3.64 | Data: 2026-06-14 (CHAT-T-088d BACKEND/SEED DONE — rozdzielenie „Dobór sprzętu" / „Dobór rozmiaru" na dwa osobne węzły Level 1 (ADR-096, decyzje 57a/58a). **Problem (test na żywo)**: węzeł `dobor` miał label „Dobór sprzętu" ale `bot_text` o ROZMIARZE (zlepione dwie ścieżki — doradztwo zakupowe vs pomiar rozmiaru). **Migracja `sql/032_split_dobor.sql` (+rollback), idempotentna**: (1) UPDATE `dobor` → bot_text czysto sprzętowy („maska, płetwy, automat, komputer nurkowy, czy coś innego?") + label „Dobór sprzętu"; (2) INSERT `dobor_rozmiar` (Level 1, sort 5, `parent_id` = id root przez podzapytanie NIE hardcode, bot_text o rozmiarze „dla mężczyzny czy kobiety?", button „Napisz czego szukasz"→ai), `ON CONFLICT (node_key) DO UPDATE` (label/bot_text/buttons/sort_order). Oba bot_text DETERMINISTYCZNE (klik → gotowy tekst, ZERO LLM; LLM dopiero gdy klient kliknie „Napisz czego szukasz"→ai lub wpisze w input — zgodne z 57a). **Rollback**: DELETE `dobor_rozmiar` + przywrócenie bot_text `dobor` do baseline seedu 029 („Pomogę dobrać sprzęt. Co Cię interesuje?", jak rollback 031). **Aplikacja na Railway = Karol** (jak 028-031, bez deployu kodu — czysty seed): zweryfikowane — Level 1 = 5 chipów: zwroty(1), serwis(2), wysylka(3), dobor(4, „Dobór sprzętu" + tekst sprzętowy), dobor_rozmiar(5, „Dobór rozmiaru" + tekst o rozmiarze). Commit `sql/032*`. **Follow-up (front, OSOBNO — CHAT-T-088e)**: limit mobilny `CHIPS_MOBILE_LIMIT` 4→6 (58a) — Level 1 ma teraz 5 chipów, przy limicie 4 jeden się nie zmieści. NIE w tym seedzie.)

# Wersja: 3.63 | Data: 2026-06-14 (CHAT-T-089b FRONTEND DONE — chipy następcze pod odpowiedzią (poprawka pozycji, 51a) + potwierdzenie linku w treści (50a). Poprawka UX po teście na żywo CHAT-T-089: bąbel `bot_text` lądował POD chipami poziomu (stały `chipsEl` u góry), zamiast chipy być przedłużeniem odpowiedzi. Commit kodu `8ca93d3`, push origin main; moduł PS deployuje Karol ręcznie (ADR-089, **pełna ścieżka `/home/divezone/public_html/newtmp2/modules/divezone_chat/views/`** — NIE `~/` w zmiennej, tylda = błąd „unexpected end of file"). **Zmiany (`widget-bundle.js` +150/-46, `widget.css` +17)**: (1) **Refaktor `renderChipLevelInto(container, node, backHandler)`** — wspólna logika renderu poziomu (← Wróć gdy nie Level 1 + dzieci nawigacyjne z limitem mobilnym + przyciski akcji), wołana DWOJAKO: startowy `state.chipsEl` (góra) i inline blok pod bąblem. `renderCurrentChipLevel` = wrapper startowy (`popChipLevelStartowe`). (2) **Tryb następczy (51a)** — flaga `state.chipsInline`. Klik węzła z `bot_text`: `appendBotMessage()` → `enterInlineMode()` (chowa startowe menu) → `appendInlineChips(node)` dokleja `.dz-chips--inline` POD świeżym bąblem w `messagesEl` (naturalne przedłużenie odpowiedzi). Węzeł czysto nawigacyjny (bez tekstu): inline gdy rozmowa trwa, inaczej startowy `chipsEl` u góry (zachowanie 089, pod przyszłe podchipy doboru). (3) **← Wróć inline (44a)** — `goBackInline()` zdejmuje szczyt `chipStack` i dokleja nowy inline blok poziomu wyżej pod ostatnią pozycją; bąble rozmowy ZOSTAJĄ (nie kasuje historii). (4) **Spójność stosu** — `spendPriorInlineChips()` wygasza starsze inline bloki (`disabled` + `.dz-chips--spent`, opacity .45), interaktywny tylko najnowszy zestaw → klik w nieaktualny „← Wróć" nie miesza współdzielonego `chipStack`; wołane też w `sendUserMessage` (wejście w swobodną rozmowę). Link w treści bąbla pozostaje klikalny. (5) **Zgodność** — `clearMessagesView` usuwa też `.dz-chips--inline`; `resetChipsToLevel1` resetuje `chipsInline=false` (powrót startowego menu); restore/persist(CHAT-T-059)/nudgeSid(CHAT-T-085)/modal(CHAT-T-043)/streaming nietknięte; mobile limit `CHIPS_MOBILE_LIMIT` wspólny w obu trybach. **KROK 2 (link w treści, 50a)**: potwierdzone z analizy — `renderMarkdown` podmienia `[label](url)`→`<a target=_blank>` na CAŁYM tekście PRZED podziałem na linie, więc link działa też w listach/paragrafach; `bot_text` renderuje się przez `renderMarkdown` w `appendBotMessage`. Seed 031 (link inline w treści) na PROD. Render w przeglądarce po deployu modułu PS. Weryfikacja: `node --check` OK, referencje funkcji silnika rozwiązane (brak `clearChips`/`popChipLevel`). **Follow-up bez zmian**: beacon klików CHAT-T-090 (hook `onChipClick` gotowy), gałąź doboru, panel edycji chipów.)

# Wersja: 3.62 | Data: 2026-06-14 (CHAT-T-089 FRONTEND DONE — silnik drzewa chipów w widgecie zastępuje statyczne `CHIPS_DESKTOP` (ADR-096, decyzje 37a/38a/42a/43a/44a). Commit kodu `4a16ac5`, push origin main; moduł PS deployuje Karol ręcznie (ADR-089), CC NIE deployował. **Zmiany (`widget-bundle.js` +212/-1, `widget.css` +10)**: (1) **`fetchChipTree()` przy mount** — `GET /api/chip-tree` (publiczny, `mode:cors`, `credentials:omit`, `cache:default` → ETag/`max-age=300` po stronie przeglądarki), cache w `state.chipTree`. **Graceful fallback**: render początkowy = statyczne `CHIPS_DESKTOP`, podmiana na dynamiczny Level 1 dopiero PO sukcesie fetch → zero pustego UI / zero migotania; błąd/puste/brak backendUrl → statyczne zostają. (2) **Level 1** z `root.children` — etykieta = `label` (42a, NIE node_key/bot_text), limit mobilny `CHIPS_MOBILE_LIMIT=4` na dzieciach (buttony bez limitu). (3) **`routeChipNode` (sedno 38a)**: węzeł z `bot_text` → `appendBotMessage()` LOKALNIE przez renderMarkdown, **ZERO LLM** (Q231a — brak `/api/chat/stream`), potem zejście do dzieci/przycisków; liść `ai` (bez tekstu/dzieci) → `sendUserMessage(label)` (114a, czytelny content NIE node_key). (4) **`routeChipButton`**: `link`+`url` → `window.open(_blank, noopener)` (URL gotowy z drzewa, 43a); `ai` → sendUserMessage; `modal:order` → `openOrderModal()`; `curated:`/inne → fallback ai (ścieżki kuratora NIE budowane). (5) **Nawigacja wstecz Wariant A (44a)**: `state.chipStack`, chip „← Wróć" (klasa `.dz-chip--back`) od Level 2 — bąble rozmowy ZOSTAJĄ, wraca tylko ZESTAW CHIPÓW. (6) **Zgodność**: `startNewConversation` → reset Level 1 z drzewa (`resetChipsToLevel1`, no-op gdy drzewo niezaładowane); restore historii chowa chipy jak dziś (fetchChipTree sprawdza `display!=='none'` → restore wygrywa); streaming/persist(CHAT-T-059)/nudgeSid(CHAT-T-085)/modal(CHAT-T-043) nietknięte. (7) **Hook beacona `onChipClick(node_key)`** pusty — kontrakt dla CHAT-T-090 (39a), wołany w `routeChipNode` na wejściu z `node.node_key` (tylko chipy nawigacyjne, NIE buttony). XSS: każda etykieta chipa przez `escapeHtml`, `bot_text` przez istniejący `renderMarkdown`. Weryfikacja: `node --check` OK; kontrakt `/api/chip-tree` + CORS (`access-control-allow-origin: https://divezone.pl`) + ETag/`max-age=300` sprawdzone curl-em na PROD. Render w przeglądarce po ręcznym deployu modułu PS. Handoff: `_instances/frontend/handoff/CHAT-T-089_done.md` (zawiera kontrakt dla CHAT-T-090). **Follow-up**: gałąź doboru (`dobor` = placeholder-liść ai), panel edycji chipów, beacon klików CHAT-T-090.)

# Wersja: 3.61 | Data: 2026-06-14 (CHAT-T-088b BACKEND DEPLOYED — dopełnienie drzewa chipów przed frontem (ADR-096, decyzje 42a/43a). Dwa braki kontraktu wykryte przy projektowaniu silnika widgetu (CHAT-T-089, idzie PO tym): (1) **label** — krótka etykieta nawigacyjna na węźle, osobna od bot_text (chip wyświetla label, nie node_key); (2) **rozwijanie `link:<klucz>`** na gotowy URL po stronie backendu (front nie zna divechat_shop_config). **Migracja 030** (`030_chip_nodes_label.sql` + rollback): `ADD COLUMN IF NOT EXISTS label TEXT NULL` + COMMENT. **Seed 030** (`030_chip_nodes_label_seed.sql`, idempotentny UPDATE): root→„W czym mogę pomóc?", zwroty→„Zwroty i wymiana", serwis→„Serwis automatu", wysylka→„Dostępność i wysyłka", dobor→„Dobór sprzętu". Zaaplikowane na Railway (Karol psql): 5 węzłów z label, 0 NULL. **ChipTreeService**: `label` w SELECT+kontrakcie (NULL-safe `isset` — chroni stary SELECT); `decodeButtons($raw, $linkMap)` rozwija `link:<klucz>` → `{label, target:"link", url:<value|null>}`, pozostałe targety (ai/modal:/curated:) → `{label, target, url:null}`; mapa linków z `loadLinkMap()` (jedno `SELECT … WHERE key LIKE 'link\\_%'`, **bez N+1**, pomija puste/'TODO' → url:null); `buildTree(rows, linkMap=[])` — mapa wstrzykiwana (testowalne, test podaje własną). **Kontrakt po zmianie**: `{node_key, label, bot_text, buttons:[{label,target,url}], children:[…], context_hint, model_level}` — target dla link zmienia się z „link:link_serwis" na czyste „link" + url wypełniony. **Deploy ADR-089** (1 plik: ChipTreeService.php): backup `_deploy_bak/CHAT-T-088b/` → rsync → **md5 match** → `php -l` ea-php84 clean → `/api/health` 200. **Smoke PROD** `GET /api/chip-tree`: każdy węzeł ma label (root + 4); `serwis.buttons[0]` = {label:"Pełny cennik", target:"link", url:"https://divezone.pl/serwis-automatow-oddechowych-…"} rozwinięty z configu; ai → url:null. **Test** `ChipTreeServiceTest` 31/31 (label, link rozwinięty, brak klucza→url:null, target nie-link→url:null, bez linkMap→url:null, kontrakt 7 pól + button label/target/url, robustność). Commit `44b7b51`. ADR-096 zaktualizowany (addendum 088b). **Kolejny krok**: CHAT-T-089 (frontend — silnik widgetu konsumuje label + url).)

# Wersja: 3.60 | Data: 2026-06-13 (CHAT-T-088 BACKEND DEPLOYED — fundament drzewa chipów (ADR-096). **Schemat (migracja 029, `divechat_chip_nodes`)**: klucz hybrydowy (id BIGSERIAL PK + node_key UNIQUE), jawna hierarchia (parent_id → self ON DELETE CASCADE, level, sort_order), węzeł hybrydowy (bot_text Markdown NULL + buttons JSONB — może mieć jedno/drugie/OBA), context_hint, model_level (CHECK NULL|basic|primary|escalation), active, indeksy (parent_id,sort_order)+(active), COMMENT-y. Dzieci-podchipy z parent_id; buttons = TYLKO akcje końcowe (link:/curated:/modal:/ai). **ChipTreeService** czyta całe aktywne drzewo, składa zagnieżdżenie po parent_id; logika `buildTree()` wydzielona jako statyczna (testowalna bez PG). **Endpoint GET /api/chip-tree** (public, bez auth jak nudge BOOT): `Response::jsonCached` z ETag (hash treści) + Cache-Control: public, max-age=300; If-None-Match → 304. Kontrakt węzła: node_key, bot_text, buttons[], children[], context_hint, model_level (pola id/parent_id/level NIE wychodzą). **Seed osobny** (`029_chip_nodes_seed.sql`, pytanie otwarte 1 — errata bez re-migracji struktury): root + zwroty/serwis/wysylka/dobor (placeholder, pełna gałąź doboru PO sesji zespołu). Treść z `_docs/37_`: zwroty WYGŁADZONE (urwane „...14 dniowego prawa do zwrotu," → pełne „...14-dniowego prawa do odstąpienia od umowy: wejdź w panel..."), serwis z `serwis@divezone.pl`, wysyłka z ADR-095 dec.1 (koszty NIE w tekście — przycisk → ai/get_shipping_info). Hybryda (32b): zwroty (2 btn)/serwis (3 btn) mają bot_text ORAZ buttons. Q231a: rdzeń deterministyczny, tylko „Inne pytanie" → ai. **Migracja 029+seed na Railway** (psql, Karol): count=5 (root+4), struktura zweryfikowana. **Deploy ADR-089** (4 pliki: Response.php, config/routes.php, ChipTreeService.php, ChipTreeController.php; bez dump-autoload — PSR-4 prefix): backup `_deploy_bak/CHAT-T-088/` → rsync per ścieżka → **md5 match 4/4** → `php -l` ea-php84 clean 4/4 → `/api/health` 200. **Smoke PROD**: `GET /api/chip-tree` → 200, drzewo root+4 (zwroty/serwis/wysylka/dobor), serwis 3 btn, ETag + Cache-Control; `If-None-Match: <etag>` → **304**; zły ETag → 200. **Test** `tests/Chip/ChipTreeServiceTest.php` — 22/22 (zagnieżdżenie, sort_order, hybryda, rekurencja poziom 3, dekodowanie buttons, kontrakt 6 pól). Commit `3c2317d`. **NIE w tym tasku** (osobno): silnik widgetu (frontend), panel edycji chipów (zakładka PS, 26), gałąź doboru sprzętu (po sesji zespołu), routing model_level per liść, dług serwis@divezone.pl w SystemPrompt l.86 (decyzja 27). **PORZĄDKI REPO** (przy okazji): zaległe ADR 093-096 + task spece (CHAT-T-036/068/077/079/081/083/087/088, ENC-013) + treść chipów/transfer encyklopedia zacommitowane; `.gitignore` += `modules/*.zip` (build artefakt) + `/_drafts/` (robocze binaria); divezone_sidebar v3 (T-035, collapse menu admina); CHAT-T-087 task → DONE + Wynik. Repo czyste.)

# Wersja: 3.59 | Data: 2026-06-13 (CHAT-T-087 KROK B+C BACKEND DEPLOYED — domknięcie 4 klas błędów z 5 chatów. **KROK B (SystemPrompt, ADR-094/095)** — 3 sekcje: (1) **DRILL-DOWN do `get_product_details`** przed odpowiedzią „nie wiem" o atrybut produktu (ważność/wymiary/ciśnienie/materiał/gwarancja/kompatybilność) — MUSI wołać search_products→get_product_details ZANIM powie „nie mam informacji"; few-shot voucher (ważność 1 rok z opisu, NIE na sztywno); nie dotyczy pytań edukacyjnych (→get_expert_knowledge). (2) **Wyjątek probabilistyczny dostawy in_stock** (ADR-095 dec.1): wolno „do 15:00 wysyłka tego samego dnia, większość paczek następnego dnia roboczego" + asekuracja „nie gwarantujemy, kurier; po pewność zadzwoń 56 307 03 03"; granica „duża szansa = OK / gwarantuję = ZAKAZ"; TYLKO in_stock (available_to_order/unavailable bez zmian). (3) **Sekcja numerów kont i linków** przez `get_shop_links` (ADR-095 dec.2): nr konta wprost tylko przy pytaniu o przelew + zawsze link kontaktu; nie zmyślać. **KROK C** — nowe narzędzie `get_shop_links` (wzorzec 1:1 ShippingInfo): czyta `divechat_shop_config` po whiteliście 14 kluczy (3 konta bank_ + 11 linków link_), pomija wartości pustе/'TODO' (graceful), filtr `topic`: payment (konta+kontakt+płatności), legal, content, contact, **service** (serwis+kontakt), **about** (o nas/B2B/filmy), default=komplet. Logika shapingu wydzielona do statycznych `buildResult()`/`filterRows()` (testowalne bez DB). Rejestracja w `config/tools.php`. **Migracja `sql/028_shop_links_seed.sql`** (+rollback): seed 14 kluczy do `divechat_shop_config`, ON CONFLICT (key) DO UPDATE (idempotentna, bez zmian strukturalnych — tabela z migracji 013). Karol zatwierdził i zweryfikował (HTTP 200) wszystkie URL-e + dodał 5 linków ponad pierwotne 9 (płatności/serwis/o nas/B2B/filmy). **Migracja zastosowana na Railway** (psql): `INSERT 0 14`, count(link_%/bank_%)=14, 0 'TODO'. **Test** `tests/Tools/GetShopLinksTest.php` — 31 asercji (filtr TODO/pusty, 6 topic, payment+płatności, service/about, graceful nulle, schema) — zielone. **Deploy ADR-089** (3 pliki: SystemPrompt.php, GetShopLinks.php, config/tools.php): backup `_deploy_bak/CHAT-T-087/` (SystemPrompt+tools.php; GetShopLinks nowy) → rsync per ścieżka → **md5 match 3/3** → `php -l` ea-php84 clean 3/3 → `/api/health` HTTP 200 / 0.46s. **Weryfikacja PROD** (jednorazowy skrypt, usunięty): `get_shop_links(topic=payment)` → konta PLN/EUR/SWIFT + linki {kontakt, platnosci} + note; default → 11 linków + konto PLN. Commit `bbd2792`. **CHAT-T-087 KOMPLETNE** (A: fix ceny wdrożony v3.58; B+C: drill-down + dostawa + linki/konta wdrożone). Granice: zero zmian w `.env`/auth/innych endpointach; Aiven nietknięty; KROK A `MysqlProductEnrichmentService` bez zmian w tym deployu.)

# Wersja: 3.58 | Data: 2026-06-12 (CHAT-T-087 KROK A BACKEND DEPLOYED — fix ceny `reduction_tax` dla rabatów kwotowych brutto (ADR-093). **Problem (potwierdzony na PROD)**: `MysqlProductEnrichmentService::enrich()` ignorował kolumnę `pr_specific_price.reduction_tax`. Dla `reduction_type='amount'` zawsze odejmował kwotę rabatu od ceny NETTO, potem doliczał VAT — dla rabatów zdefiniowanych jako BRUTTO (`reduction_tax=1`) zaniżało to cenę o `reduction × rate`. Przykład: SUUNTO Eon Core (id 5463) — bot podawał 1707 zł zamiast 1900 zł (839 × 0,23 ≈ 193 zł). Skala: **98 produktów** z rabatem `amount`+`reduction_tax=1` (~92% rabatów kwotowych liczonych źle; zapytanie na PROD MySQL, id_group IN (0,1)). **Fix**: `fetchSpecificPrices()` dociąga `sp.reduction_tax`; logika ceny wydzielona z `enrich()` do czystej, testowalnej `computeBruttoPrice(priceNetto, taxRate, sp)` — dla `amount`+`reduction_tax=1` odejmuje kwotę od ceny BRUTTO (`round(baseBrutto − reduction, 2)`), dla `reduction_tax=0` i `percentage` bez zmian. `price_before_discount` bez zmian. **A4**: `get_curated_recommendations` woła `enrich()` na żywo (NIE trzyma snapshotu cen — `verified_at` to metadana weryfikacji zespołu) → reseed NIEPOTRZEBNY. **Test** `tests/Shop/ProductPriceTest.php` — 10 asercji (5463→1900.00, regresja amount/tax0→1707.03, percentage→1107.00, sp_price override, brak promo) — zielone. **Deploy ADR-089** (tylko 1 plik, decyzja Karola: A osobno jako P1): backup `_deploy_bak/CHAT-T-087/` → rsync → md5 match → `php -l` ea-php84 clean → `/api/health` HTTP 200 / 0.44s. **Weryfikacja PROD** (jednorazowy skrypt Config::load+enrich, usunięty po teście): `enrich([5463])` → `price=1900.00`, `price_before_discount=2739`, in_stock, qty 4. Commit `cf86a52`. **KROK B (SystemPrompt: drill-down ADR-094, dostawa in_stock 2b ADR-095, sekcja kont/linków) + KROK C (`get_shop_links` + migracja `sql/028` z realnymi URL-ami + 5 dodatkowych linków + rejestracja) — GOTOWE LOKALNIE, niewdrożone, niewcommitowane; czekają na drugie wdrożenie (osobny STOP). Migracja 028 na Railway = osobny explicit STOP. TODO KROK C: rozszerzyć whitelist `GetShopLinks` o 5 nowych kluczy (link_platnosci/serwis/o_nas/b2b/filmy), które Karol dodał do seedu 028.**)

# Wersja: 3.57 | Data: 2026-06-08 (CHAT-T-086 + CHAT-T-084 BACKEND + MODUŁ PS DEPLOYED — **FAZA 2 ADR-090 KOMPLETNA**: telemetria + sticky bucket + atrybucja konwersji + zdarzenie dismiss + panel z 4 metrykami. Dwa zadania koordynowane jednym deployem. **CHAT-T-086 (zdarzenie nudge_dismiss)**: dotąd klik X na nudge tylko chował dymek + flaga sessionStorage, NIE wysyłał beacona — niemierzalne "świadomie odrzucił" vs "zignorował" (decyzje 256b/257a). Tania dokładka (cała maszyneria już istnieje z CHAT-T-082/083). Migracja `sql/027_nudge_dismiss_event.sql` (+ rollback z DELETE FIRST): DROP CONSTRAINT + ADD CHECK z 3. wartością `nudge_dismiss` (PG nie pozwala na ALTER CHECK in-place), UNIQUE (session_id, event_type) bez zmian — dedup "jeden dismiss per sesja" automatyczny. Wykonana psql Railway — CHECK rozszerzony na `('nudge_shown', 'nudge_cta_click', 'nudge_dismiss')`. Backend `WidgetEventController` (`VALID_EVENT_TYPES` += `'nudge_dismiss'`, reszta logiki RateLimiter/ON CONFLICT/204 bez zmian — dismiss leci tą samą ścieżką). Frontend `widget-loader.js` w handlerach X dla OBU wariantów (v1 ~528, v2 ~673): `sendNudgeEvent('nudge_dismiss')` PRZED `hideNudge()` (fire-and-forget, błąd beacona nie blokuje zamknięcia, guard `!nudgeShownSid` chroni przed wysłaniem bez ekspozycji). **CHAT-T-084 (panel raport CTR)** — odblokowane przez CHAT-T-085 (atrybucja konwersji działa). Backend `AdminNudgeCtrController` (newy plik 167 linii): GET `/api/admin/nudge-ctr?days=N`, kanał serwerowy (ServerHmacVerifier + rola admin-only, wzorzec 1:1 z `AdminAnalyticsController`). Pojedyncze zapytanie z CTE: `agg` agreguje per (bucket, ab_active) — COUNT DISTINCT session_id per event_type z 3 typów; `conv` JOIN-uje events.session_id ↔ conversations.nudge_sid z filtrem `EXISTS jsonb_array_elements(c.messages) WHERE role='user'` (≥1 wiadomość użytkownika, jsonb zamiast JOIN divechat_messages — szybsze, single query). Końcowy SELECT liczy: shown, clicks, dismissals, `ignored = GREATEST(0, shown - clicks - dismissals)` (clamp ≥0 wg 257a — w rzadkich przypadkach mogłoby wyjść ujemne), ctr/dismiss_rate/conversion_rate (null gdy shown=0). Param `days` clamp 1-365, default 30. Interval `(? || ' days')::interval` (wzorzec z RateLimiter, bezpieczny dla bind). Rejestracja w `routes.php`: import + `$router->get('/api/admin/nudge-ctr', ...)`. Frontend `AdminDivezoneChatController.php` — nowa zakładka **CTR zachęty** (admin-only obok Analityki, link ukryty dla nie-admina spójnie z CHAT-T-049/118b): `TAB_NUDGE_CTR='nudge_ctr'`, `ENDPOINT_NUDGE_CTR='/api/admin/nudge-ctr'`, `NUDGE_CTR_MIN_SAMPLE=100` (próg flagi małej próby). `renderNudgeCtrSection($employeeId)` (162 linie): callBackend + dispatch 403/error/data, filtr `?days=7|30|90` (onchange submit), opis nad tabelą (definicje CTR/zamknięcia/zignorowane/konwersja + uwaga o stare rozmowy = nudge_sid NULL), `renderNudgeCtrTable` z 10 kolumnami: Wariant, Tryb (A/B vs baseline), Ekspozycje, Kliki(CTA), CTR%, Zamknięcia(X), Zamknięcia%, Zignorowane, Rozmowy, Konwersja%. Wiersze z shown<100 dostają tło `#fffbea` + ikonkę ⚠ "za mała próba na wnioski". Helper `fmtPercent` formatuje float→"X.X%" lub "—" dla null. Sanity note pod tabelą: `ekspozycje = kliki + zamknięcia + zignorowane` (clamp ≥0 backendzie). **Deploy ADR-089 backend (3 pliki)**: backup `_deploy_bak/CHAT-T-086-084/{WidgetEventController,routes}.php.bak` (AdminNudgeCtrController to nowy plik), rsync per ścieżka (port 5739, klucz id_ed25519, BEZ `--delete`/`.env`/`vendor`), STOP 1 zatwierdzony przez Karola. md5 match 3/3 (`e54f7bd5`/`daace450`/`7b2c1d09`), `php -l` clean 3/3, smoke `/api/health` HTTP 200, smoke `/api/admin/nudge-ctr` bez podpisu HTTP 401 (auth działa), smoke POST `nudge_dismiss` HTTP 204 + wiersz `1e5734ef…` w bazie z event_type='nudge_dismiss' (whitelist+CHECK). **Test endpointu z podpisem ServerHmacVerifier**: HTTP 200 / 3 wiersze (v1 ab=true: 380 shown 6 clicks 0 dismiss 374 ignored CTR=1.58%; v2 ab=false: 2 shown 1 click 0 dismiss 1 ignored CTR=50% mała próba; v2 ab=true: 256 shown 3 clicks **1 dismiss** 252 ignored 1 rozmowa CTR=1.17% konwersja=0.39%). **Invariant** `clicks+dismissals+ignored=shown` ✓ we wszystkich 3 wierszach (clamp działa). **Deploy moduł PS 116b** (Karol ręcznie, jeden rsync obejmuje OBA pliki — widget-loader.js [086] + AdminDivezoneChatController.php [084]): rsync `modules/divezone_chat/` do `~/public_html/newtmp2/`, `--exclude config_pl.xml`, BEZ `--delete`, dry-run pre-check (oczekiwane 2 pliki). Po wgrywie smoke Karola: dymek X → POST nudge_dismiss 204 + wiersz w bazie; zakładka "CTR zachety" w panelu admin pokazuje 4 metryki w tabeli z flagą małej próby dla v2 ab=false. **Kryteria akceptacji 086 (1-6) + 084 (1-6)**: K086.1 CHECK 3 wartości na Railway ✓, K086.2 klik X v1/v2 → 204 + wiersz `nudge_dismiss` ✓ (test dismiss `1e5734ef`), K086.3 dedup ON CONFLICT (UNIQUE bez zmian, obejmuje 3 typy) ✓, K086.4 klik X nadal zamyka dymek + dz_nudge_dismissed (regresja zero — tylko dochodzi beacon) ✓, K086.5 click CTA + shown działają jak dotąd ✓ (smoke endpoint pokazuje), K086.6 lint clean ✓. K084.1 endpoint z podpisem 200 / bez 401 ✓, K084.2 CTR/dismiss/ignored/konwersja poprawne (invariant ✓ wszystkich 3 wierszach, vs verify_nudge_correlation.php konwersja=0.5% v2 spójnie), K084.3 panel pokazuje 8 kolumn + flaga małej próby ✓ (smoke Karola "działa"), K084.4 baseline/A/B rozróżnione ✓ (v2 ma oba wpisy), K084.5 zero wpływu reszty panelu/backendu ✓, K084.6 lint clean ✓. **Granice respektowane**: ZERO zmian w `.env`/auth/SystemPrompt/innych endpointach. CHAT-T-082 endpoint i UNIQUE constraint bez zmian (whitelist rozszerzona, struktura nie). CHAT-T-085 atrybucja działa identycznie (JOIN po nudge_sid). Aiven nietknięty. **FAZA 2 ADR-090 KOMPLETNA** — pełen łańcuch działania: ekspozycja (CHAT-T-082 endpoint + CHAT-T-083 bucket+sid-at-shown) → klik CTA / klik X / brak reakcji (CHAT-T-086 dismiss event domknął pomiar) → rozmowa z atrybucją (CHAT-T-085/ADR-092 osobny nudge_sid) → raport delegowalny (CHAT-T-084 panel CTR z 4 metrykami). Dane od dnia wdrożenia każdego elementu, stare rozmowy/eventy = NULL (brak atrybucji wstecz, ADR-091 świadome). **Co dalej**: faza 2 ADR-090 zamknięta, można przejść do innych priorytetów backlogu. Naturalne follow-upy do rozważenia: monitoring CTR-u przez kilka tygodni (czy v1 lub v2 wyraźnie lepszy przy próbach >=100/wariant), prosty z-test proporcji w panelu (sekcja 7 spec: MVP bez tego, można dorzucić gdy próba urośnie), dashboard time-series ekspozycji/CTR przez dni (analogicznie do trendu kosztów w Analityka).

# Wersja: 3.56 | Data: 2026-06-08 (CHAT-T-085 BACKEND + MODUŁ PS DEPLOYED — fix korelacji konwersji nudge (ADR-091): rozdzielenie sessionId rozmowy od nudge_sid atrybucji. **Tło**: CHAT-T-083 założył jeden sessionId dla całej ścieżki ekspozycja→klik→rozmowa, ale `tryRestoreSession` (CHAT-T-059, persystencja localStorage 30 dni) nadpisywał `state.sessionId` starym sid dla każdego powracającego klienta. Skutek: pierwsza wiadomość pod stary sid rozmowy, beacony pod nowy sid nudge → rozjazd → konwersja w panelu zawsze 0%. Smoke korelacji 2026-06-08 13:33 wykazał: klik sid=9de1a748… rozmowa sid=566c618f…. Każda warstwa z osobna poprawna — to konflikt persystencji (UX) z pomiarem (CTR). **Architekt rozwiązanie (ADR-091, 253a/254a)**: rozdzielić dwie role które błędnie pełnił jeden sid. `session_id` = identyfikator ROZMOWY (zmienny przez restore, UX). `nudge_sid` = identyfikator ATRYBUCJI ekspozycja→konwersja (stały od momentu nudge_shown). Front trzyma nudge_sid w OSOBNYM polu `state.nudgeSid`, dosyła w body pierwszej wiadomości. Backend zapisuje `nudge_sid` w `divechat_conversations.nudge_sid` przy INSERT nowej rozmowy. Konwersja = JOIN `divechat_nudge_events.session_id ↔ divechat_conversations.nudge_sid`. **Migracja Railway PG** (`sql/026_conversations_nudge_sid.sql` + rollback): `ALTER TABLE divechat_conversations ADD COLUMN nudge_sid VARCHAR(64) DEFAULT NULL` + partial index `WHERE nudge_sid IS NOT NULL` (większość rozmów bez atrybucji — pełny indeks marnowałby miejsce) + COMMENT objaśniający. Wykonana psql na `switchback.proxy.rlwy.net:14368/railway` — ALTER + CREATE INDEX + COMMENT OK, `\d divechat_conversations` pokazuje kolumnę VARCHAR(64) NULL + `idx_conversations_nudge_sid`. **Backend 3 warstwy** (przepływ nudge_sid): (1) `ChatController` `handle()`+`stream()` — odczyt `$body['nudge_sid']` przez nową `resolveNudgeSid(mixed)`: walidacja przez `isValidSessionIdFormat` (UUID v4 lub legacy 32-hex), brak/niepoprawny → null (atrybucja opcjonalna, NIE generujemy fallbacku), przekazanie do `chatService->handle(...nudgeSid: $nudgeSid)`; (2) `ChatService::handle` — nowy opcjonalny param `?string $nudgeSid = null` przekazywany do `conversationStore->startOrResume(...$nudgeSid)`; (3) `ConversationStore::startOrResume` — nowy opcjonalny param `?string $nudgeSid = null`, INSERT do nowej kolumny TYLKO przy gałęzi "sessionId nie istnieje" oraz ownership mismatch generujący nowy effectiveSessionId (oba = nowa rozmowa); resume istniejącej NIE nadpisuje (atrybucja należy do momentu powstania rozmowy — świadome, ADR-091). Sygnatury opcjonalne = wsteczna kompatybilność (klient bez nudge wyśle null lub pole brakuje). **Frontend moduł PS** (CHAT-T-085 część C + fix race smoke 2026-06-08): `widget-bundle.js`: (1) `state.nudgeSid: null` w init obok `sessionId`, (2) `startNewConversation` czyści też `nudgeSid` (świadomie nowa rozmowa = brak atrybucji nudge), (3) w `DivezoneChatMount` blok pending pobiera OBA pola: `state.sessionId = state.nudgeSid = pendingSessionId`, one-shot zerowanie pendingSessionId w BOOT, (4) `tryRestoreSession` (CHAT-T-059) nadpisuje tylko `state.sessionId` — `state.nudgeSid` PRZEŻYWA restore (sednio fixu), (5) `sendUserMessage` PRZED `transport.sendMessage` lazy fallback: `if (state.boot.nudge.pendingSessionId)` → przenieś do `state.nudgeSid` (zawsze gdy pusty) i `state.sessionId` (tylko gdy null, żeby nie nadpisać restored) i wyzeruj pending — KRYTYCZNY fix race między `requestIdleCallback bootBundle` (mount przed nudge_shown) a `setupNudge.setTimeout` (pendingSessionId ustawiany po `nudge_delay`s); bez tego mount widziałby pending=undefined, (6) `sendUserMessage` przekazuje `state.nudgeSid` jako 3-i pozycyjny arg do `transport.sendMessage`, (7) `onDone` zeruje `state.nudgeSid` po pierwszej wiadomości (atrybucja jednorazowa, kolejne wiadomości tej rozmowy nie potrzebują, baza już ma wpis). `transport.js`: (1) sygnatura `sendMessage(message, sessionId, nudgeSid, callbacks)`, (2) `if (nudgeSid) body.nudge_sid = nudgeSid` obok istniejącego `if (sessionId) body.session_id = sessionId`. **Skrypt diagnostyczny** `verify_nudge_correlation.php`: sekcje 2 i 3 JOIN zmienione z `c.session_id = e.session_id` na `c.nudge_sid = e.session_id` (nowy klucz konwersji). Sekcje 4a/4b zostają jako diagnostyka rozjazdu (format sid + dokładne dopasowanie). **Deploy ADR-089 backend** (4 pliki): backup `_deploy_bak/CHAT-T-085/{ChatController,ChatService,ConversationStore,verify_nudge_correlation}.php.bak`, rsync per ścieżka (port 5739, klucz id_ed25519, BEZ `--delete`/`.env`/`vendor`), STOP 1 zatwierdzony przez Karola. md5 match 4/4, `php -l` clean 4/4 (`/opt/cpanel/ea-php84/root/usr/bin/php`), smoke `/api/health` HTTP 200 / 99B / 0.4s. Backend smoke OK (przed wgraniem modułu): POST z `nudge_sid` w body → INSERT zapisuje kolumnę; POST bez → kolumna NULL. **Deploy moduł PS 116b** (Karol ręcznie): rsync `modules/divezone_chat/` do `~/public_html/newtmp2/modules/divezone_chat/` (`--exclude config_pl.xml`, BEZ `--delete`, dry-run pre-check). **Bug fix race wykryty po pierwszym smoke** (sesja CC + Karol): pierwszy smoke po deploy modułu wykazał `c.nudge_sid=NULL` mimo świeżej karty (incognito) — diagnoza: `widget-loader.js` linia 747 `schedule(function () { bootBundle(); }, { timeout: 4000 })` ładuje bundle na `requestIdleCallback` ZANIM `setupNudge.setTimeout` (po `nudge_delay` 5-10s) ustawi `BOOT.nudge.pendingSessionId`. Mount wykonuje się gdy pending=undefined → blok if{} nie wchodzi → state.nudgeSid=null + state.sessionId=null → pierwsza wiadomość bez sid → backend generuje fresh UUID + nudge_sid=NULL. **Fix** dodany w `widget-bundle.js sendUserMessage` PRZED `transport.sendMessage`: lazy konsumpcja pending w drugim punkcie cyklu życia (gdy setupNudge już zdążył ustawić). Drugi rsync modułu PS, drugi smoke przez Karola w nowej przeglądarce: klik `b639d29a-bd73…` o 13:40 → rozmowa `b639d29a-bd73…` (ten sam sid!) o 13:40 z `nudge_sid=b639d29a-bd73…`. **Smoke korelacji `verify_nudge_correlation.php` po fixie**: sekcja 2 pokazuje 1/10 (b639d29a `ma_rozmowe=TAK wiadomosci=2`), sekcja 3 pokazuje bucket=v2 ab=true rozmowy=1 **konwersja=0.5%** (pierwsza niezerowa konwersja w historii projektu). Stare kliki sprzed deploy wszystkie `ma_rozmowe=NIE` (ADR-091 świadome — brak atrybucji wstecz dla rozmów stworzonych przed CHAT-T-085). **Kryteria akceptacji 1-7**: K1 migracja przeszła, kolumna+index istnieją ✓, K2 smoke korelacji ≥1/N dopasowanych (b639d29a) ✓, K3 powracający user — restored session_id + nudge_sid z nowej ekspozycji w body (technical limitation: resume nie nadpisuje istniejącego nudge_sid; atrybucja należy do momentu utworzenia rozmowy, zgodne ze spec) ✓, K4 klient bez ekspozycji nudge → nudge_sid=NULL ✓ (smoke 17bf9d0f), K5 stare rozmowy nudge_sid=NULL ✓ (sekcja 4a wszystkie sprzed CHAT-T-085), K6 regresja zero — `/api/chat/stream` bez `nudge_sid` w body działa identycznie (backend resolveNudgeSid akceptuje brak), sygnatury opcjonalne ✓, K7 `php -l` 4/4 + `node --check` 2/2 clean ✓. **Granice respektowane**: ZERO zmian w panelu PS, ZERO zmian w innych endpoints, ZERO zmian w `.env`/auth/SystemPrompt. Aiven nietknięty (ADR-019). CHAT-T-059 zachowane (tryRestoreSession nadpisuje tylko sessionId, nudgeSid odporne). CHAT-T-082 ownership check zachowany (effectiveSessionId nadal podmienia przy cudzym customerId, nudge_sid wtedy też trafia do INSERT nowej rozmowy). **ADR-091 (UWAGA — kolizja numerów)**: w `_docs/10_decyzje_projektowe.md` są DWA wpisy ADR-091 (jeden TASK-ENC-014 fizyka wyporu, drugi CHAT-T-085 nudge atrybucja). Numeracja monotonicna ADR powinna unikać kolizji — do rozważenia renumerowanie drugiej na ADR-092 jako follow-up (poza zakresem CHAT-T-085, decyzja architekta). **Co dalej**: CHAT-T-084 (panel raport CTR per wariant, ADR-090 faza 2 krok 3/3) — teraz odblokowane, JOIN nudge_sid ma dane.

# Wersja: 3.55 | Data: 2026-06-08 (TASK-ENC-014 BACKEND DEPLOYED — errata fizyki wyporu w bazie bota (sesja 2 po wcześniejszej blokadzie). **Tło incydentu**: bot odpowiedział błędnie na pytanie "wyporność jacketu do butli 18l + suchy skafander" twierdząc że suchy wymaga większego worka 20+ kg — odwrócona fizyka. Diagnoza wykazała: (1) odwróconą fizykę w raw JSON (suchy mylony z grubą pianką jako przyczyna dużej wyporności), (2) brak metody liczenia w danych — model dopowiada liczby z wiedzy bazowej gdy chunki nie zawierają wzoru, (3) wcześniejsza propozycja 16-20 kg dla single zawyżona (myliła single z twinem). **Pierwsza sesja CC zablokowana**: 7 edycji raw JSON (A1, A2, B1a/b/c, B2, B3) + re-embed Railway PG (20 chunków, $0.0010), ale odpowiedź bota nadal granicznie nieakceptowalna — Karol odmówił commit, sprawa wróciła do architekta. **Architekt dostarczył kompletne lekarstwo** (ADR-091 + dok 33 fizyka_wypornosci_zrodla + dok 34 wsad_do_encyklopedii_wypornosc + dok 35 metoda_doboru_wypornosci): (a) korekta liczb dla single 16-20 kg → 13-16 l (dolna granica produkcyjna), (b) nowe hasło metodyczne `DOBOR_WYPORNOSCI_WORKA` (concept_number 106) ze wzorem na ciężar gazu, dwiema składowymi, regułą partnera, asymetria świadoma (single+suchy/twin+suchy = liczby, gruba pianka = konsultacja), (c) reguła w SystemPrompt.php blokująca ekstrapolację modelu (12 linii w heredoc PROMPT). **Implementacja w 3 warstwach**: (1) `data/encyclopedia/v3/gen_v2/raw/ZESTAW_SKRZYDLO_SINGLE.json` (3 pola: subtypes_client[0] worek 13-16 l z 13 l jako dolna granica + wymóg konsultacji przy grubej piance, purchase_parameters[0] przeformułowanie na metodę z 3 przypadkami single+suchy/gruba pianka/lekka pianka, faq[4] travel 10-13 l vs 13-16 l dla suchego z odsyłaniem do konsultacji), (2) `data/encyclopedia/v3/gen_v2/raw/DOBOR_WYPORNOSCI_WORKA.json` NOWE hasło 152 linie ze strukturą zgodną z BALAST.json (concept_key, name_pl/en, definition, subtypes_client x3, synonyms, longtail_phrases, purchase_parameters x3, cross_sell x3, faq x5, seller_notes, concept_number=106, missing_data x2, not_to_confuse BALAST, related_concept_keys x9, subtypes_technical x3, ungrounded_additions), treść z dok 35 A/B/C dokładnie + cytowanie Bardy 2005 w subtypes_technical, ZERO półpauz/średników wg styl PL, (3) `standalone/src/Chat/SystemPrompt.php` (+12 linii) nowa sekcja `DOBÓR WYPORNOŚCI WORKA/SKRZYDŁA/BCD — KRYTYCZNE` brzmienie DOKŁADNIE wg dok 35 sekcja D (między ---), wstawiona PO Bug do uniknięcia case 94 (INT/DIN bullet w FAKTY DOMENOWE) i PRZED PYTANIA DOPRECYZOWUJĄCE, wcięcie 12 spacji jak sąsiednie sekcje. **Re-embed Railway PG**: `--mode check` pre-embed wykrył 1 ZMIENIONE (ZESTAW_SKRZYDLO_SINGLE) + 1 NOWE (DOBOR_WYPORNOSCI_WORKA), `--mode changed` 2 hasła × 5 chunków = 10 chunków, 4190 tokenów, $0.0005, 1.8s (`MAX(updated_at) = 2026-06-08 12:13:08+00`), `--mode check` post-embed 106 AKTUALNE exit 0. **Deploy SystemPrompt ADR-089**: backup `_deploy_bak/TASK-ENC-014/SystemPrompt.php.bak` (83 562 B), rsync 1 plik (port 5739, klucz id_ed25519, BEZ `--delete`/`.env`/`vendor`), STOP 2 zatwierdzony przez Karola, md5 match `4b379c64362e94809a480a7f7d26f2a0` (lokalne == PROD), `php -l` clean przez `/opt/cpanel/ea-php84/root/usr/bin/php`, smoke `/api/health` HTTP 200 / 99B / 0.3s. **Bot test PROD przed deploy SystemPrompt** (test A — samo re-embed chunków): pytanie "jaką wyporność jacketu do butli 18l i suchego skafandra?" → bot WPROST obala wcześniejszy błąd: "Suchy skafander = mała wymagana wyporność jacketu — to najczęstszy błąd w myśleniu", podaje wzór 18 × 200 / 1000 × 1,3 = 4,7 kg, konkluzja "12-16 l wystarcza, 20+ l potrzebne PRZY GRUBEJ PIANCE LUB TWINSECIE", max_similarity chunków 0.729 vs 0.696 poprzednio (lepsze dopasowanie). Sam re-embed wystarczył, reguła SystemPrompt to defensywne wzmocnienie. **Bot test PROD po deploy SystemPrompt** (test B): ta sama odpowiedź jakościowo, "stosunkowo małej wymaganej wyporności... częsty błąd myślenia że suchy wymaga dużego worka. Nie wymaga, bo: składowa gazu 4,7 kg, suchy utrata wyporu bliska zeru. Większy worek (20+ kg) nie jest potrzebny", drobna ekstrapolacja "15-18 kg" zamiast literalnie 13-16 l z dok 35 (margines bot dodaje sam, konkluzja taka sama), cache hit `cache_read_tokens=31289` (Claude API cache'uje system prompt po deploy, $0.145 vs $0.247 wcześniej = 41% taniej per request). **Kryteria akceptacji 1-7**: K1 single 13-16 l skorygowane + relikt usunięty z 4 plików ✓, K2 nowe hasło z formatem sąsiednich ✓, K3 reguła SystemPrompt we wskazanym miejscu, brzmienie dok 35 D ✓, K4 re-embed log ZMIENIONE+NOWE ✓, K5 check exit 0 ✓, K6 bot PROD nie cytuje "suchy → większy worek" ✓ (2x weryfikacja pre i post deploy SystemPrompt), K7 deploy ADR-089 z STOP 2 + md5 match + smoke ✓. **Granice respektowane**: ZERO zmian w pozostałych 102 hasłach raw JSON, ZERO regresji w `/api/chat/stream` ani innych endpointach, ZERO zmian w panelu PS / module / froncie, NIE ruszane KIESZENIE_ZINTEGROWANE (suchy→balast=prawda spec C), NIE ruszane TWINSET/DUMP_VALVE_DRYSUIT/ZESTAW_AUTOMATU_TWIN. Aiven martwy bez zmian (ADR-019). **Uwaga merytoryczna do follow-up**: `ZESTAW_SKRZYDLO_TWIN.json` faq[0] dalej mówi "18-22 kg (40-50 lbs)" dla twin+suchy, dok 35 mówi "~18 l (40 lbs)". Górna granica 22 l zawyżona względem dok 35 — spec K1 NIE wymagał korekty TWIN, zostawione jako możliwy follow-up. **Co dalej**: zamknięcie incydentu fizyki wyporu (TASK-ENC-014 DONE), powrót do CHAT-T-084 (panel raport CTR per wariant, ADR-090 faza 2 krok 3/3) lub CHAT-T-085 (fix korelacji nudge↔sid w panelu) — kolejność wg priorytetów Karola.

# Wersja: 3.54 | Data: 2026-06-08 (CHAT-T-083 FRONTEND DEPLOYED — bucket A/B sticky + sessionId-at-shown + beacony nudge (ADR-090 faza 2 krok 2/3), moduł PS wgrany ręcznie przez Karola (116b). **Tło**: po CHAT-T-082 (backend telemetrii — tabela `divechat_nudge_events`, endpoint `/api/widget/event`, client-supplied sessionId) front musiał: (a) wybierać bucket A/B sticky client-side cache-safe (decyzja 241a, ADR-087 zabrania losowania w PHP w hooku), (b) generować UUID v4 W MOMENCIE pokazania nudge i przekazać go bundlowi (decyzja 247a — to samo ID dla ekspozycji i rozmowy, umożliwia JOIN konwersji), (c) wysyłać beacony `nudge_shown`+`nudge_cta_click` ZAWSZE dla obu wariantów niezależnie od trybu A/B (decyzja 248a — baseline CTR działa też przy zwykłym przełączniku z CHAT-T-081). **KRYTYCZNA pułapka MIME** (zweryfikowana smokem CHAT-T-082 2026-06-08, dopisana w spec sekcja 6): ModSecurity na `chat.divezone.pl` zwraca 403 PRZED PHP dla `Content-Type: text/plain` — domyślny `navigator.sendBeacon(url, string)` wysyła `text/plain;charset=UTF-8` → odpadnie. Front MUSI użyć `new Blob([JSON.stringify(payload)], {type: 'application/x-www-form-urlencoded'})` (nadal "simple request" CORS, brak preflight, backend tolerancyjnie parsuje JSON z `php://input` niezależnie od MIME). Handoff CHAT-T-083 dał starszy text/plain — świadomie ZIGNOROWANY na rzecz sekcji 6 spec jako źródła prawdy. **Implementacja w 3 plikach modułu PS** (158 insertions, 8 deletions): `views/js/widget-loader.js` (+115 linii): (1) state zmienne `nudgeShownSid`/`nudgeBucket`/`nudgeAbActive` na poziomie mountAll, (2) helpers `lsGet`/`lsSet` (Safari ITP / private mode safe, try/catch zwraca null/no-op), (3) `getBucket(cfg)`: gdy `!cfg.ab` → bucket = `cfg.variant === 'v2' ? 'v2' : 'v1'`; gdy `cfg.ab` → odczyt `dz_ab_bucket` z localStorage (`v1`|`v2`), brak → losowanie `Math.random() < 0.5 ? 'v1' : 'v2'`, zapis, reload = ten sam wariant, (4) `generateSessionId()`: `crypto.randomUUID()` z try/catch + fallback ręczny RFC 4122 v4 (8-4-4-4-12 hex, pozycja 14='4', 19∈{8,9,a,b}) dla starszych Safari/iOS, (5) `sendNudgeEvent(type)`: guard `if (!nudgeShownSid || !nudgeBucket) return` (klik launchera bez nudge = no-op), URL = `BOOT.backendUrl + (BOOT.nudge.eventPath || '/api/widget/event')`, body JSON `{session_id, event_type, bucket, ab_active}`, **Blob z MIME `application/x-www-form-urlencoded`** (pułapka), próba `navigator.sendBeacon(url, blob)` w try/catch → jeśli false (queue pełny) lub wyjątek → fallback `fetch(..., keepalive:true, mode:'cors', credentials:'omit', headers:'application/x-www-form-urlencoded')` w drugim try/catch + `.catch(()=>{})` na promise — 2x try/catch + .catch gwarantuje że błąd beacona NIE blokuje otwarcia czatu, (6) modyfikacja `setupNudge`: po recheck guards w setTimeout callback generuje sid + przypisuje do state loadera ORAZ `BOOT.nudge.pendingSessionId = sid` (mostek do bundla), wybiera funkcję renderu wg bucket (NIE variant), woła `sendNudgeEvent('nudge_shown')` po render, (7) modyfikacja `openChatFlow`: NA POCZĄTKU `sendNudgeEvent('nudge_cta_click')` ZANIM `ssSet`/`hideNudge`/`bootBundle` — guard w sendNudgeEvent zapewnia że launcher.click (bez nudge, nudgeShownSid=null) NIE wyśle click. `views/js/widget-bundle.js` (+12 linii): w `DivezoneChatMount` po `state.boot = ctx.boot;` PRZED `tryRestoreSession()`: jeśli `state.boot.nudge.pendingSessionId` → `state.sessionId = pending` + **one-shot** (`state.boot.nudge.pendingSessionId = null` zapobiega ponownemu użyciu np. po startNewConversation). `tryRestoreSession` (CHAT-T-059) MA PIERWSZEŃSTWO — restore `{exists:true}` nadpisuje pending przez `state.sessionId = data.sessionId`; restore fail/`{exists:false}` zostawia pending nietknięte. `divezone_chat.php` (+31 linii): (1) `const KEY_NUDGE_AB = 'DIVEZONE_CHAT_NUDGE_AB'` + `const NUDGE_EVENT_PATH = '/api/widget/event'`, (2) lazy read `$nudgeAb = (Configuration::get(KEY_NUDGE_AB) === '1')` w `renderConfigForm`, (3) checkbox `"Test A/B v1 vs v2 (50/50)"` w sekcji 5 PO select Wyglad zachety i PRZED checkbox Wlacz dymek z `<small>` info że tryb nadpisuje przełącznik wyżej a pomiar CTR działa zawsze, (4) zapis w `submitDivezoneChatConfig` `(int)Tools::getValue('nudge_ab', 0) === 1 ? '1' : '0'`, (5) read w `hookDisplayFooter` + dodanie `'ab' => $nudgeAb, 'eventPath' => self::NUDGE_EVENT_PATH` do `$boot['nudge']` array (obie wartości stałe identyczne dla wszystkich → cache-safe LiteSpeed ADR-087). **Cykl życia sessionId** (potwierdzony z bundle code review): pending z loadera → state.sessionId w mount (one-shot konsumpcja) → tryRestoreSession async może nadpisać gdy `exists:true` → pierwsza `sendUserMessage` z aktualnym state.sessionId (pending lub restored) → backend (CHAT-T-082) dla pending sid traktuje jako nową rozmowę i INSERT z tym ID (sekcja 3 spec — client-supplied UUID akceptowany przy NOWej rozmowie, wiązany z HMAC customerId), zwraca `payload.session_id == pending` → `persistSession` zapisuje w localStorage. **JOIN konwersji** w panelu CHAT-T-084: `divechat_nudge_events.session_id == divechat_conversations.session_id` — beacon `nudge_shown` ma to samo UUID co pierwsza wiadomość użytkownika. **Decyzje wbudowane w kod**: 241a (bucket sticky 50/50 localStorage), 247a (sessionId at-shown UUID v4), 248a (zawsze beacony dla obu wariantów), ADR-087 (wartości BOOT stałe identyczne dla wszystkich → cache-safe), 116b (moduł PS wgrywa Karol ręcznie). **Defensywność beacona**: 2x try/catch + .catch na fetch promise — błąd sendBeacon/fetch nie może rzucić wyjątku do openChatFlow. Fire-and-forget zgodnie ze spec K5. **Rollback-safe**: AB OFF (default lazy) zachowuje zachowanie CHAT-T-081 — bucket = variant z panelu, brak losowania. Cofnięcie tego tasku = przełącznik panelu na OFF (zero re-deployu). **KROK 0**: `git pull origin main` (CHAT-T-082 commit `2416f4f` + docs `e32aeff` v3.53 zaciągnięty), odczyt spec CHAT-T-083 + sekcja 6 `_docs/22_spec_ab_ctr_nudge.md` (pułapka MIME), aktualnego widget-loader.js (po CHAT-T-081) + widget-bundle.js (state.sessionId cycle + tryRestoreSession) + divezone_chat.php fragmenty (KEY_NUDGE_VARIANT, sekcja 5, $boot['nudge']). **KROK 1-2 weryfikacja**: `node --check` na obu plikach JS → Both JS files OK; `php -l divezone_chat.php` → No syntax errors detected; `git diff --stat` 3 files changed, 158 insertions(+), 8 deletions(-) — żaden inny plik modułu nie dotknięty. **Kryteria akceptacji 1-7**: K1 AB=OFF baseline + beacony bucket=variant ✓ (getBucket zwraca variant gdy !ab), K2 AB=ON sticky 50/50 ✓ (lsGet/lsSet + Math.random), K3 nudge_shown raz przy renderze ✓ (guard `if (nudgeEl) return` w renderNudge/Card + setupNudge wywoływany raz w mountAll), K3 nudge_cta_click tylko z nudge ✓ (guard w sendNudgeEvent `if (!nudgeShownSid)` + launcher.click bez wywołania openChatFlow), K4 sid nudge==rozmowa ✓ (pending sid → state.sessionId one-shot → backend dla pending UUID v4 robi INSERT z tym ID per CHAT-T-082 sekcja 3), K5 błąd beacona nie blokuje czatu ✓ (2x try/catch + .catch), K6 regresja zero ✓ (renderNudge/Card kod niezmieniony, tryRestoreSession niezmieniony, restore wygrywa nad pending), K7 mobile/Shadow DOM OK ✓ (CSS .dz-card* niezmieniony, modyfikacje tylko logika). **KROK 3 commit + push**: `CHAT-T-083 modul: bucket A/B sticky + sessionId-at-shown + beacony nudge (ADR-090 faza 2)`, commit `e8e2e64`, push `e32aeff..e8e2e64 main -> main` OK. **KROK 4 deploy** (granica 116b — moduł PS = Karol ręcznie): podana komenda `rsync -avz --exclude config_pl.xml -e "ssh -i ~/.ssh/id_ed25519 -p 5739" modules/divezone_chat/ divezone@divezonededyk.smarthost.pl:~/public_html/newtmp2/modules/divezone_chat/` (BEZ `--delete`, multi-line z `\\` zgodnie z fix CHAT-T-081 dla pułapki zsh interpretującego zawinięte linie jako osobne komendy), wersja dry-run `-avzn` jako pre-check. Karol wgrał ręcznie po dry-run, potwierdzenie „ok". (W trakcie pisania komendy w `Bash` HEREDOC drobny typo `id_ed25739` poprawiony in-place na `id_ed25519` — Karol nie wgrał błędnej wersji.) **Co dalej (poza zakresem CHAT-T-083)**: CHAT-T-084 (panel raport CTR — `GET /api/admin/nudge-ctr` w standalone admin z `ServerHmacVerifier` jak `AdminAnalyticsController`, agregacja per bucket: `shown`, `clicks`, `ctr = clicks/shown`, JOIN z `divechat_conversations` dla `conversations` (≥1 user-msg) + `conversion_rate = conversations/shown`, rozdzielenie `ab_active=true` vs `false`; UI nowa sekcja/zakładka w panelu PS — tabela 2 wiersze v1/v2 × kolumny shown/clicks/CTR/conversations/conversion z flagą "za mała próba" gdy shown<100/wariant). Sanity-check po wgrywie do wykonania przez Karola (panel sklepu + F12 DevTools): (a) sekcja 5 panelu modułu — checkbox `"Test A/B v1 vs v2 (50/50)"` widoczny PO select Wyglad zachety, default OFF, (b) AB=OFF + v1: odwiedź sklep z innej sesji → dymek pokazuje się jak dziś + F12 Network → jeden `POST /api/widget/event` (HTTP 204) `nudge_shown` z `bucket:v1, ab_active:false`, (c) klik dymka → otwiera czat + drugi POST `nudge_cta_click` z tym samym `session_id`, (d) AB=ON: reload 3-5x → bucket sticky (ten sam wariant z localStorage `dz_ab_bucket`), wyczyść klucz → losuje na nowo (50/50 rozkład widać po N próbach), (e) wyślij pierwszą wiadomość w rozmowie → `divechat_conversations.session_id` (Railway PG) == `divechat_nudge_events.session_id` z obu beaconów (potwierdzenie cyklu sessionId-at-shown), (f) F12 console bez errorów Shadow DOM/JS, mobile ≤599px bez zmian wizualnych vs CHAT-T-081.)
# Wersja: 3.53 | Data: 2026-06-08 (CHAT-T-082 BACKEND DEPLOYED — telemetria CTR nudge (ADR-090 faza 2 krok 1/3): tabela `divechat_nudge_events` na Railway PG + publiczny endpoint `POST /api/widget/event` + akceptacja client-supplied sessionId (decyzja 247a). **Tło**: ADR-090 faza 1 (CHAT-T-081) postawiła wariant v2 zachęty na PROD; do mierzenia CTR brakowało warstwy telemetrii. Spec `_docs/22_spec_ab_ctr_nudge.md` sekcja 4-5 (kontrakt fazy 2), 3 (zmiana cyklu życia sessionId — front generuje UUID v4 przy `nudge_shown`, backend MUSI to zaakceptować z modelem własności po HMAC). **Migracja Railway** (`sql/025_nudge_events.sql` + rollback): `divechat_nudge_events` z `id BIGSERIAL PK`, `session_id TEXT NOT NULL`, `event_type TEXT CHECK ('nudge_shown'|'nudge_cta_click')`, `bucket TEXT CHECK ('v1'|'v2')`, `ab_active BOOLEAN`, `created_at TIMESTAMPTZ DEFAULT NOW()`, UNIQUE `(session_id, event_type)` (dedup — front gwarantuje raz/sesję, backend defensywnie ON CONFLICT DO NOTHING), index `(bucket, event_type, created_at)` (agregacje panelu CTR — CHAT-T-084) + index `(session_id)` (JOIN z divechat_conversations dla wskaźnika konwersji). Wykonana psql na `switchback.proxy.rlwy.net:14368/railway` — CREATE TABLE + 2 indeksy + 5 COMMENT-ów OK, `\d divechat_nudge_events` pokazuje 2 CHECK + UNIQUE + 4 indeksy. **Endpoint** (`standalone/src/Controller/WidgetEventController.php` 92 linie + rejestracja w `config/routes.php`): publiczny POST `/api/widget/event`, BEZ HMAC klienckiego (beacon nie niesie nagłówków auth), BEZ CostGuard, BEZ admina. Walidacja whitelist: `event_type`∈{nudge_shown,nudge_cta_click}, `bucket`∈{v1,v2}, `session_id` strict UUID v4 regex `^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`, `ab_active` tolerancyjnie bool/string. Cokolwiek niezgodne → 204 cichy no-op (NIE 4xx — beacon i tak ignoruje, nie zaśmiecamy logu szumem). RateLimiter per IP (reuse `Usage/RateLimiter.php`, klucz `widget_event_ip:`, luzny limit 60/60s — to nie LLM, fail-open przy awarii PG). Zapis przez nowy `Usage/NudgeEventStore.php` (29 linii: ON CONFLICT DO NOTHING + fail-soft do error_log — beacon fire-and-forget). Body parsowane tolerancyjnie z `php://input` niezależnie od Content-Type (sekcja 5 spec — sendBeacon Blob może mieć dowolny MIME). Odpowiedź ZAWSZE 204. **Client-supplied sessionId** (sekcja 3 spec — kluczowe bezpieczeństwo): `ConversationStore::startOrResume` zwraca teraz `{id, history, session_id}` — EFEKTYWNY sessionId (caller propaguje). Trzy ścieżki: (a) sessionId istnieje + `ps_customer_id` == `customerId` z HMAC → resume z tym ID, (b) sessionId istnieje + cudzy customerId → NIE resume, NIE nadpisuj — generujemy nowy server-side UUID v4 i INSERT z nim (zapobiega podszyciu pod cudzą rozmowę, zachowanie spójne z `{exists:false}` w `/api/chat/history` CHAT-T-046), (c) sessionId nie istnieje → INSERT z TYM client-supplied ID + customerId z HMAC (NOWE — przyjmujemy zaufany UUID). Goście (customerId=null/0): porównujemy 0==0, sessionId pełni rolę sekretu (decyzja 145a). `ChatController::resolveSessionId` waliduje format przed SQL: akceptuje strict UUID v4 (nowy front CHAT-T-083) ALBO legacy 32-hex (server-generated sprzed CHAT-T-082, persystowany w localStorage CHAT-T-059) — inne stringi/null/pusty → generujemy fresh UUID v4 server-side. `generateSessionId()` zmieniony z `bin2hex(random_bytes(16))` na pełny UUID v4 (RFC 4122 wersja 4) dla spójności z `crypto.randomUUID()` na froncie. `ChatService::handle` przyjmuje session_id z `startOrResume` (1 linia zmiany — `$sessionId = $session['session_id']`). **Plików zmienionych**: 2 NEW SQL (`sql/025_nudge_events.sql` + rollback), 2 NEW PHP (`Usage/NudgeEventStore.php`, `Controller/WidgetEventController.php`), 4 MOD PHP (`config/routes.php` import+rejestracja, `Controller/ChatController.php` resolveSessionId+UUID v4 generator, `Chat/ConversationStore.php` ownership check + zwrot session_id, `Chat/ChatService.php` capture effective session_id). **Deploy ADR-089**: backup 4 modyfikowanych do `_deploy_bak/CHAT-T-082/{routes,ChatController,ConversationStore,ChatService}.php.bak`, rsync 6 plików per ścieżka (port 5739, klucz id_ed25519, BEZ `--delete`/`.env`/`vendor`), STOP-point zatwierdzony przez Karola. md5 match repo↔serwer 6/6, php -l clean 6/6 (`/opt/cpanel/ea-php84/root/usr/bin/php`). **Smoke 6/6 zielony** (po wykryciu pułapki MIME — patrz niżej): `/api/health` HTTP 200; POST poprawny body → 204 + wiersz w bazie; powtórka tego samego `(session_id, event_type)` → 204, BEZ duplikatu (ON CONFLICT DO NOTHING działa — sequence skonsumowany ale row nie wstawiony, gap id potwierdza); drugi `event_type` → 204 + drugi wiersz; bad bucket / bad UUID → 204 + zero wpisów; CORS Origin `https://divezone.pl` zaakceptowany (Response::setCorsHeaders whitelist). **PUŁAPKA MIME zweryfikowana w smokie** (DOPISANE do `_docs/22_spec_ab_ctr_nudge.md` sekcja 6 jako wytyczna dla CHAT-T-083): LiteSpeed/ModSecurity na `chat.divezone.pl` zwraca 403 PRZED PHP dla POST z `Content-Type: text/plain` (i `text/plain;charset=UTF-8`). Domyślny `navigator.sendBeacon(url, string)` wysyła text/plain → odpadnie. Działają: `application/x-www-form-urlencoded`, `multipart/form-data`, brak Content-Type. Front (CHAT-T-083) MUSI użyć `new Blob([JSON.stringify(payload)], {type: 'application/x-www-form-urlencoded'})` — nadal "simple request" CORS (brak preflight), backend dalej parsuje JSON tolerancyjnie z php://input niezależnie od Content-Type. **Granice respektowane**: ZERO zmian w `.env`/auth/innych endpoints, ZERO zmian w module PS, ZERO zmian w panelu PS, ZERO regresji w `/api/chat/stream` (sygnatura ChatService::handle bez zmian — przyjmuje string sessionId, ale dzięki podmianie wewnątrz robi to samo co przed: dla nowego UUID v4 INSERT, dla istniejącego matching resume, dla mismatch nowy server-side UUID). Aiven NIE dotknięte (MARTWE, ADR-019). CHAT-T-059 persystencja localStorage zachowana (legacy 32-hex sessionIds dalej akceptowane przez resolveSessionId). CHAT-T-046 weryfikacja właściciela `/api/chat/history` bez zmian (porównuje `ps_customer_id` z HMAC customerId, niedopasowanie → `{exists:false}` — ta sama logika co startOrResume mismatch). **Acceptance criteria 1-7**: K1 migracja przeszła + constrainty + indeksy ✓, K2 dedup ON CONFLICT ✓, K3 niepoprawne body → 204 no-op, zero wpisu ✓, K4 CORS shop-origin OK ✓, K5 client UUID → INSERT z tym ID; cudzy → nowa sesja (code-level + chronione przez ownership check, smoke pełny wymagałby HMAC + DIVECHAT_SECRET — przesunięte do CHAT-T-083 e2e), K6 regresja zero (kontrakt zewnętrzny `/api/chat/stream` niezmieniony), K7 php -l clean ✓. **Co dalej**: CHAT-T-083 (frontend — sticky bucket localStorage 50/50, UUID generowany przy renderNudge/renderNudgeCard, beacony `nudge_shown`/`nudge_cta_click`, `BOOT.nudge.ab` flag, MIME `application/x-www-form-urlencoded` w Blob), potem CHAT-T-084 (panel raport CTR — `GET /api/admin/nudge-ctr` agregacja per bucket + JOIN konwersji + UI w panelu PS).

# Wersja: 3.52 | Data: 2026-06-08 (CHAT-T-081 FRONTEND wariant v2 zachęty (karta z gradientem) + przełącznik v1/v2 w panelu PS, ADR-090 faza 1 — wgrane na PROD (moduł PS, Karol ręcznie wg granicy 116b). **Tło**: ADR-090 (decyzja 245a) wprowadza dwa wyglądy proaktywnego dymka — v1 = obecny prosty dymek (CHAT-T-056/058/060, BEZ ZMIAN), v2 = karta „Welcome Prompt" z gradientem głębi wody (384px, mini-nagłówek DIVEZONE.PL z maską + X, wielki nagłówek „Nie wiesz, jaki sprzęt wybrać?", 2-akapitowy opis, biała karta CTA „Porozmawiajmy na czacie · Odpowiadamy 24/7" z paper-plane). KRYTYCZNE rozumienie 245a: v2 to NOWY WYGLĄD ISTNIEJĄCEGO DYMKA (pojawia się auto po `nudge_delay` sekundach z tymi samymi guardami sessionStorage `dz_nudge_dismissed`/`dz_chat_opened` i dziedziczeniem gatingu launchera), NIE nowy ekran po kliknięciu launchera. Mechanizm wyzwalania (`setupNudge`, delay, recheck po setTimeout) ten sam dla obu wariantów — zmienia się tylko funkcja renderująca. Źródło prawdy wizualnej: `_drafts/design_handoff_welcome_prompt/` (README.md tokeny+copy, Hi-Fi.html markup+gradient+bąble+paper-plane). **Faza 1 = TYLKO przełącznik + render v2; faza 2 (CHAT-T-082/083/084) = A/B + CTR — OSOBNE taski, NIE w tym zakresie.** **Implementacja w 2 plikach modułu PS**: `modules/divezone_chat/views/js/widget-loader.js` (+245 linii: nowy blok CSS pod klasami `.dz-card*` osobny od `.dz-nudge*` v1 — gradient `linear-gradient(165deg,#2a8585→#1e6363→#103f4f→#0a2438)`, 7 dekoracyjnych bąbli `rgba(255,255,255,0.05-0.14)` z pozycjami z Hi-Fi.html, focus-visible ring `2px solid #1e6363` na karcie CTA, `dzCardIn` 180ms ease-out z `transform-origin:bottom right`, `prefers-reduced-motion: reduce` + `@media (max-width:599.98px)` `width:calc(100vw-32px); right:12px; bottom:84px`; nowa funkcja `renderNudgeCard(_text)` reużywa `maskIconSvg(15,'#ffffff')` (NIE duplikuje SVG), paper-plane SVG path skopiowany 1:1 z Hi-Fi.html linie ~731-734 (`M21.5 2.5 L2.8 11.2 ... L21.5 2.5 Z` fill #fff + linia zagięcia opacity 0.35); rozgałęzienie w `setupNudge()`: `var variant = (cfg.variant === 'v2') ? 'v2' : 'v1'`, w `setTimeout` po recheck guards: `if (variant === 'v2') { renderNudgeCard(text); } else { renderNudge(text); }`); `modules/divezone_chat/divezone_chat.php` (+28 linii: `const KEY_NUDGE_VARIANT = 'DIVEZONE_CHAT_NUDGE_VARIANT'` + `const DEFAULT_NUDGE_VARIANT = 'v1'`, lazy read w `renderConfigForm` z fallback do `'v1'` na każdą inną/brakującą wartość, select „Wyglad zachety" NA POCZĄTKU sekcji 5 „Proaktywny dymek" PRZED checkboxem „Wlacz dymek" z dwoma `<option>` v1/v2 + small opis informujący że pole „Tresc zachety" w v2 jest ignorowane, zapis w `submitDivezoneChatConfig` `(Tools::getValue('nudge_variant') === 'v2') ? 'v2' : 'v1'`, dodanie `'variant' => $nudgeVariant` do `$boot['nudge']` array w `hookDisplayFooter`). **Spec A5 — co stałe w kodzie vs configu**: v1 (`renderNudge`) bierze tekst z configu (`BOOT.nudge.text`) + emoji `NUDGE_EMOJI` z loadera — BEZ ZMIAN; v2 (`renderNudgeCard`) IGNORUJE `BOOT.nudge.text` — nagłówek, opis, tytuł i podpis CTA = stałe w kodzie loadera (dopracowany copywriting z draftu, `pr_configuration` utf8 3-bajt i tak nie obsłużyłoby emoji jak v1; jeśli kiedyś v2 ma brać tekst z configu → osobny task). Pole „Tresc zachety" w panelu zostaje dla v1; w v2 jest świadomie ignorowane. **Cache-safe LiteSpeed (ADR-087)**: variant to JEDNA stała wartość w już istniejącym dla-wszystkich-identycznym BOOT (jak `enabled`/`delay`/`text`/`assets`) — HTML hooka pozostaje cache-safe, brak ryzyka warunkowego renderu kolidującego z LiteSpeed. **Rollback-safe**: default v1 (lazy init = brak klucza w bazie = v1), każda inna/brakująca wartość variant w `BOOT.nudge` = v1, fallback PHP i JS niezależne. Rollback po wgrywie = przełącznik panelu na `v1` (zero potrzeby re-deployu lub git revert). **KROK 0**: `git pull origin main` (już aktualne), odczyt spec CHAT-T-081_FRONTEND_wariant-v2-zachety-przelacznik.md, ADR-090 (linie 2646-2691 `_docs/10_decyzje_projektowe.md`), draft README.md + Hi-Fi.html (markup linie 610-740). **KROK 1-2 implementacja**: 4 edycje w widget-loader.js (CSS .dz-card* + renderNudgeCard + branch setupNudge), 5 edycji w divezone_chat.php (KEY_NUDGE_VARIANT const, lazy read w renderConfigForm, select w sekcji 5, zapis w submit, read+'variant' w hookDisplayFooter). **KROK 3 weryfikacja**: `node --check widget-loader.js` → JS syntax OK; `php -l divezone_chat.php` → No syntax errors detected; kryteria akceptacji 1-7 spełnione (panel select, v1 pixel-identyczny — żadna linia `.dz-nudge*` ani `renderNudge` nietknięta, v2 karta gradientowa, te same guardy sessionStorage, teksty v2 stałe z draftu, brak zmian w widget-bundle.js/transport.js/`/token`/drabinie ekspozycji — tylko 2 pliki modyfikowane: `git diff --stat` 2 files changed, 273 insertions(+), 1 deletion(-)). **KROK 4 commit + push**: `CHAT-T-081 modul: wariant v2 zachety (karta z gradientem) + przelacznik v1/v2 w panelu (ADR-090)`, commit `de33943`, push `cacb792..de33943 main -> main` OK. **KROK 5 deploy** (granica 116b — moduł PS = Karol wgrywa, NIE CC): podana komenda `rsync -avz --exclude config_pl.xml -e "ssh -i ~/.ssh/id_ed25519 -p 5739" modules/divezone_chat/ divezone@divezonededyk.smarthost.pl:~/public_html/newtmp2/modules/divezone_chat/` (BEZ `--delete`, multi-line z `\\` po incydencie zsh interpretującym wrappingowane linie jako osobne komendy — pierwszy paste wyrzucił `zsh: permission denied: <source>` i `zsh: no such file: <target>`, fix: linia kontynuacji `\\` na końcu każdej linii czyni komendę copy-paste safe), wersja dry-run `-avzn` jako pre-check + wersja final `-avz`. Karol wgrał ręcznie po dry-run, potwierdzenie „wgrałem". **Co dalej (poza zakresem CHAT-T-081)**: faza 2 ADR-090 = CHAT-T-082 (logika randomizacji wariantu per-wizyta cache-safe, ADR-087 wymusza klient-side losowanie nie PHP w hooku) + CHAT-T-083 (telemetria CTR `divechat_nudge_events` z `variant` + `event_type` enum show/click_card/click_send/dismiss) + CHAT-T-084 (admin dashboard CTR per wariant z statistical significance). Sanity-check po wgrywie do wykonania przez Karola (panel sklepu): (a) sekcja 5 panelu modułu — select „Wyglad zachety" widoczny z opcjami v1/v2, (b) zapis v1 → odwiedź sklep z innej sesji → dymek wygląda identycznie jak dziś (regresja zero), (c) zapis v2 → po `nudge_delay`s pojawia się karta z gradientem nad launcherem, klik karty CTA lub paper-plane otwiera czat, X zamyka i nie pokazuje ponownie w sesji, (d) F12 console — bez errorów Shadow DOM/JS, (e) mobile ≤599px — karta `width:calc(100vw-32px)` mieści się w viewport bez clipping.)
# Wersja: 3.51 | Data: 2026-06-07 (CHAT-T-080 BACKEND DEPLOY na PROD + ADR-089 procedura rsync — DOPIERO TERAZ alert DB z CHAT-T-079 faktycznie żyje na chat.divezone.pl. **Tło**: CHAT-T-079 zaraportowano DONE 2026-06-07 (commit 9bf04d9), ale audyt md5 wszystkich 71 plików PHP standalone repo↔serwer wykrył 3 pliki rozjeżdżone — wszystkie z CHAT-T-079. Diagnoza (architekt): serwer `chat.divezone.pl` NIE jest repo git, więc `git push origin main` ≠ deploy. CC interpretował konwencję 116b „backend standalone — CC wdraża samo" jako „commit + push = wdrożenie", co było BŁĘDNE. Konsekwencja w boju: realny 1045 wystąpił, `divechat_db_alerts`=0, `[DB-DOWN]` w error_log nieobecny — kod alertu fizycznie nie istniał na serwerze (`src/Usage/DbHealthAlert.php` brak; `public/index.php` + `src/Chat/ChatService.php` na stare md5 sprzed CHAT-T-079). **Procedura wykonania CHAT-T-080** (rsync z backupem, weryfikacja, STOP-point): KROK 0 git pull + sanity 3 plików lokalnie (md5: DbHealthAlert.php=81e3de28, index.php=1374b255, ChatService.php=1d3685c6) + grep DbHealthAlert w index.php (linie 27/54/62) i ChatService.php (linie 15/33/479) — logika obecna w repo. KROK 1 SSH preflight: `ssh -p 5739 -i ~/.ssh/id_ed25519 divezone@divezonededyk.smarthost.pl 'echo OK'` → OK, /home/divezone, divezonededyk.smarthost.pl. KROK 2 backup zdalny: `mkdir -p _deploy_bak/CHAT-T-080`, `cp -p public/index.php → index.php.bak`, `cp -p src/Chat/ChatService.php → ChatService.php.bak` (DbHealthAlert.php to nowy plik — brak backupu jest poprawny). Pre-deploy md5 PROD: index.php=89b39066, ChatService.php=e916bdec (różne od lokalnych — potwierdza, że deploy jest potrzebny). `src/Usage/` na PROD JUŻ ISTNIAŁO (CostGuard.php + RateLimiter.php) — mkdir był idempotentny, plan był nadgorliwy. KROK 3 STOP-point: pokazana DOKŁADNA komenda rsync 3 plików Karolowi, czekanie na zgodę. KROK 4 wykonanie po „tak": 3 osobne `rsync -avz -e "ssh -p 5739 -i ~/.ssh/id_ed25519"` per ścieżka (BEZ `--delete`, BEZ rekursji): standalone/src/Usage/DbHealthAlert.php → src/Usage/, standalone/public/index.php → public/, standalone/src/Chat/ChatService.php → src/Chat/. Transferred 3/3, sent ~6KB total. Weryfikacja: `md5sum` na PROD → 81e3de28/1374b255/1d3685c6 = 3/3 match z repo. `php -l` przez `/opt/cpanel/ea-php84/root/usr/bin/php` na 3 plikach → No syntax errors detected 3/3. KROK 5 smoke `curl -s -o /dev/null -w "%{http_code}" https://chat.divezone.pl/api/health` → HTTP 200, 99B, 0.45s. Brak regresji. KROK 6 test alertu w boju (rotacja `.env` ~2 min): zgłoszona gotowość, NIE wykonano autonomicznie (architekt prowadzi z backupem `.env`). KROK 7 docs (ten wpis + ADR-089). **ADR-089 PRZYJĘTA** — `_docs/10_decyzje_projektowe.md` (procedura przejściowa rsync z backupem do czasu przejścia na Deploy Key + git pull na serwerze). Kluczowe punkty ADR-089: (1) każdy task standalone ze zmianą plików ma osobny KROK „DEPLOY" z backupem + rsync + md5 match + php -l + smoke /api/health, (2) rsync per ścieżka BEZ `--delete` (chroni `_deploy_bak/`, `error_log`, sieroty), (3) STOP-point obowiązkowy — architekt zatwierdza komendę rsync przed wykonaniem, (4) zmiany dotykające `.env`/auth/migracji = dodatkowy explicit STOP, (5) granica 116b nie rozszerza się na `newtmp2` — ta pozostaje pod ręczną kontrolą Karola, (6) docelowo Deploy Key na GitHub + `git pull` na serwerze + `git rev-parse HEAD` repo==serwer jako acceptance, ale to OSOBNY task. **Dług powiązany odkryty przez audyt md5**: 6 sierot na serwerze (`scripts/t015_smoke.php`, `t017_smoke.php`, `t020_smoke.php`, `t022_cache_probe.php`, `t022_smoke_provider.php`, `cron_editorial_picks_expire.php`) — pliki bez odpowiednika w repo. Do przeglądu w osobnym tasku: każdy z osobna albo dodać do repo, albo usunąć z serwera. **Granice respektowane**: TYLKO 3 pliki PHP CHAT-T-079 dotknięte. ZERO zmian w `.env`, vendor, error_log, admin/, m/, .htaccess. Backup natychmiastowo dostępny w `_deploy_bak/CHAT-T-080/` do rollbacku. ZERO zmian w `newtmp2/` (granica 116b respektowana). **Co teraz robi alert na PROD**: ChatService::executeTool catch wywołuje DbHealthAlert::maybeAlert. Przy następnym realnym 1045/2002/2003/2006/2013 (MySQL) lub PG SQLSTATE 08*/57P0[13] → mail() na adres z SettingsStore `protect_cost_alert_email` (fallback `.env DIVECHAT_COST_ALERT_EMAIL`) z subject `[DiveChat][DB-DOWN] Baza mysql niedostępna (1045)` + log `[DB-DOWN] target=mysql sqlstate=28000 code=1045 tool=search_products` w error_log + wpis do `divechat_db_alerts` na Railway. Dedup 30 min/baza chroni przed zarzuceniem skrzynki. Pułapki Karol→architekt: test rotacją `.env` musi pokazać że (a) mail przychodzi w sekundach, (b) drugi request w 30-min oknie NIE generuje drugiego maila, (c) rollback hasła = czat wraca do normalnej pracy bez restartu (per-request PHP).)
# Wersja: 3.50 | Data: 2026-06-07 (TASK-ENC-013 EMBEDDINGS DEPLOYED — automatyczny update encyklopedii z source_hash w metadata. Tła: Karol regularnie aktualizuje encyklopedię (dodaje hasła, czasem poprawia istniejące, nigdy nie usuwa), do tej pory `embed_encyclopedia.py` miał tylko `--mode full` (105 haseł × 5 chunków = 525 chunków re-embedowanych za każdym razem, ~$0.025) i `--mode single --concept KEY` (ręczne wskazywanie po nazwie). Po tasku: dwa nowe tryby — `--mode check` (read-only raport rozjazdu plik↔baza, zero API, exit 0=AKTUALNE, exit 2=są zmiany) i `--mode changed` (auto-wykrywa NOWE+ZMIENIONE po porównaniu source_hash z plików raw vs `metadata->>'source_hash'` w `encyclopedia_chunks`, embeduje TYLKO zmienione). **Zmiany w kodzie** (scripts/embed_encyclopedia.py +290/-8 linii): (1) `compute_source_hash(entry)` sha256 z kanonicznego JSON-a (`sort_keys=True, ensure_ascii=False, separators=(',',':')`) — odporne na reformatowanie pliku (kolejność kluczy, wcięcia nie wywołują re-embedu), (2) `build_chunks()` dorzuca `"source_hash"` do metadata per-hasło (identyczny w 5 chunkach hasła), zapisywany w KAŻDYM trybie (full/single/changed), (3) `load_entries(concept_key)` przyjmuje `None | str | list` — wsteczna zgodność dla full/single, nowa ścieżka dla changed, (4) `check_status(conn)` + `print_check_report()` klasyfikują: NOWE / ZMIENIONE (z wyodrębnioną podkategorią `legacy_no_hash` — chunki bez source_hash sprzed tego taska, jednorazowa migracja, NIE anomalia) / AKTUALNE / SIEROTY (raport, NIE usuwam — Karol nie usuwa haseł) / ANOMALIE (niespójność hash między chunkami, count != 5). **Granice**: ZERO nowych tabel (hash w istniejącym jsonb metadata), ZERO usuwania chunków, ZERO zmiany modelu (text-embedding-3-large, 3072 dim — zgodność z ExpertKnowledge.php), ZERO zmiany istniejącego zachowania trybów full/single/test-query (tylko teraz zapisują source_hash). **Runbook**: _docs/32_runbook_update_encyklopedii.md (numer kolejny po najwyższym 31 w _docs/; spec sugerował 23 ale ten numer zajęty 2x). Opisuje: jak dodać/poprawić hasło, kiedy changed vs full, jak działa hash kanoniczny, granice, szybki diagnostyczny przepływ. **Test end-to-end na Railway PG** (switchback.proxy.rlwy.net:14368/railway): (a) `--mode full` przed wdrożeniem hash w bazie wszedł i embedował 525 chunków, 196308 tokenów, $0.0255, 8.9s; verify pokazał 525/525 chunków z embeddingami (definition/faq/purchase/seller/synonyms × 105); test_query "jaki automat do nurkowania wybrać" → top-1 AUTOMAT_ODDECHOWY [faq] sim=0.6717, "suchy skafander ocieplacz" → top-1 SUCHY_SKAFANDER [definition] sim=0.6891, "bojka dekompresyjna szpulka" → top-1 SZPULKA [faq] sim=0.6384 — jakość semantyczna OK po dorzuceniu hash. (b) `--mode check` po full → 105 AKTUALNE, exit 0, czysty raport. (c) Mikro-test: edytowano testowo `seller_notes` w ANTIFOG.json (dopisek " [TEST-ENC-013]"); `check` wykrył jako "ZMIENIONE TREŚCI: 1 ANTIFOG", exit 2; `changed` embedował TYLKO ANTIFOG (5 chunków, 1858 tokenów, $0.0002 = 127× taniej niż full); revert pliku + `changed` (powrót do oryginału, znowu $0.0002); `check` → 105 AKTUALNE, exit 0. Cykl edycja→check→changed→check działa dokładnie jak w runbook. **Workflow Karola po tasku**: edytuj plik `data/encyclopedia/v3/gen_v2/raw/{CONCEPT_KEY}.json` → `python scripts/embed_encyclopedia.py --mode changed` → koszt tylko za ruszone hasła → `--mode check` żeby potwierdzić "Wszystko AKTUALNE". Czat (ExpertKnowledge.php) czyta encyclopedia_chunks na żywo per-request — zmiana widoczna w czacie natychmiast, bez deployu/restartu. ADR-088 (encyklopedia jako jedyne źródło wiedzy eksperckiej czatu — nie blog, nie CMS) zachowane bez zmian; ten task to procesowe usprawnienie nad tą decyzją.)
# Wersja: 3.49 | Data: 2026-06-07 (CHAT-T-079 BACKEND DEPLOYED — alert na awarię połączenia DB. **DŁUG ADR-088 ZAMKNIĘTY** (incydent 1045 z 2026-06-06: ~18h niedostępności produktów w czacie niezauważonej — bez sygnału, że MySQL padł). Teraz każda awaria połączenia jest sygnalizowana w minutach. **Detekcja**: ChatService::executeTool catch (jedyny przechwytywacz pokrywający wszystkie narzędzia: search_products MySQL + get_expert_knowledge/get_shipping_info/order PG) → DbHealthAlert::classifyConnectionFailure. MySQL po errorInfo[1]: 1045/2002/2003/2006/2013 → target=mysql. PG po SQLSTATE: klasa 08 (regex `^08[0-9A-Z]{3}$`) + 57P01 admin_shutdown + 57P03 cannot_connect_now → target=pgsql. Błędy logiczne (42703 zła kolumna, 42P01 zła tabela, 42601 syntax, 23505 duplicate) NIE alertują (decyzja 246a — bug kodu/migracji, nie awaria infra). **Kanał**: mail() z From: noreply@divezone.pl + log [DB-DOWN] do error_log ZAWSZE (niezależnie od dedup maila, decyzja 248a — tani sygnał). Adres z SettingsStore `protect_cost_alert_email` (reuse klucza CostGuard, decyzja 248a "jak reszta" — zero nowych kluczy konfiguracyjnych) + fallback .env DIVECHAT_COST_ALERT_EMAIL + sanity FILTER_VALIDATE_EMAIL. **Dedup**: tabela divechat_db_alerts w PG (Railway) z UNIQUE (alert_window, db_target) — okno 30 min (decyzja 245b: krótsze niż cost_alerts dobowe, awaria infra wymaga szybszego ponowienia po nawrocie). INSERT ON CONFLICT DO NOTHING + rowCount() race-safe (wzorzec 1:1 z CostGuard maybeSendAlert, decyzja 247a osobna klasa + osobna tabela). alert_window liczone PG-em: to_timestamp(floor(EPOCH/1800)*1800) — jedno źródło czasu, zero driftu PHP↔PG. **Pliki**: sql/024_db_health_alerts.sql + rollback (7-column table z full COMMENT-ami), standalone/src/Usage/DbHealthAlert.php (236 linii: maybeAlert public; classifyConnectionFailure / isConnectionFailure / sanitizeMessage public static — pure functions; resolveAlertEmail i sendMail private), standalone/src/Chat/ChatService.php (+13 linii: import DbHealthAlert, ctor param, catch executeTool wywołuje maybeAlert wewnątrz try/catch — best-effort, NIGDY nie rzuca, NIE zmienia zachowania czatu, dalej zwracamy ['error' => ...] do tool_result), standalone/public/index.php (+7 linii: konstrukcja DbHealthAlert($db, new SettingsStore()) i wstrzyknięcie do ChatService), standalone/tests/DbHealthAlertTest.php (227 linii: 32 testy, wszystkie zielone — 5 MySQL codes + SQLSTATE 28000 dla 1045, 7 PG SQLSTATEs, 5 błędów logicznych → null, 4 nie-PDO Throwable → null, isConnectionFailure × 4, sanitizeMessage × 6). **Bezpieczeństwo**: error_excerpt z preg_replace 'password=|pwd=' → [REDACTED] + mb_substr 240 znaków, bez DSN. Tabela alertu w PG (Railway) NIE w MySQL PS — gdy padnie MySQL, alert musi działać. Gdy padnie PG sam, dedup INSERT się wywróci → catch → NIE wysyłamy maila (defensywa przed zarzuceniem skrzynki przy dłuższej awarii PG); [DB-DOWN] w error_log jako fallback sygnał. **Granice respektowane**: TYLKO ścieżka catch w ChatService + nowa klasa + nowa tabela. ZERO zmian w treści odpowiedzi do klienta. ZERO zmian w CostGuard / RateLimiter / SettingsStore / ChatController. Model dostaje to samo co dziś. Zero zmian w module PS, frontu, panelu admina. **Wyniki 6 kryteriów + lint**: K1 isConnectionFailure() klasyfikuje poprawnie 5 MySQL + 7 PG kodów (test 32/32 PASS), K2 błędy logiczne 42703/42P01/syntax/duplicate → null (NIE alert), K3 error_excerpt bez hasła (test 'password=SuperSekret123' → '[REDACTED]', 'pwd="SecretPass!@#"' → '[REDACTED]'), K4 truncate 240 znaków, K5 zachowanie czatu niezmienione (executeTool dalej zwraca ['error' => ...]), K6 php -l clean dla DbHealthAlert.php + ChatService.php + index.php + DbHealthAlertTest.php. **Migracja PROD wykonana** na Railway (switchback.proxy.rlwy.net:14368/railway) — CREATE TABLE OK, 7 COMMENT-ów OK, \d divechat_db_alerts pokazuje UNIQUE (alert_window, db_target) + serial id PK, count=0. **Backend deploy autonomiczny** (konwencja 116b) po push 9bf04d9 do origin/main. Aiven już MARTWY (zweryfikowane 2026-06-07: Karol potwierdził, że Aiven DATABASE_URL jest zakomentowany w .env jako "STARE", realny Config::load czyta Railway). **Dedup INSERT-em zweryfikuje smoke po deployu** (ON CONFLICT to sprawdzona ścieżka 1:1 z CostGuard, gdzie działa od CHAT-T-064). **Walidacja w boju** (opcjonalna): bezpieczny test polega na czasowym podaniu złego hasła MySQL w .env (rotate-back-2-min) — przy najbliższym pytaniu produktowym DbHealthAlert powinien wstawić wiersz mysql/1045 i wysłać mail w ciągu sekund; rollback hasła i drugi request w tym samym 30-min oknie NIE powinien wysłać drugiego maila. **Pułapki**: gdy SettingsStore::get rzuci (PG-down scenario), DbHealthAlert::resolveAlertEmail łapie w try/catch i idzie na .env DIVECHAT_COST_ALERT_EMAIL → alert dalej dotrze. Dług ADR-088 zamknięty: 18h ciszy → minuty.)
# Wersja: 3.48 | Data: 2026-06-05 (CHAT-T-076 BACKEND DEPLOYED — skrócenie TTL tokenu klienta 1h→15min, domknięcie ADR-084 191c. Po wdrożeniu T-069 (refresh tokenu na froncie) długie okno 1h straciło uzasadnienie; 15 min domyka okno replay z ADR-079 i ujednolica z `expires_in:900` deklarowanym przez endpoint tokenu front-controllera PS (przed zmianą był rozjazd: front 15 min vs backend 60 min). **Problem 401 ZAMKNIĘTY** (T-069 refresh reaktywny+proaktywny + T-076 TTL spójny). **Zmiana**: standalone/src/Auth/HmacVerifier.php linia 14 — `private int $maxAgeSec = 3600` → `900` + komentarz "(CHAT-T-076; po wdrozeniu odswiezania tokenu T-069 dlugie okno zbedne; domyka okno replay ADR-079; spojne z expires_in:900 endpointu)". Plus dopisek do ADR-084 191c: "ZREALIZOWANE CHAT-T-076 (2026-06-05): maxAgeSec 3600→900 po potwierdzeniu refreshu na PROD; ServerHmacVerifier nietknięty." **Propagacja**: 4 konsumentów (ChatController:220/303/427 dla handle/stream/history, OrderStatusController:56) wołają new HmacVerifier($secret) BEZ drugiego argumentu → dziedziczą nowy default 900. ZERO zmian w kontrolerach. **Granice respektowane**: TYLKO HmacVerifier.php (1 linia + komentarz) i 1 linia dopisku w ADR-084. ServerHmacVerifier NIETKNIĘTY (maxAgeSec=300, osobny kanał serwerowy panel PS + mobile /m/api/* — inny trust, inny payload employee_id:ts vs customerId:ts). ZERO modułu PS, ZERO frontu, ZERO kontrolerów. Mobile panel /m/ (T-071 cookie 12h sliding) nieruszany. **Wyniki kryteriów 1-6 + 7 ad hoc**: K1 maxAgeSec=900 grep potwierdza, K2 fresh token PROD smoke: GET https://divezone.pl/module/divezone_chat/token → {token, customerId:0, time, expires_in:900} → POST /api/chat/stream z tymi nagłówkami → HTTP 200 + SSE event:status + event:done + "pong" + conversation_id=356 utworzona, K3 token >15min → 401 (lokalny test PHP 6 timestampów: 0/600/900s ACCEPT, 901/1800/3600s REJECT — granica abs(time()-ts)>900 matchuje), K4 token 16-60min (przed zmianą ważny) → 401 (lokalnie 1800s i 3600s REJECT — okno faktycznie zacieśnione), K5 ServerHmacVerifier nietknięty (grep maxAgeSec=300), K6 php -l clean lokalnie + na PROD (ea-php84) → No syntax errors detected, K7 widget refresh-łapie-po->15min — do potwierdzenia ad hoc gdy Karol kliknie widget po >15min bezruchu (T-069 refresh-on-401 z proaktywnym check wieku BOOT.time przejmuje ścieżkę: pierwsze kliknięcie po bezruchu → 401 z backendu → transport łapie → endpoint tokenu PS → fresh → ponawia → 200, UX bez błędu; tylko trigger przesunięty z >60min na >15min). PROD curl negatyw: time przesunięte -1000s + sygnatura niepasująca → HTTP 401 (potwierdzenie że jakieś źródło niezgodności daje 401, nie tylko deklaracja). **Smoke 2 ślady**: curl GET token endpoint → expires_in:900, POST /api/chat/stream → HTTP 200 + SSE pong + cost ~0.116 USD ~0.425 PLN. **Co się NIE zmienia**: panel PS /api/conversations* (ServerHmacVerifier 300s), mobile /m/api/* (cookie session), endpoint tokenu PS (T-069 already declared expires_in:900). **Co się zmienia z perspektywy klienta sklepu**: aktywna sesja — refresh proaktywny T-069 trzyma token świeży, klient nigdy nie widzi 401; bezruch >15min — pierwsze kliknięcie 401 → transport retry → fresh → 200, UX bez błędu (przed T-069 było analogicznie po >60min, teraz po >15min — okno replay węższe, ścieżka identyczna); czat na karcie otwartej godzinami — refresh nadal pokrywa. ADR-079 (TTL dług) i ADR-084 (refresh + skrócenie TTL) zrealizowane w całości.)
# Wersja: 3.47 | Data: 2026-06-05 (CHAT-T-073 FRONTEND DEPLOYED — PWA dla /m: manifest + ikony + apple-touch + service worker. Ostatni z 3 tasków mobile admin MVP (T-071 auth → T-072 front → T-073 PWA), z T-074 (zamknięcie konsoli) i T-075 (markdown) dopełniającymi pakiet. **MOBILE ADMIN MVP KOMPLETNY**. Fundament pod push fazy 2 na iOS (web push działa TYLKO z PWA na ekranie głównym — ADR-086 218b/219a). **Pliki**: standalone/public/m/manifest.webmanifest (877B: name/short_name DiveZone, start_url/scope/id="/m/" spójne z cookie Path=/m z T-071, display=standalone, orientation=portrait, theme_color #1e6363 + background_color #f3f5f6, lang=pl, 4 ikony any+maskable 192+512), standalone/public/m/icons/ × 5 PNG przeskalowane z ~/public_html/newtmp2/img/logo-divezone-fv.png (290×95 czarne na transparent, alpha=Blend — istniejący asset marki, NIE tworzono nowego logo) komendą `magick "$SRC" -background white -alpha remove -alpha off -resize WxH -gravity center -extent SxS PNG24:out.png` — wszystkie PNG24 (BEZ alfy spójnie z favicon128 sklepu, apple-touch wymaga BEZ alfy): icon-192.png (9297B, padding 8% any), icon-512.png (30661B, any), icon-192-maskable.png (7995B, safe zone 80% maskable Android), icon-512-maskable.png (25506B, maskable), apple-touch-icon.png (8537B, 180×180 iPhone), standalone/public/m/sw.js (3067B: minimalny SW z CACHE_VERSION='dz-m-v1-2026-06-05' bumpalnym przy następnym deploy, install=pre-cache shellu /m/+index.html+app.js+styles.css+manifest+3 ikony+skipWaiting, activate=clients.claim+skasowanie starych cache, fetch z 5 reguł: 1) /m/api/* → wczesny return PRZED respondWith bypass cache całkowicie (dane na żywo + cookie auth), 2) method!=GET → bypass, 3) request.mode=navigate → network-first z fallbackiem caches.match(/m/index.html) lub Response Offline 503 dla offline shellu, 4) same-origin /m/<asset> → stale-while-revalidate, 5) cross-origin → bypass), standalone/public/m/index.html (<head> +link rel="manifest", +link rel="apple-touch-icon", +meta apple-mobile-web-app-{capable=yes, status-bar-style=black-translucent, title=DiveZone} dla iOS Safari PWA standalone, ścieżki RELATYWNE), standalone/public/m/app.js (+15 linii: registerServiceWorker() wywoływana z boot, HTTPS-only sprawdzenie + try/catch + scope:/m/ + cicho na błąd bo PWA opcjonalna). **Granice respektowane**: TYLKO public/m/, ZERO backendu, ZERO modułu PS, ZERO bibliotek. SW NIE cache'uje /m/api/* (static sanity grep "pathname.startsWith('/m/api/')" = 1 wczesny return), NIE cache'uje POST/PUT/DELETE. Scope /m/ wszędzie (start_url/scope/id/register spójnie z cookie Path=/m). Logika auth/listy/detalu/markdown z T-071/072/075 nietknięte. **Smoke 10/10 PASS na PROD**: K1 GET /m/manifest.webmanifest → 200 valid JSON (name=DiveZone — rozmowy, scope=/m/, start_url=/m/, icons=4; Content-Type application/octet-stream akceptowalne — Chrome/Safari oba parsują manifest niezależnie od CT, dowód: Android install zadziałała), K2 wszystkie 5 ikon → HTTP 200 CT=image/png, K3 Android Chrome (zwykła karta, NIE incognito — Chrome świadomie nie oferuje instalacji PWA w incognito, decyzja Google): menu ⋮ → "Zainstaluj aplikację" → ikona DiveZone na ekranie głównym, uruchamia się standalone bez paska URL (Karol potwierdził), K4 iPhone Safari Udostępnij→Dodaj do ekranu — do potwierdzenia ad hoc gdy wspólniczka dostępna (wszystkie meta + apple-touch-icon na miejscu, statyczna weryfikacja OK), K5 SW activated — pośrednio OK (Android install wymaga aktywnego SW), K6 SW NIE cache /m/api/* — static sanity OK + API żyje (/m/api/whoami bez cookie 401, /api/health 200), K7 offline shell — kod ma fallback caches.match(/m/index.html), test airplane ad hoc, K8 user-flow w standalone OK pośrednio (cookie Path=/m działa też w standalone, scope spójny), K9 markdown OK pośrednio (app.js z T-075 nietknięty), K10 brak regresji /m/ w przeglądarce — curl /m/ 200 + body zawiera rel=manifest=1, apple-touch=1, app-capable=1, app-title=1. Pierwszy test Karola był w incognito → "nic się nie pokazuje do instalacji" było SPODZIEWANE (Chrome nie oferuje PWA install w incognito); retest w zwykłej karcie potwierdził sukces. **Pułapki SW**: CACHE_VERSION bumpować przy każdym przyszłym deploy żeby activate skasował stare cache (bez tego SW jest "lepki" — klient widzi stary JS/CSS po deploy). Klient zmuszony do odświeżenia: DevTools → Application → Service Workers → Unregister + hard reload. **Push fazy 2 (NIE w MVP, ADR-086 219a)** — VAPID + subskrypcje urządzeń w PG + serwis wysyłki, SELEKTYWNY per knowledge_gap/eskalacja (nie każda rozmowa — szum), konfigurowalny per pracownik. Osobny task gdy wolumen wzrośnie lub pojawi się realna potrzeba bycia wołanym (przy 2-4 rozmowach/dzień ręczne pull-to-refresh + przycisk wystarcza).)
# Wersja: 3.46 | Data: 2026-06-05 (CHAT-T-074 BACKEND/OPS DEPLOYED — P0 zamknięcie publicznej deweloperskiej konsoli testowej "DiveChat Test Console" pod https://chat.divezone.pl/. API już było chronione (401 dla /api/chat, /m/api/*, /admin/), problem = rekonesans (mapa systemu: providerzy/modele/endpointy/format JSON) + wizerunek. Decyzja 220a (ADR-086): pliki konsoli przenieść POZA webroot (nie kasować), index.html podmienić na minimalną zaślepkę BEZ JS/linków/ujawnień. **Operacje**: (1) Lokalnie git rm 6 plików (standalone/public/index.html stara konsola linkowała js/* + css/chat-test.css, standalone/public/css/chat-test.css 21921B, standalone/public/js/{chat,console,history,pricing,settings}.js — po git rm katalogi css/ i js/ puste, git je pomija; nowy standalone/public/index.html 936B zaślepka z meta robots noindex+nofollow, inline CSS, brak JS/linków/odwołań do API, treść "DiveZone" + "Brak dostępu publicznego."; .gitignore dodano standalone/public/error_log defensywnie). (2) PROD ssh: mkdir ~/public_html/chat.divezone.pl/_console_backup_2026-06-05/, mv public/{index.html,chat.js root duplikat 20641B,chat-test.css root duplikat 21921B,js cały katalog 5 plików,css/chat-test.css} → backup POZA webroot (zachowane, nie skasowane na ślepo), rmdir public/css, rm public/error_log (25242B publiczny log PHP), scp nowy index.html → public/index.html. (3) public/ po operacjach: admin/ (nietknięty, własne admin/css/admin.css + admin/js/admin-*.js, zero linków do /js/* lub /css/chat-test.css — greppano), .htaccess (NIETKNIĘTY: DirectoryIndex + RewriteCond !-f && !-d → index.php), nowy index.html 936B, index.php (NIETKNIĘTY front controller API), m/ (nietknięty: T-072 mobilny panel), .well-known/. **Granice respektowane**: index.php API nietknięty, /m/ + /api/ + /m/api/ + /admin/ + /.well-known/ żyją bez zmian, widget produkcyjny ładowany przez moduł PS NIE linkuje do chat.divezone.pl/{js,css}/ (greppano widget-bundle.js), pliki konsoli przeniesione POZA webroot do backupu (możliwy przywrócenie). **Smoke 10/10 PASS na PROD**: (1) GET / → HTTP 200 zaślepka SIZE=936, grep w body: claude=0/gpt=0/sonnet=0/'/api/'=0/json|tools_used|provider=0 (zero ujawnień), (2) GET /js/{chat,console,history,pricing,settings}.js + /css/chat-test.css → 6/6 HTTP 404, (3) GET /index.html → 200 SIZE=936 zaślepka, (4) POST /api/chat bez auth → 401 "Brak wymaganych headerów autoryzacji" (API żyje), (5) GET /m/ → 200 SIZE=4971 (T-072 niezłamane), (6) GET /m/api/whoami → 401 "Unauthorized" (T-071 niezłamane, routing API żywy), (7) GET /admin/ → 401, (8) GET /error_log → 404 (usunięty z webroot), (9) widget kliencki na divezone.pl działa (Karol potwierdził, widget ładowany przez moduł PS, sanity /api/health → 200), (10) mobilny /m/ pełny user-flow działa po zmianach (Karol potwierdził: login → lista → detail → status → logout). Backup na PROD POZA gitem: ~/public_html/chat.divezone.pl/_console_backup_2026-06-05/ zawiera index.html stara konsola + chat.js + chat-test.css + css_chat-test.css + js/ (5 plików). Opcjonalnie skasować po ~30 dniach. Follow-up: rozważyć przekierowanie PHP error_log do katalogu poza webroot (np. ~/public_html/chat.divezone.pl/logs/) — osobny temat hardeningu logów.)
# Wersja: 3.45 | Data: 2026-06-05 (CHAT-T-075 FRONTEND DEPLOYED — renderowanie markdown w /m (bold/linki/listy/paragrafy) jak na produkcji. ADR-086. Wcześniej widok rozmowy w /m pokazywał surowy tekst (gwiazdki, gołe URL-e) bo używał escHtml(content). Teraz renderuje jak produkcyjny widget. **Pliki**: `standalone/public/m/app.js` (+60 linii: funkcja `renderMarkdown` skopiowana 1:1 z modules/divezone_chat/views/js/widget-bundle.js linie 264-321 — kolejność krytyczna: 1) escHtml na START (& < > " ' — XSS-safe), 2) linki `[label](url)` regex matchuje TYLKO https?:// i mailto: → `<a href target=_blank rel="noopener noreferrer">label</a>` (`javascript:` i inne schematy zostają jako raw text), 3) bold `**...**` → `<strong>`, 4) listy: linie zaczynające się od `- ` lub `• ` → `<ul><li>`, 5) paragrafy: linie tekstu → `<p>` z `<br>`, pusta linia = separator. Decyzja escHtml vs widget escapeHtml: użyłem istniejącej escHtml — funkcjonalny ekwiwalent, te same 5 znaków escape, różnica tylko `&#39;` vs `&#039;` (oba poprawne entity). Brak duplikacji helpera. W renderDetail linia ~423 escHtml(content) → renderMarkdown(content) dla obu rol user/assistant (escape jest pierwszym krokiem renderMarkdown — XSS-safe nawet jeśli user wkleił `<script>`). Filtr `m.role in (user, assistant)` zachowany, tool_result/system dalej pomijane). `standalone/public/m/styles.css` (+34 linie: sekcja `.bubble p/strong/a/ul/li` — `.bubble p {margin:0 0 8px}` + `:last-child margin-bottom:0` żeby `<p>` nie psuł odstępów w dymku, `.bubble strong {font-weight:700}`, `.bubble a {text-decoration:underline; word-break:break-word; overflow-wrap:anywhere}` (długie URL-e zawijają), `.bubble-user a {color:#e6f3f3}` (jasny kontrast na teal tle) vs `.bubble-assistant a {color:var(--teal-dark)}` (ciemny na białym), `.bubble ul {padding-left:22px}` + `.bubble li {line-height:1.45}`, hover/focus-visible text-decoration-thickness:2px). **Granice respektowane**: TYLKO 2 pliki frontowe (app.js + styles.css), ZERO backendu, ZERO modułu PS. renderMarkdown XSS-safe — escape PRZED tagami. Vanilla, zero bibliotek. Filtr user/assistant nietknięty. **Smoke 8/8 PASS**: lokalnie node --check + 10 input testów (plain/bold/link/list/multi-para/4xXSS/nested) — bold/listy/linki renderują, XSS payloady wszystkie escapowane (script→&lt;script&gt;, img→escape, javascript:→raw text przez selektywny regex, " onclick→&quot;). PROD: curl app.js 200 SIZE=20604, styles.css 200 SIZE=10985. Baza ma 148 rozmów z `**` i 155 z linkami markdown — Karol potwierdził render w przeglądarce telefonu + desktopa (rozmowa "Pomóż dobrać sprzęt" 647427b3a5… z listami **bold** w odp. asystenta). Reszta /m bez regresji (login, lista, status, logout, pull-to-refresh nietknięte — jedyne zmiany to renderMarkdown + 1 linia w renderDetail + style .bubble). Renderer logicznie identyczny z widget-bundle (1:1 kopia). T-073 (PWA) dalej NIE rozpoczęty — STOP po T-075. Markdown obsługuje tylko bold/linki/listy/paragrafy — nie italic, headery, blockquote, code (spójne z widget produkcyjnym).)
# Wersja: 3.44 | Data: 2026-06-05 (CHAT-T-072 FRONTEND DEPLOYED — mobilny widok obsługi rozmów (3 ekrany, vanilla, pod /m/). ADR-086. Drugi z 3 tasków mobile admin (T-071 auth DONE → T-072 front → T-073 PWA). **Pliki**: standalone/public/m/index.html (129 linii, SPA shell z 3 sekcjami view-login / view-list / view-detail, meta viewport-fit=cover + theme-color, robots noindex, autocomplete username/current-password, inputmode/autocapitalize=none dla autofill iOS), standalone/public/m/styles.css (449 linii, mobile-first, custom properties --teal/--tap=44px/--radius, min-height:100dvh + safe-area-inset-*, bubbles user (teal prawa) vs assistant (biale lewa), pull-to-refresh indicator, prefers-reduced-motion, **`.view[hidden] { display: none }` override UA** — patrz bug niżej), standalone/public/m/app.js (548 linii, vanilla IIFE bez ES6 lambda/let/const, stan w pamięci bez localStorage dla rozmów (RODO), `api()` helper ZAWSZE credentials:'include', boot=whoami → goToList/login, login z setTimeout(verifyAndEnter, 0) (defensywne przed iOS Safari cookie persistence quirks), lista z 3 filtrami (todo→admin_status=new domyślny + gap→knowledge_gap=1 + all, search debounce 350ms, "Załaduj więcej"), karta wiersza first_message clamp 2 linie + truncate 240 + relativeTime PL + badge admin_status + badge "Luka" + message_count, pull-to-refresh touchstart/move/end z threshold 70px (tylko gdy scroll na top, dy>12 aktywuje), detail z renderowaniem tylko user+assistant (tool_result/system POMIJANE jak widget T-059), stringifyContent obsluguje string/array (Claude content blocks)/object, save status segmented control 4 wartosci 1:1 z backendem (new/reviewed/knowledge_created/ignored) + toast + loadList w tle, logout=POST /logout + cleanup state, handleAuthFailure na 401 z każdego fetcha → showView('login') + "Sesja wygasła"). **.htaccess bez zmian** — `RewriteCond !-f && !-d` przepuszcza istniejące statyki (/m/ → DirectoryIndex zwraca index.html), /m/api/* → router PHP → MobileAuth*Controller. **Bug w smoke (CSS specificity)**: po loginie front "cicho wracał" na ekran logowania — Network DevTools pokazało WSZYSTKIE requesty 200 (login + whoami + conversations). Root cause: `.view { display: flex }` (0,1,0) nadpisywal UA `[hidden] { display: none }` (0,0,1), 3 sekcje wyświetlone na sobie naraz, login na wierzchu wizualnie. Fix: 4 linie CSS `.view[hidden] { display: none }` (0,1,1 > 0,1,0). Redeploy samego styles.css. Diagnostyka via DevTools Network screenshot była decydująca — symptom naprowadzał na cookie/auth, ale to było CSS. Smoke 12/12 PASS na PROD: GET /m/ → HTML 200, /m/app.js → JS 200 18476B, /m/styles.css → CSS 200 10011B (po fix), /m/api/whoami bez cookie → 401 (T-071 nieuszkodzone), pełny user-flow przez curl jar: login → whoami → list (filtry admin_status=new / knowledge_gap=1 / all) → detail #352 (messages roles user/assistant/tool_result/assistant — renderuje 3 user+assistant) → status new→reviewed→new (admin_notes zapisane) → logout → whoami=401. Karol potwierdził telefon + desktop: "Działa". **Granice**: ZERO modułu PS, ZERO zmian w backendzie PHP, ZERO modyfikacji .htaccess, ZERO localStorage dla rozmów (RODO), ZERO auto-pollingu (216a — pull-to-refresh + przycisk wyłącznie), tylko user+assistant w widoku rozmowy, credentials:'include' wszędzie. T-073 (PWA manifest + ikony) NIE rozpoczęty — STOP po T-072 zgodnie z brifem.)
# Wersja: 3.43 | Data: 2026-06-05 (CHAT-T-071 BACKEND DEPLOYED — auth mobilnego panelu admina. ADR-086 (mobilny widok obsługi rozmów poza panelem PS — Karol+wspólniczka na telefonach). Pierwszy z 3 tasków mobile admin (T-071 auth → T-072 front → T-073 PWA). NAJWRAŻLIWSZY task: dotyka haseł. **Architektura**: dwie nowe klasy w `standalone/src/Auth/` (MobileAuthenticator, MobileSessionStore) + helper PsCookieKeyReader + dwa kontrolery (MobileAuthController dla login/logout/whoami, MobileConversationsController dla list/detail/status). Brama auth: cookie HttpOnly+Secure+SameSite=Lax+Path=/m+Max-Age=12h (sliding). Zero modyfikacji desktopowego ConversationsController (kanał serwerowy HMAC panelu PS dziala bez zmian). Reuse: ConversationStore (list/getBySessionId/updateAdminStatus) + UsageLogger + RateLimiter (klucz mlogin:ip:* 10 prob/15 min). **MobileAuthenticator**: SELECT pr_employee.email/passwd/active (MysqlConnection, SELECT-only) → verifyPassword: prefix \$2 → password_verify (bcrypt, wszystkie 6 realnych kont admin/operator), len==32+ctype_xdigit → legacy hash_equals(md5(_COOKIE_KEY_.pass)) (defensywny fallback, dzis ZERO takich kont w sklepie), inne formaty → false. Timing-safe: gdy email nieznany/active=0/null-hash/nieznany format → dummy password_verify na stałym bcrypt cost-10 hashu (lokalnie zmierzone: dummy=45.2ms vs real=43.4ms — w szumie). Po haśle OK: SELECT divechat_admin_roles.role (PostgresConnection) — brak roli = brak dostępu mimo poprawnego hasła. PS_DB_PREFIX z .env (whitelist regex). PsCookieKeyReader: lazy include parameters.php PS, wyciąga `[parameters][cookie_key]`, NIE loguje wartości, cache w pamięci procesu; default path `<projectRoot>/newtmp2/app/config/parameters.php`, path z `PS_PARAMETERS_PATH` w .env nadpisuje; brak pliku → error_log warning + null (fallback nieaktywny, bcrypt działa niezależnie). **MobileSessionStore**: tabela divechat_mobile_sessions(session_token PK varchar(128), employee_id, role, created_at, expires_at, last_seen_at) + indeksy na employee_id i expires_at. create() → bin2hex(random_bytes(32)) + INSERT z expires_at=NOW()+12h. validate() → atomowy UPDATE SET last_seen_at=NOW(), expires_at=NOW()+12h WHERE token=? AND expires_at>NOW() RETURNING — sliding TTL race-safe. destroy() → idempotentne DELETE. **Endpointy /m/api/**: POST /login (rate-limit per IP 10/15min → 429; anti-enumeration: zły email/złe hasło/brak roli/active=0 → 401 identyczny komunikat "Nieprawidlowy login lub haslo"), POST /logout (destroy + clear cookie), GET /whoami (validateOrFail → employee_id+role lub 401), GET /conversations (paginacja+search+knowledge_gap+admin_status, filtr "wymagające uwagi" ustala FRONT), GET /conversations/{sid} (reuse + conversation_cost), POST /conversations/{sid}/status (whitelist new|reviewed|knowledge_created|ignored). Cookie name: dz_madmin, format: 64 hex (whitelist regex w validateOrFail). **Migracja PG**: sql/023_mobile_sessions.sql + rollback. **Deploy**: scp 5 nowych plików PHP + routes.php na chat.divezone.pl, psql -f sql/023_mobile_sessions.sql na Railway. php -l clean lokalnie i na PROD (ea-php84). Smoke 12/12 PASS na PROD: anti-enumeration (TC1 zły email + TC2 zły hasło → identyczne 401 "Nieprawidlowy login lub haslo"); whoami/conversations bez cookie → 401; rate-limit po 10. próbie → 429; LOGIN realne konto k.susicki@divezone.pl (id=5 admin) → 200 {ok:true,role:admin} + cookie dz_madmin HttpOnly+Path=/m+Secure+SameSite=Lax+Expires=12h; whoami z cookie → {employee_id:5,role:admin}; conversations list → 351 rozmów + first_message; detail #352 → pełna rozmowa+messages+conversation_cost; status new→reviewed→new (admin_notes zapisane, weryfikacja kolejnym GET); logout → 200, cookie cleared, sesja usunięta z PG (SELECT FROM divechat_mobile_sessions zwraca 0 rows); whoami po logout → 401. Istniejący panel PS /api/conversations* bez zmian (ConversationsController NIETKNIĘTY, /api/health 200). Granice: ZERO zapisu do MySQL PS, ZERO duplikacji SQL rozmów (reuse store), cookie_key NIGDY w logach/API, sekret serwerowy nie wychodzi do przeglądarki. T-072 (front mobilny) i T-073 (PWA) NIE rozpoczęte — STOP po T-071 zgodnie z brifem. Commit kodu + commit docs status osobno.)
# Wersja: 3.42 | Data: 2026-06-04 (CHAT-T-070 BACKEND DEPLOYED — HOTFIX halucynacji daty otwarcia. ADR-085. Root cause #55931 (sesja d105947…): model defaultowal do knowledge cutoffu (~styczeń 2025) gdy mial sam ustalic "dzis" i wysylal `get_shop_schedule(date="2025-01-25")` jako "jutro" → narzedzie liczylo poprawnie wzgledem 2025, bot oddawal "poniedzialek 27 stycznia" klientowi. Fix przesuwa caly rachunek dat do deterministycznego backendu (Europe/Warsaw): model wskazuje INTENCJE przez nowy enum `relative`, nie liczy zadnej daty sam. **GetShopSchedule.php**: dodany parametr `relative` (enum: today/tomorrow/day_after_tomorrow/this_*/next_* dla mon..sun), priorytet relative > date (gdy oba — date ignorowany). Translacja relative→DateTimeImmutable w prywatnej resolveRelative() z mapy DAY_OF_WEEK_ISO (pon=1..niedz=7, NIE locale): today/tomorrow/day_after_tomorrow trywialne; this_<day> deltaThis=(target-todayDow+7)%7 (dzis wlacznie); next_<day> = deltaThis==0 ? 7 : deltaThis+7 (zawsze PRZYSZLY tydzien). Walidacja `date` ISO (204c): rok<bieżący → error "Podana data jest w przeszłym roku (X). Dla dat wzglednych uzyj parametru relative; dla konkretnej daty podaj rok biezacy lub przyszly." NIE clampujemy — przelom roku bywa uprawniony. Pole `server_today` w KAZDEJ odpowiedzi (sukces I error) — jawna kotwica w tool-result. **SystemPrompt.php**: sygnatura `build(bool $emojiEnabled=true, ?DateTimeImmutable $now=null)` (default Europe/Warsaw), stała mapa WEEKDAY_PL (1..7). Blok na początku promptu po pierwszym zdaniu: "AKTUALNA DATA: <dzien_tyg_pl> YYYY-MM-DD (Europe/Warsaw). Jutro: <dzien_tyg_pl> YYYY-MM-DD." + 5 regul (KRYTYCZNE — ADR-085): (1) "dzis/jutro/pojutrze" + stan otwarcia → relative=today/tomorrow/day_after_tomorrow; (2) "w <dzien>" / "następny <dzien>" → this_/next_; gdy goła nazwa dnia + dzis JEST tym dniem — DOPYTAJ, nie zgaduj; (3) konkretna data ("15 lipca", "6 czerwca") → rok z kotwicy + parametr date; (4) NIGDY dzien tygodnia/status otwarcia bez get_shop_schedule, odpowiadasz WYLACZNIE z wyniku (server_today potwierdza dzis); (5) **CYTOWANIE DATY** (dodane po pierwszym smoke): odczytaj DD i MM WPROST z pola `date` (YYYY-MM-DD), NIE przeliczaj, "5 czerwca" dla 2026-06-05 — pomylka cyfry dnia = blad klientowski. Few-shot "Pracujecie jutro?" przepisany z `date="YYYY-MM-DD" gdzie YYYY-MM-DD = jutrzejsza data` (to bylo source rootu — model liczyl sam) na `relative="tomorrow"`. Nowy few-shot dwuznacznosci: gdy dzis piątek + klient "Pracujecie w piatek?" → bot dopytuje "dzis czy za tydzien?" PRZED wolaniem. ChatService.php:71 NIETKNIETY (default $now wystarcza, sygnatura wstecznie kompatybilna). ShopCalendar.php NIE tkniety (rdzen swiat/weekendow/override poprawny — dowod #55811 dzialal). Verify lokalne 10/10 OK (PHP skrypt z kalendarzem bez DB): kryteria 1-7 (relative arytmetyka + walidacja roku + priorytety + regresja "brak parametrow" → today) + 8-9 (anchor + few-shot tekst w prompcie) + sanity (nieznane relative → error+server_today). php -l clean lokalnie i na PROD (ea-php84). **Smoke PROD #55931**: pierwszy run — bot wywolal relative="tomorrow", narzedzie zwrocilo date=2026-06-05+server_today=2026-06-04 (rok 2026 poprawny — halucynacja cutoffu wyeliminowana), bot odpowiedzial "jutro (piątek **6** czerwca)" — rok/miesiac/dzien-tyg/godziny ok, ale pomylil DD (6 zamiast 5). Decyzja: dolozona regula CYTOWANIE DATY w SystemPrompt + redeploy SystemPrompt.php. Drugi run smoke: **"Tak, jutro (5 czerwca) sklep jest otwarty 9:00–17:00"** — DD odczytane wprost z pola `date`, tool call: get_shop_schedule(relative="tomorrow"), zero halucynacji. Kryterium 11 spelnione. **Deploy**: scp 2 plikow (GetShopSchedule.php, SystemPrompt.php) na chat.divezone.pl po pierwszej iteracji + redeploy SystemPrompt.php po dolozeniu reguly cytowania. Autoload niezmieniany (bez nowych klas/namespace). Granice: ZERO modulu PS, ZERO MysqlConnection/ConversationStore, ZERO ChatService logiki — wylacznie 2 pliki backend. **Otwarty osobno**: ewentualny tuning prompt dla scenariuszy "w piatek" gdy dzis NIE jest piatkiem (powinno isc this_friday bez dopytywania) — jesli pojawi sie regresja w logach, addressing osobnym taskiem. Commit kodu + commit docs status osobno (line spec)).
# Wersja: 3.41 | Data: 2026-06-03 (CHAT-T-063 DEPLOYED — prompt patches po ewaluacji 03.06.2026 (5 ZMIAN w `standalone/src/Chat/SystemPrompt.php`, zero zmian w narzedziach/danych — to byl T-062). Decyzje: 151a (zakaz "cena na pewno aktualna" + disclaimer), 153 doprecyzowane (antifog/plyn do neoprenu = produkt, NIE poluzowanie zakazu procedur), C5/D4 (uzyj podanego budzetu), 155a+c (sort po cenie z zawezeniem zakresu/price_floor). **ZMIANA 1 (E5, sekcja DOSTEPNOSC/CENY ~517)**: linia "Dane o cenach AKTUALNE" oslabiona na "swiezy snapshot na moment wywolania narzedzia"; dodana sekcja "CENY — UCZCIWA NIEPEWNOSC" z 4 regulami: (1) NIGDY "na pewno/zawsze aktualna/gwarantowana", (2) przy podaniu cen jeden raz disclaimer "Aktualna cene potwierdz na karcie produktu" (naturalnie, NIE przy kazdym produkcie), (3) na "czy cena aktualna?" uczciwie ze snapshotu bez absolutnej pewnosci, (4) bug do uniknięcia "cena na pewno aktualna" potem rozjazd 2940→2600. **ZMIANA 2 (sekcja 5 ewaluacji)**: nowa sekcja "FORMAT ODPOWIEDZI PRODUKTOWEJ" po PRZYKLADACH PLANOWANIA — 5-elementowa wytyczna (NIE sztywny szablon): (1) krotka odp 1-2 zdania, (2) rekomendacja jeden wiodacy + alternatywy, (3) 3-5 produktow z linkiem/cena/availability, (4) disclaimer ceny raz, (5) CTA naturalnie z 4 opcji. Edukacyjne (A1/A2/B-series ocenione 5) PRO-AKTYWNIE WYLACZONE z wymogu — formuluja sie swobodnie, produkty opcjonalnie na koncu jako CTA. **ZMIANA 3 (C5/D4)**: nowa sekcja "UZYJ PODANEGO PARAMETRU — NIE PYTAJ PONOWNIE" przed PORADAMI PREZENTOWYMI (ogolna zasada: budzet/rozmiar/zastosowanie podany → wykorzystaj, pytaj tylko o brakujace). PORADY PREZENTOWE: dodany KROK 0 "sprawdz czy budzet w pytaniu — pomin 1, idz do 2"; krok 2 zmieniony na "MAJĄC BUDZET (podany lub po dopytaniu)"; voucher zaznaczony jako DODATEK nie ZASTAPIENIE. **ZMIANA 4 (D1, antifog/plyn do neoprenu)**: dopisek przy guardrailu ZERO PROCEDUR ~260 — "WYJATEK PRODUKTOWY (nie procedurowy)" z 2 przykladami (paruje maska→antifog, pianka pachnie→plyn do neoprenu) + analogiczne, oraz "GRANICA KRYTYCZNA — produkt vs procedura": wskazanie produktu z linkiem OK (to dobor sprzetu), procedura uzycia/prania/dawkowania NADAL zakazana (odsylanie do instrukcji producenta), META-REGULA POD PRESJA zostaje, test sam dla siebie "CO kupic" (OK) vs "JAK uzywac" (zakaz). Guardrail SAM W SOBIE niezlamany — golden DOMAIN-006 (pralka), SCOPE-002 (rashguard), pranie sucnego skafandra **NADAL BLOKOWANE** (test grep w PROD: 4 wystapienia ZERO PROCEDUR/DOMAIN-006/SCOPE-002/pralka/rashguard zachowane). **ZMIANA 5 (E4 UX, 155a+c)**: nowa sekcja "SORTOWANIE PO CENIE" w JAK SZUKAC PRODUKTOW — wytyczne kiedy sort=price_asc/price_desc (slowa "najtanszy/tani" → asc, "najdrozszy/premium" → desc); KRYTYCZNE OSTRZEZENIE ze kategorie parent miesza sprzet z akcesoriami (Komputery Nurkowe ma baterie/kompasy/manometry) — sort=price_asc bez zawezenia zwroci akcesorium 20-33 zl zamiast komputera 2000+ zl (krytyczny blad E4); 4 ZASADY OBOWIAZKOWE: zawez kategorie (child > parent), uzyj price_min odcinajacy drobnice (komputer >800, automat >800, maska >50, pianka >200), zweryfikuj wynik (jak akcesorium → popraw filtry), NIE prezentuj blednej rekomendacji — lepiej powtorzyc wyszukanie. Dodany przyklad planowania w PRZYKLADY: "Najtanszy komputer nurkowy" → sort=price_asc + price_min=800 + reasoning ze odcinam akcesoria. INT/yoke nieobecne w nowych dopiskach — istniejace reguly (linia 635/639 "INT to martwy standard, NIE proponuj") NIETKNIETE. php -l clean lokalnie i na PROD (ea-php84). md5 lokalny == PROD (f66a856233865ec710637c12051b490f). Deploy: scp 1 plik (autoload nieregenerowany — bez nowych klas). +65/-6 linii w 1 pliku. Commit c9b286f)
# Wersja: 3.40 | Data: 2026-06-03 (CHAT-T-062 DEPLOYED — ceny w czacie: sortowanie po cenie (E4) + jedno zrodlo ceny produktu (E5). Z ewaluacji 03.06.2026: E4 "najtanszy komputer" wskazal 2940 zl mimo ze byly 2276 i 2674; E5 cena tego samego produktu 2940→2600 miedzy turami. Decyzje 150a (sortowanie po cenie przez parametr ProductSearch), 151b (diagnoza zmiany ceny), 154a (E4 zaczyna od diagnozy enrichment vs limit). **DIAGNOZA E4 (KROK 1)**: w `mergeRRF()` 5 torow (3 semantic + FTS + trigram) kazdy daje top-30 (TRACK_LIMIT), po RRF i boostach `$candidateLimit = min(count, limit*3)` = 15-30 → `array_slice` → enrichment dokladany dla TOP-N tej puli (15-30 z mozliwych 188-341 produktow w kategorii). Sortowanie po cenie tej puli NIE znajdzie najtanszego z calej kategorii bo semantyka nie koreluje z cena. **WYBOR WARIANT B (lekki)**: nowa metoda `searchByPrice()` ~130 linii — fetch szerokiej puli (LIMIT 1000 bezpiecznik; max sensowne kategorie: Komputery 188, Maski 222, Automaty 341) z `divechat_product_embeddings` przez `buildFilters` (active + category + price_min/max + brand + exclude + blacklist — strukturalne, NIE semantyczne), enrich w jednym batch SQL (IN(...)), filtr active/visible/in_stock_only, sortowanie po `mysqlData[id]['price']` ASC/DESC, top $limit. Reusuje `buildFilters` + `enrichmentService` — zero nowego silnika. Nie "duza zmiana" (130 linii, jedna sciezka kompetytywna do RRF). Schemat: nowy parametr `sort` (enum `relevance`/`price_asc`/`price_desc`, default `relevance`) z opisem dla modelu. **E4 test PROD**: sort=price_asc + kategoria "Komputery Nurkowe" + in_stock_only=true → 5 pozycji od najtanszej, pool_size=188 eligible=156 (NIE top-30); sort=price_desc → 9248 zl Garmin Mk3i na pozycji 1 (poprawnie najdrozszy). **UWAGA UX**: pula kategorii "Komputery Nurkowe" parent obejmuje tez akcesoria (baterie, kompasy, manometry, oslony) — sortowanie zwraca akcesoria 20-33 zl jako "najtansze komputery". Tool dziala poprawnie (sortuje cala kategorie); UX wymaga nauki modelu w prompcie zeby podawal sort + sensowne filtry (`price_min` lub konkretna child-kategoria zamiast parent) — to **CHAT-T-063 (prompt task)**, poza zakresem T-062. Sam RRF bez sort=price_asc zwraca SUUNTO NAUTIC S 2276 zl na #1 (semantyka tu pomaga — slowo "komputer" w nazwie produktu boost) — czyli pierwotny problem ewaluacji prawdopodobnie byl gdzie indziej (model nie zawsze wybral pierwszy z listy). **E5 fix**: `ProductDetails` mial wlasna sciezke ceny — `pr_product.price` netto + `pr_specific_price` tylko jako oddzielne pole, BEZ liczenia brutto z VAT, BEZ aplikacji promocji do `price`. Vs `MysqlProductEnrichmentService` ktory liczy brutto + VAT (pr_tax_rule + pr_tax id_country=14 PL) + aplikuje specific_price (percentage/amount, hierarchia priorytetow id_shop>0/id_group>0). Stad rozjazd 2940 vs 2600. Fix: ProductDetails DI rozszerzony o `MysqlProductEnrichmentService $enrichment` (przez tools.php), w `execute()` po fetch produktu wola `$this->enrichment->enrich([$productId])` i z niego bierze `price` (brutto z promocja), `availability` (3-stanowy spojny z ProductSearch: in_stock/available_to_order/unavailable) oraz `price_before_discount` (gdy promocja). Lokalny SQL `$specialPrice` zostaje dla metadanych `special_price.reduction/type` (model widzi dodatkowo). Fallback gdy enrichment padl (timeout MySQL): netto * 1.23 (przyblizenie), availability z lokalnego stock. **E5 test PROD 4/4 PASS**: produkt 7515 SUUNTO NAUTIC S → enrichment 2276 zl (price_before_discount 2399) == ProductDetails 2276 (2399); 7517 SUUNTO Nautic → 2940.7 (3199) == 2940.7 (3199); 5706 SUUNTO D5 → 1204.41 (2106) == 1204.41 (2106); 25 APEKS XTX50 → 1105.1 (1255.8) == 1105.1 (1255.8). Pelna spojnosc dla produktow z promocja i bez. Granice T-062 respektowane: MysqlProductEnrichmentService nietkniety (git diff pusty), zero zmian w promptach (CHAT-T-063 osobny), zero zmian w innych narzedziach. php -l clean (3 pliki) lokalnie i na PROD (ea-php84). Deploy: scp 3 plikow (autoload niezmieniany — bez nowych klas). Commit 89f8135)
# Wersja: 3.39 | Data: 2026-06-03 (CHAT-T-059 BACKEND DEPLOYED, MODUL CZEKA NA RECZNY DEPLOY KAROLA. Persystencja sesji czatu miedzy stronami sklepu — rozwiazany glowny problem PROD (nawigacja resetowala czat, stan tylko w pamieci JS). Decyzje: 145a (sessionId w localStorage + historia odtwarzana z nowego endpointu backendu — backend = source of truth, NIE trzymamy tresci rozmowy w przegladarce), 146b/147a (TTL + przycisk "Nowa rozmowa"), TTL 30 dni KONFIGUROWALNY (1-365). DWA OBSZARY DEPLOY. ## BACKEND (CC WDROZYL): nowy endpoint **GET /api/chat/history?sid={id}** w `ChatController::history` — auth HMAC 1:1 z handle()/stream() (X-DiveChat-Token/Customer/Time), weryfikacja wlasciciela (`ps_customer_id` rozmowy == `customerId` z HMAC; niedopasowanie → {exists:false} jak nieistniejaca, NIE rozrozniamy zeby nie ulatwiac enumeracji sessionId). Lekka metoda `ConversationStore::findActiveBySessionId()` — odczyt `{id, ps_customer_id, messages}` aktywnej rozmowy (closed_at IS NULL), wzorzec query 1:1 z startOrResume, NIE tworzy rozmowy. ChatController DI rozszerzony o ConversationStore (`new ChatController($chatService, new ConversationStore())` w routes.php). **UWAGA WAF**: hostingowy LiteSpeed/ModSecurity blokuje query stringi z `session_id=` (regula PHPSESSID-like, 403 zanim PHP zobaczy request) — wykryte podczas testow, naprawione zmiana nazwy parametru na `sid` (krotszy, jednoznaczny, nie triggeruje WAF; body POST /api/chat dalej `session_id`, body nie podlega tej regule). Deploy: scp 3 plikow + composer dump-autoload. **Test bezpieczenstwa 8/8 PASS na PROD**: (1) bez naglowkow → 401, (2) bez sid → 400, (3) nieistniejaca → 200 {exists:false}, (4) guest własna sesja → 200 {exists:true, messages:[...]}, (5) **guest pyta o sesje usera 55899 → 200 {exists:false}** (NIE ujawnia cudzej), (6) user 55899 wlasna sesja → 200 exists:true, (7) **user 999 pyta o sesje usera 55899 → 200 {exists:false}** (kryterium #7 spec), (8) **user 55899 pyta o guest sesje → 200 {exists:false}** (cross-user). Commity backend: 5c0372b (endpoint+routes), def3f11 (WAF tweak sid). ## MODUL PS (KAROL WGRYWA RECZNIE 116b): nowe pole `KEY_PERSIST_TTL_DAYS` (DIVEZONE_CHAT_PERSIST_TTL_DAYS, default 30) — sekcja 6 "Persystencja sesji czatu" w renderConfigForm (number 1-365 z walidacja → default), handleConfigSubmit zapisuje. hookDisplayFooter rozszerzony o `'persist' => array('ttl_days', 'historyPath' => '/api/chat/history')` w `$boot`. **Widget**: transport.js: nowa `fetchHistory(sessionId, callbacks)` (GET z naglowkami HMAC, query `?sid=`, gracefully na 401/5xx zwraca onError → swiezy czat), KOMENTARZE "5 min TTL" zaktualizowane na "1 h (CHAT-T-057)". widget-bundle.js: `persistSession(sid)` po onDone (localStorage `dz_chat_session` = {sessionId, ts}), `loadPersistedSessionId()` przy mount (auto-czyszczenie wygaslych/skorumpowanych), `tryRestoreSession()` po mount → fetchHistory → renderHistoryMessages (renderuje user/assistant; tool/system pomijamy), `startNewConversation()` (czysci state.sessionId + localStorage + bable, zachowuje welcome+chipy), **przycisk "Nowa" w header** (.dz-newconv aria-label, margin-left:auto), padl/cudza → cichy fallback. **C4 (3d) WYBRANE: TAK — zapamietywanie stanu otwarcia per-tab** (sessionStorage `dz_chat_open` — set w openWindow, remove w closeWindow, przy mount jesli =='1' → auto-openWindow); 3 linie, ciaglosc UX gdy klika kategorie z czatem otwartym, zero kosztu kiedy zamkniety. CSS: `.dz-newconv` (height 30px padding 0 10px, spojny z dz-x ale szerszy dla tekstu, margin-left:auto pcha w prawo). Bezpieczne lsGet/lsSet/ssGet/ssSet wrappery (try/catch dla Safari ITP + Firefox prywatne). php -l clean (modul), node --check clean (transport + widget-bundle). +318/-6 linii w 4 plikach modulu. **Komenda rsync dla Karola**: `rsync -avz -e "ssh -p 5739 -i ~/.ssh/id_ed25519" /Users/karol/Documents/3_DIVEZONE/Aplikacje/Chat_dla_klientow_2026/modules/divezone_chat/ divezone@divezonededyk.smarthost.pl:~/public_html/newtmp2/modules/divezone_chat/ --exclude config_pl.xml` (bez --delete). Po wgraniu: wyczyscic cache PS + twarde odswiezenie przegladarki (JS bundle podlega cache jak CHAT-T-053). Commit modul: 2bca0f7)
# Wersja: 3.38 | Data: 2026-06-03 (CHAT-T-057 DEPLOYED — PILNY FIX 401 PROD. Czat na PROD zwracal 401 "Nieprawidlowy token" gdy strona otwarta >5 min przed pytaniem; przyczyna: token klienta generowany raz przy renderze (`hookDisplayFooter`), `transport.js` go NIE odswiezal (skrot etapu 1), `HmacVerifier::maxAgeSec` default=300s. Szybki fix (decyzja 138c/139a): TTL 5 min → 1 h. JEDNA zmiana w `standalone/src/Auth/HmacVerifier.php` — default konstruktora `maxAgeSec` z `300` na `3600` (komentarz `// 1 h (CHAT-T-057; bylo 5 min — za krotkie dla realnych sesji; odswiezanie tokenu w ADR-064)`). Default a nie hardcode w kontrolerach — 3 konsumentow (`ChatController::handle` linia 44, `ChatController::stream` linia 105, `OrderStatusController::handle` linia 56) tworzy `new HmacVerifier($secret)` bez 2. argumentu, dziedzicza ten sam default — jedno miejsce, zero rozjazdu. Kontrolery NIETKNIETE (git diff pusty dla ChatController.php + OrderStatusController.php). `verify()` bez zmian (logika sprawdzania wieku + hash_equals). Deploy: scp 1 plik (autoload nie regenerowany — tylko zmiana wartosci w istniejacym pliku, mapping PSR-4 nietkniety). `php -l` clean lokalnie i na PROD (ea-php84). Testy logiczne lokalne 4/4 PASS: ts=-600s OK, ts=-3540s (59min) OK, ts=-3660s (61min) ODRZUCONY, ts=-7200s (2h) ODRZUCONY. Testy E2E PROD 2/2 PASS: (A) `POST /api/chat` z ts=now-600 + podpis dobry + body `{message:""}` → `HTTP 400 "Pole message jest wymagane"` (HMAC PRZESZEDL, walidacja body zadziala — przed fix dawal 401 "Nieprawidlowy token"); (B) `POST /api/chat` z ts=now-3700 (>1h) + podpis dobry → `HTTP 401 "Nieprawidlowy token"` (TTL nadal egzekwowany — granica 1h dziala). Nota: komentarze w `transport.js` mowiace o "5 min TTL" do aktualizacji przy najblizszym dotknieciu frontu. Pelne odswiezanie tokenu (transport pobiera fresh token przed wyslaniem) = osobny temat **ADR-064**, poza zakresem tego fixa. Commit 82ef914)
# Wersja: 3.37 | Data: 2026-06-03 (CHAT-T-056: proaktywny nudge przy launcherze widgetu — ZAIMPLEMENTOWANY, CZEKA NA RECZNY DEPLOY KAROLA. Cel: maly launcher → dodanie wysuwanego dymka po N sekundach z zacheta do rozmowy (zwiekszenie otwieralnosci czatu). 2 pliki: modul PHP (3 pola configu + boot payload) + widget-loader.js (DOM dymka + sessionStorage). Decyzje: 133b (caly dymek klikalny + przycisk; X zamyka stopPropagation), 134a (sessionStorage raz na sesje — dz_nudge_dismissed / dz_chat_opened), 135a (3 pola w configu), 136a (prosty nudge bez A/B/tracking/raport — to osobny przyszly projekt). PHP (`divezone_chat.php`): 3 nowe stale KEY_NUDGE_ENABLED/DELAY/TEXT + DEFAULT_NUDGE_DELAY=20 + DEFAULT_NUDGE_TEXT "Hej! 🤿 Nie wiesz, jaki sprzet wybrac? Zapytaj naszych specjalistow." (lazy init bez bumpa wersji jak CHAT-T-047, klucze nieobecne = default), nowa sekcja 5 w renderConfigForm "Proaktywny dymek (nudge)" po Filtrach (checkbox enabled default OFF + number delay 3-300 default 20 + textarea text), handleConfigSubmit zapisuje 3 pola z walidacja delay 3-300 → default jak poza zakresem + pusty text → DEFAULT_NUDGE_TEXT (UX: czyszczenie pola = reset). hookDisplayFooter rozszerza $boot o `'nudge' => array('enabled'=>bool,'delay'=>int,'text'=>string)`. JS (`views/js/widget-loader.js`): nowy blok logiki nudge w istniejacym IIFE (po launcher click handler) — setupNudge() sprawdza BOOT.nudge.enabled + sessionStorage guards (dz_nudge_dismissed/dz_chat_opened), setTimeout(delay*1000), recheck guards w callback, renderNudge() tworzy DOM (role=dialog, aria-label, container .dz-nudge z `<p>` text + `<button>` CTA + `<button>` close) — KLUCZOWE: `textEl.textContent = text` zamiast innerHTML (ESCAPE anty-XSS dla configu z panelu PS). Listenery: klik kontener/CTA (oprocz close) → openChatFlow() (ssSet dz_chat_opened + hideNudge + bootBundle + DivezoneChatOpen — TA SAMA sciezka co klik launcher). Klik close (stopPropagation) → ssSet dz_nudge_dismissed + hideNudge (NIE otwiera czatu). launcher.click rozszerzony o ssSet dz_chat_opened + hideNudge (rownolegle gdyby user kliknal launcher w trakcie wyswietlania nudge). CSS w baseStyle (Shadow DOM): .dz-nudge (fixed right:20px bottom:88px nad launcherem 56+20+12, width:280px max-width:calc(100vw - 40px), bialy, box-shadow, border-radius:12px, pointer-events:auto, cursor:pointer, animation:dzNudgeIn .3s ease), .dz-nudge__text (margin:0 0 10px word-break: break-word white-space:pre-line), .dz-nudge__cta (TEAL #1e6363 / hover TEAL_DARK #155050, block 100%, border-radius:6px), .dz-nudge__close (24x24 absolute top:6 right:6 transparent hover #f0f0f0, focus-visible outline TEAL), @keyframes dzNudgeIn (translateY 8px + opacity), prefers-reduced-motion: dz-nudge animation: none, max-width:599.98px: bottom:84px right:12px width:240px (mobile compact nad launcherem). Pre-fetch idle bundla (linia ~278) NIETKNIETY — bundle pobiera sie idle jak dotad, nudge sam NIE inicjuje pobrania (kryterium 7). Nudge dziedziczy gating launchera — shouldShowWidget() przepuscil = launcher widoczny = nudge moze sie pokazac (zero wlasnej geo/IP logiki). php -l clean, node --check clean. +209/0 linii w 2 plikach. Modul PS NIE wgrany na PROD — 116b: Karol wgrywa recznie + wyczysci cache PS + UWAGA o cache JS przegladarki (loader podlega cache jak widget-bundle z CHAT-T-053). Commit 1043405)
# Wersja: 3.36 | Data: 2026-06-03 (CHAT-T-055: zakladka Editorial Picks w panelu PS — ZAIMPLEMENTOWANA, CZEKA NA RECZNY DEPLOY KAROLA. **PANEL PS MA KOMPLET ZAKLADEK** — etap 3 (ostatni) migracji panelu zakonczony, /admin gotowy do wylaczenia (zostaje tylko `conversations/{id}` z Basic Auth 109a do usuniecia razem). Architektura 132a: PELNY CRUD server-side, ZERO JS poza inline `confirm()` przy usuwaniu, ZERO proxy/live-fetch — wyszukiwarka produktow przez reload zakladki z `?q=...`, klik "Wybierz" w wyniku robi link `?ep_product_id=...&ep_product_name=...&tab=editorial` ktory prefilluje formularz dodawania. Sekcje: (a) Pasek pending reviews (z `/api/admin/editorial-picks/pending-reviews`) — `expired_this_week / long_unreviewed / total`, gdy total=0 dyskretny komunikat "Brak pickow do przegladu"; (b) Sekcja "Dodaj nowy pick" — wyszukiwarka (GET form, hidden controller/token/tab) input `q` (min. 2 znaki) + "Szukaj produkt" -> reload kontrolera, woła `products/search?q=...`, lista wynikow jako klikalne pozycje (nazwa/cena/badge "w magazynie"/"brak"), kazdy = link "Wybierz →" z parametrami; formularz add (POST submitDivezoneChatEpAdd, hidden controller/token/tab, ZALOZONA tab=editorial w action) — hidden ep_product_id+ep_product_name z URL, pola: reason (textarea required), boost_factor (number 1.0-2.5 step 0.1, dflt 1.5), category_hint (text opc.), ttl_days (number opc. — puste = bezterminowo); placeholder gdy brak wybranego produktu "Najpierw wyszukaj i kliknij Wybierz"; (c) Filtry GET (`active` wszystkie/1/0 + `order_by` whitelist 5: added_at/expires_at/boost_factor/product_name/last_review_at, dflt added_at) + tabela 9 kolumn (Produkt+#id, Boost, Powod skrocony ~80zn z title=full, Kategoria lub "wszystkie", Dodane data + added_by, Wygasa lub "bezterminowo", Ost. przeglad lub "—", Status badge aktywny/nieaktywny, Akcje); (d) Akcje per wiersz: `<details>` "Edytuj" z mini-formularzem (boost/reason/active/ttl_extend 30/90/365 dni; submitDivezoneChatEpUpdate, alias POST /editorial-picks/{id}), quick action "Oznacz przejrzane" (submitDivezoneChatEpUpdate + ep_action=mark_reviewed -> {mark_reviewed:true}), "Usun" z inline `onsubmit=confirm()` (submitDivezoneChatEpDelete, alias POST /{id}/delete z body `{}`). Handlery: handleEpAdd/handleEpUpdate/handleEpDelete z walidacja lokalna PRZED POST (product_id>0+name, reason!='', boost 1.0-2.5) — odrzucenie z flashem error zanim leci request; setEpFlashFromResponse mapuje 401/403/400/404 na czytelne komunikaty PL. Property $epFlash/$epFlashType wzorem $modelsFlash/$convFlash. Aliasy POST (128a, NIE PUT/DELETE): callBackend w module wysyla body tylko dla `method=POST`, wiec backend wystawia `/editorial-picks/{id}` i `/editorial-picks/{id}/delete` jako POST. Hidden controller/token/tab we WSZYSTKICH formularzach (GET wyszukiwarka, GET filtry, POST add, POST update, POST mark_reviewed, POST delete). Nav (118a): Rozmowy | Rekomendacje | [Analityka jesli admin] | Editorial | Modele | Konfiguracja (Editorial widoczne dla operator I admin — any-role 127b, NIE ukrywane wg roli inaczej niz Analityka). 403 guard tak samo jak CHAT-T-050 (anyResponseIs403 helper). CSS dz-ep-* spojne z dz-* (pending pasek, wyszukiwarka inline, wyniki flex, formularze, tabela, akcje wiersza). Wzorzec sekcji z CHAT-T-050 (4 wywolan + 403 guard), wzorzec handler z handleConvStatusSave (whitelist + walidacja + flash). PHP 7.2/PS 1.7.6 compatible (brak typed props, brak match). php -l clean. +590/-3 linii. Modul PS NIE wgrany na PROD — 116b: Karol wgrywa recznie + wyczysci cache PS. Commit 9c831ba)
# Wersja: 3.35 | Data: 2026-06-03 (CHAT-T-054 DEPLOYED — Etap 3 migracji panelu, BACKEND GOTOWY. Editorial Picks (6 endpointow: list/add/update/delete/pendingReviews/productsSearch) przepiete z Basic Auth (AdminAuthMiddleware) na kanal serwerowy panelu PS z rola ANY-ROLE (operator+admin), wdrozone na chat.divezone.pl. Decyzje 127b (any-role: editorial w workflow operatora, nie tylko admin) + 128a (callBackend w module PS wysyla body TYLKO dla method=POST -> aliasy POST update/delete). AdminEditorialPicksController: DI zmieniona (`EditorialPicksService`+`ServerHmacVerifier`+`PostgresConnection`, AdminAuthMiddleware usuniete z konstruktora i z `use`), nowa prywatna `requireAnyRole(): int` (zwraca employee_id z weryfikowanego HMAC — wzorzec 1:1 z AdminRecommendationsController, BEZ wymogu role==='admin'), wszystkie 6 metod uzywa `$this->requireAnyRole()` zamiast `$this->auth->check()`. W `add()`: `$addedBy` pochodzi z `'employee:' . $employeeId` (zamiast wycofanego PHP_AUTH_USER z Basic Auth) — audit trail jednoznaczny (id z podpisu HMAC). Logika serwisu (EditorialPicksService) bez zmian (git diff pusty). routes.php: nowa instancja `new AdminEditorialPicksController($editorialPicksService, $serverVerifier, $db)`, 2 nowe trasy POST aliasy obok PUT/DELETE (zgodnosc wsteczna): `POST /api/admin/editorial-picks/{id}/delete` (bardziej specyficzna PRZED `{id}`) + `POST /api/admin/editorial-picks/{id}` (update). Kolejnosc statyczne (pending-reviews, products/search) PRZED `{id}` zachowana. AdminAuthMiddleware nadal w routes.php — uzywany TYLKO przez `$adminController->conversationDetail()` (conversations/{id}, decyzja 109a; po wylaczeniu /admin podpada do usuniecia). Deploy: scp 2 plikow + `composer.phar dump-autoload --no-dev` (ea-php84) — `php -l` clean lokalnie i na PROD. Smoke produkcyjny 6/6 PASS: (1) `GET /api/admin/editorial-picks` bez naglowkow → 401 Unauthorized, (2) operator id=14 z HMAC → 200 + JSON picks, (3) employee id=999 bez roli → 403 {"reason":"no_role"}, (4) `POST /api/admin/editorial-picks/10` body `{mark_reviewed:true}` operator → 200 {"success":true,"id":10} (alias POST update dziala, idempotentne mark_reviewed bez efektow ubocznych), (5) POST update bez naglowkow → 401 (trasa POST /{id} istnieje, chroniona), (6) `POST /api/admin/editorial-picks/999/delete` bez naglowkow → 401 (potwierdza ze bardziej specyficzna /{id}/delete dopasowuje sie PRZED /{id}, chroniona). Stary panel `/admin` (admin-editorial.js, admin.js) na editorial zacznie dostawac 401 — OCZEKIWANE (analogicznie do CHAT-T-046 conversations i CHAT-T-049 koszty). Po CHAT-T-054 w `/admin` zywy zostaje TYLKO `conversations/{id}` (Basic Auth, 109a) — `/admin` gotowy do wylaczenia po CHAT-T-055 (UI Editorial w PS). Commit 2524d7d)
# Wersja: 3.34 | Data: 2026-06-03 (CHAT-T-053: fix widgetu czatu — ZAIMPLEMENTOWANY, CZEKA NA RECZNY DEPLOY KAROLA. Dwa drobne fixy, wylacznie modules/divezone_chat/views/js/widget-bundle.js (bez backendu, bez panelu admin). Fix 1 (124a, mobile autofocus): w openWindow() owinieto setTimeout(focus, 180) warunkiem `if (!isMobile)` — `isMobile` juz istnial w scope funkcji (matchMedia max-width:599.98px, linia 770). Efekt: mobile (<=599.98px) po otwarciu panelu NIE otwiera klawiatury ekranowej (zaslaniala chipy powitalne), klawiatura otwiera sie dopiero gdy user sam tapnie pole tekstowe. Desktop bez zmian (focus po 180ms jak wczesniej). Linia ~407 (focus po odpowiedzi bota — rozmowa juz trwa, klawiatura oczekiwana) NIETKNIETA — poza zakresem. Fix 2 (link polityki): linia 41 — href https://divezone.pl/content/3-polityka-prywatnosci -> https://divezone.pl/polityka-prywatnosci (zweryfikowany, zywy URL po removal old PS content/ID slug), target=_blank rel="noopener noreferrer" ZACHOWANE. node --check clean. +9/-5 linii. Modul PS NIE wgrany na PROD — 116b: Karol wgrywa recznie + wyczysci cache PS. UWAGA o cache: zmiany w JS bundle — po wgraniu wymusic odswiezenie (cache przegladarki, ew. wersjonowanie assetu). Commit d6f57ab)
# Wersja: 3.33 | Data: 2026-06-03 (CHAT-T-050: zakladka Analityka w panelu PS — ZAIMPLEMENTOWANA, CZEKA NA RECZNY DEPLOY KAROLA. Etap 2 UI migracji panelu (handoff _docs/25). Architektura 120a: PHP osadza dane server-side przez 4 callBackend (/api/admin/cost/kpi + /trend?period&days + /by-model?days + /admin/conversations/top?limit&days), JS tylko rysuje wykres z osadzonego JSON — ZERO fetch z przegladarki (sekret HMAC zostaje na serwerze). Sekcje: (a) Filtry GET form (days whitelist 7/30/90 dflt 30 + period whitelist daily/weekly/monthly dflt daily, hidden controller/token/tab); (b) 4 karty KPI w grid auto-fit minmax(220px,1fr): Dzis/Tydzien/Miesiac (PLN glowne + USD + rozmowy + wiadomosci) + dedykowana karta "Koszt na rozmowe (miesiac)" z benchmark branzy + vs agent ludzki; (c) Wykres trendu Chart.js v4.4.0 z CDN jsdelivr — odtworzony 1:1 config z admin-charts.js (type line, borderColor #0066cc fill, tooltip PLN+USD+rozm., y-axis PLN, x rotate 45), guard typeof Chart === 'undefined' -> graceful degradation gdy CSP/siec blokuje (info "Chart.js niedostepny — wykres pominiety", reszta sekcji dziala), osadzony JSON w <script type="application/json" id="dz-trend-data"> z escape "<" -> \\u003c (anti </script> injection); (d) Tabela by-model: Model/Provider/Uzycia/Tokeny in+out+cache/Koszt PLN+USD/Sr.koszt-uzycie/Sr.latencja (sort backend); (e) TOP 10 najdrozszych rozmow (109a) — kazdy wiersz CALY w linku do &tab=conversations&session_id=... (NIE osobny widok rozmowy). Rola: kontroler ZNA $whoami['role'] -> link "Analityka" w nav RENDEROWANY TYLKO dla role==='admin' (operator/no_role/blad whoami = brak linka, safe default); niezaleznie: 403 z dowolnego endpointu -> czytelny komunikat "Brak uprawnien. Analityka dostepna tylko dla administratorow" zamiast bialego ekranu (anyResponseIs403 helper). Pasek nav (118a): Rozmowy | Rekomendacje | [Analityka jesli admin] | Modele | Konfiguracja. CSS dz-analytics-* spojne z dz-* (grid kart, wykres-wrapper 320px, tabele compact 12px). Pierwszy JS w kontrolerze admin (dotychczas server-side only) — inline <script> tylko init wykresu, JSON sanityzowany. PHP 7.2/PS 1.7.6 compatible (brak typed props, brak match). php -l clean. +436/-5 linii. Modul PS NIE wgrany na PROD — 116b: Karol wgrywa recznie + wyczysci cache PS. Commit 0c44c6f)
# Wersja: 3.32 | Data: 2026-06-03 (CHAT-T-049 DEPLOYED — Etap 2 migracji panelu, BACKEND GOTOWY. Analityka (cost/kpi, cost/trend, cost/by-model, conversations/top) przepieta z Basic Auth (AdminController) na kanal serwerowy panelu PS (ServerHmacVerifier + role admin-only), wdrozona na chat.divezone.pl. Decyzja 118b: NOWY `AdminAnalyticsController` (PHP 8.4, final, strict_types, namespace DiveChat\Controller; DI: CostAnalytics+ServerHmacVerifier+PostgresConnection; metoda `requireAdmin()` 1:1 z `SettingsController` — HMAC X-DiveChat-Server-Token/-Employee/-Time, filter_var INT, verify, lookup divechat_admin_roles, 401 brak/zly podpis, 403 no_role/admin_only), stary `AdminController.php` NIETKNIETY (git diff pusty, obsluguje tylko `conversations/{id}` — decyzja 109a, ginie z `/admin`). routes.php: dodana instancja `$adminAnalyticsController = new AdminAnalyticsController($costAnalytics, $serverVerifier, $db)`, 4 trasy przepiete (cost/kpi, cost/trend, cost/by-model, conversations/top), kolejnosc top PRZED `{id}` zachowana, conversations/{id} nadal na starym kontrolerze. Logika danych (CostAnalytics) bez zmian — tylko warstwa auth. Deploy: scp 2 plikow + `composer.phar dump-autoload --no-dev` (ea-php84) — `php -l` clean dla obu plikow lokalnie i na PROD. Smoke produkcyjny 3/3 PASS: (1) `GET /api/admin/cost/kpi` bez naglowkow → 401 {"error":"Unauthorized"}, (2) admin id=2 z poprawnym HMAC → 200 + JSON KPI (today/this_week/this_month cost_usd+cost_pln, conversations, messages), (3) operator id=14 z poprawnym HMAC → 403 {"error":"Forbidden","reason":"admin_only"}. Stary panel `/admin` js (admin.js/admin-charts.js/admin-tables.js) na koszty zacznie dostawac 401 — OCZEKIWANE (analogia history.js po CHAT-T-046, `/admin` i tak wygaszamy). UI Analityki w zakladce PS = CHAT-T-050 (osobny task). Commit b43a6c5)
# Wersja: 3.31 | Data: 2026-06-03 (CHAT-T-052: polish layoutu zakladki Rozmowy — ZAIMPLEMENTOWANY, CZEKA NA RECZNY DEPLOY KAROLA. Wylacznie modul PS, bez backendu, bez logiki — 6 poprawek CSS/teksty wg obserwacji z zywego panelu: (1) renderConvListItem: scal 2 divy dz-conv-item-meta w 1 — data lewo + "Klient | Status badge | ⚠" prawo (jedna linia, flex space-between). (2) renderConversationsList: wrapper po panel-heading padding:0 (lista styka sie z krawedziami; pozycje maja wlasny padding 10px 12px), flash/filtry/error/pager dostaja wlasne ramki paddingu (14px) zeby nie wygladaly edge-to-edge. (3) renderConvFilters: usuniete labele "Szukaj"/"Status", placeholder na "Szukaj konwersacji", pierwsza opcja "— Wyswietlane wszystkie —" (UI feel), checkbox skrocony do "Luki wiedzy", wszystko w 1 linii (CSS .dz-conv-list-col .dz-conv-filters nadpisany na flex-direction:row + flex-wrap:wrap + search flex:1 1 110px); hidden controller/token/tab ZACHOWANE (routing PS). (4) renderConversationDetail: nowy wrapper .dz-conv-meta-row {display:flex;gap:14px;flex-wrap:wrap} dla meta + koszty obok siebie (2 kolumny 280px+, wrap na waskim). (5) renderConvStatusForm: 2 kolumny grid 1fr 1fr — LEWA: Status select + przycisk "Zapisz status" pod nim, PRAWA: Notatki textarea rows=2 (nizsza); etykiety "Status"/"Notatki". (6) renderConvCosts: usunieto nadmiarowe " · estimated_cost: $X USD" (potwierdzone w zrodle: getConversationCost czyta dokladnie kolumne estimated_cost — duplikat conversation_cost.total_usd), $estCost var tez usunieta. Bez JS/bibliotek. PHP 7.2/PS 1.7.6 compatible. php -l clean. +57/-32 linii. Modul PS nie wgrany na PROD — 116b: Karol wgrywa recznie + wyczysci cache PS. Przypomnienia 116a: ostrzezenia PS u gory = Wydajnosc->Debug=NIE + display_errors=Off (NIE kod modulu). Etykieta "Wyswietlany" jesli wciaz widoczna = Miedzynarodowy->Tlumaczenia modulow->divezone_chat->PL->"Status" (CHAT-T-051 zalecenie). Commit 61ba1f4)
# Wersja: 3.30 | Data: 2026-06-03 (CHAT-T-051: redesign zakladki Rozmowy — BACKEND WDROZONY PRZEZ CC, MODUL CZEKA NA RECZNY DEPLOY KAROLA. Backend: ConversationStore::list dodaje pole first_message (skorelowane podzapytanie z divechat_messages WHERE role=user ORDER BY created_at LIMIT 1, wzorzec z CostAnalytics::topConversations), zdeploy'owane na chat.divezone.pl (rsync /home/divezone/public_html/chat.divezone.pl/src/Chat/ConversationStore.php, ea-php84 parsuje clean, /api/admin/whoami zwraca 401 — zdrowy). Modul PS: redesign master-detail server-side (113a, BEZ JS) — JEDEN ekran zakladki, lewa kolumna 340px ze stalym scrollem zawiera waska liste rozmow (3 pola: pierwsza wiadomosc skrocona ~80 znakow + ellipsis z fallback "(brak tresci)" 115a + data started_at + "Klient | Status badge"), prawa kolumna elastyczna z wybrana rozmowa (meta+koszty+formularz statusu+przebieg) lub placeholder gdy brak ?session_id. Klik w pozycje listy = pelen reload z nowym session_id, .is-active podswietla biezaca. Filtry/page zachowane w linkach (page/per_page/search/admin_status/knowledge_gap). Formatowanie czatu (Problem B): nowa metoda formatConvBubbleText zastapila nl2br(htmlspecialchars()): NAJPIERW htmlspecialchars (zero surowego HTML z tresci), POTEM **bold** -> <strong>, URL http(s) -> <a target=_blank rel="noopener noreferrer nofollow" word-break:break-all>, na koncu nl2br. Zakres minimalny — bez markdown-parsera, bez obrazkow. Etykieta (Problem C): brak literalu "Wyswietlany" w kodzie modulu (potwierdzone grep), 4x $this->l('Status') — poprawka tlumaczenia PS po stronie Karola: Miedzynarodowy -> Tlumaczenia -> Tlumaczenia modulow -> divezone_chat -> polski, znalezc "Wyswietlany"/"Status" i ustawic poprawne PL. PHP 7.2/PS 1.7.6 compatible (bez typed props, bez match). php -l clean (lokalnie + na serwerze ea-php84). +151/-46 w AdminDivezoneChatController.php. Modul PS NIE wgrany na PROD — 116b: Karol wgrywa recznie komenda rsync z raportu. Commity: backend 80884c2, modul a1b8cd6)
# Wersja: 3.29 | Data: 2026-06-03 (CHAT-T-048 ZAIMPLEMENTOWANY, CZEKA NA RECZNY DEPLOY KAROLA: zakladka "Rozmowy" w panelu PS DiveZone Chat — lista (paginacja + filtry search/admin_status/knowledge_gap), szczegoly (meta, koszty USD+PLN z conversation_cost, bable user/AI, zwijana diagnostyka), zmiana statusu (POST /api/conversations/{sid}/status, whitelist 4 lokalnie zanim leci POST, decyzja 105a). Rozmowy ustawione jako DOMYSLNA zakladka (decyzja 106a — wchodzisz w panel bez ?tab i od razu widac Rozmowy). Pasek wg czestosci (118a): Rozmowy | Rekomendacje | Modele | Konfiguracja. Render wiadomosci wzorem standalone/public/js/history.js: user/assistant -> bable, tool_result pomijany. Backend gotowy (CHAT-T-046 any-role), zero zmian po stronie standalone. Modul PS nieiwgrany na PROD — 116b: Karol wgrywa recznie komenda rsync z raportu (instrukcja po commitcie b5026b0). PHP 7.2 / PS 1.7.6 compatible: brak typed properties, brak match, explicit type juggling. php -l clean. +546/-12 linii w AdminDivezoneChatController.php. Commit b5026b0)
# Wersja: 3.28 | Data: 2026-06-01 (CHAT-T-036 DEPLOYED: fix doboru w kategorii regulator_recreational wykryty przez Karola na panelu LIVE po CHAT-T-035 CZESC B — pid 25 (Apeks XTX50 1.st, sama czesc) na prio 2 zamieniony na pid 7421 (Apeks EVX200 + Octopus EVX200, kompletny zestaw wyzszej polki) wg decyzji 47a. Spojna gradacja kategorii: ATX40/DS4+Octopus (budzet, prio 1) → EVX200+Octopus (wyzsza polka, prio 2) → Legend 3+Octopus (premium, prio 3) — wszystkie kompletne zestawy. sql/020 BEGIN/DELETE+INSERT atomicznie (UNIQUE category_key+product_id wyklucza UPDATE product_id), apply na Railway, smoke get_curated_recommendations 3/3 (pid 25 nieobecny, pid 7421 obecny in_stock 4061.90 zł brutto). Pierwszy task z prefiksem CHAT- (nowa konwencja anty-kolizyjna). Commit 848fa5e)
# Wersja: 3.27 | Data: 2026-06-01 (T-033 DEPLOYED: fix instalacji modulu PS divezone_chat — diagnoza wskazala TAB_PARENT='AdminTools' (z T-032) nie istnieje w PS 1.7+ (`Tab::getIdFromClassName` zwracal 0 → `id_parent=0` → wieszacy AJAX instalatora). Fix: `TAB_PARENT_PRIMARY='AdminAdvancedParameters'` (potwierdzony w pr_tab id=103) + `TAB_PARENT_FALLBACK='AdminTools'` legacy, idempotencja installTab (skip gdy class_name juz istnieje), defensive `error_log()` na wszystkich krokach install/uninstall. DB CZYSTA (0 orphans w pr_module/pr_tab dla `divezone_chat`). modules/divezone_chat/ wciaz nie ma na prod sklepu — Karol uploaduje + instaluje. Commit cde9b8f)
# Wersja: 3.26 | Data: 2026-06-01 (T-032 DEPLOYED: kregoslup panelu admin PS faza 1 wdrozony — kanal serwerowy HMAC+employee_id (ServerHmacVerifier, .env DIVECHAT_SERVER_SECRET), tabela rol divechat_admin_roles na PG zaseedowana 6 wpisami (3 admin: 2/5/46, 3 operator: 14/54/61), echo endpoint GET /api/admin/whoami zwraca {status, employee_id, role}, modul PS divezone_chat zbudowany od zera (natywny AdminDivezoneChatController, PHP 7.2 + PS 1.7.6, lint ea-php72 clean). Backend smoke 7/7 PASS (admin/operator/no_role 403/brak header 401/stary ts 401/admin 46/operator 61). Modul PS wymaga instalacji + wprowadzenia sekretu przez Karola w panelu PS (instrukcja w handoffie T-032). ADR-068 wszystkie 3 decyzje (174a/175a/176a) zwalidowane end-to-end. Commit 8887ec3)
# Wersja: 3.25 | Data: 2026-06-01 (T-031 DEPLOYED: re-seed kuratorowanych rekomendacji ADR-065 Faza B — pusta tabela divechat_curated_recommendations zaseedowana 9 wpisami w 3 kategoriach wg wyboru Karola po analizie rankingu Subiekta 12mc (Faza A commit 547bebf). Kategorie: regulator_recreational (Apeks ATX40 2368 p1, Apeks DST 25 p2, Aqualung Legend 3 5983 p3), computer_budget_first (Suunto Zoop Novo 5060 p1, Suunto D5 5706 p2, Shearwater Peregrine 5986 p3), computer_polish_waters (Peregrine 5986 p1, Suunto NAUTIC S 7515 p2 świeży hit bez historii sprzedaży zgodnie z uzup.1, Suunto EON Core 5463 p3). Peregrine 5986 celowo w 2 kategoriach komputerów. Smoke test get_curated_recommendations PASS dla 3/3 kategorii: status=ok, available_count=3/3, cena/availability/promocje z żywego MySQL przez MysqlProductEnrichmentService (np. D5 1204/2106, NAUTIC S 2276/2399, EON Core 1959/2739), kolejność wg priority. Commit 612ed22)

# Aktualizowany ręcznie po każdej sesji architekta (dziennik wersji, najnowsze na górze).
