# HANDOFF — sesja 2026-07-15/16 → nowy czat architekta

**Dla:** nowej instancji Claude-architekta (okno czatu z Karolem)
**Data:** 2026-07-16, ~09:00
**Stan:** wszystkie fakty poniżej ZWERYFIKOWANE przez SSH bezpośrednio przed napisaniem tego dokumentu. Nie przyjmuj ich na wiarę bez potrzeby, ale nie zaczynaj od ich podważania — były sprawdzone.

---

## 0. JAK ZACZĄĆ — przeczytaj to najpierw

**NIE zaczynaj od audytu poprzedniej sesji.** Poprzednim razem nowy czat zaczął od wynajdywania błędów poprzednika i zasypał Karola pytaniami. To zmarnowało jego czas. Fakty w tym dokumencie są sprawdzone i mają podane źródła — jeśli któryś budzi wątpliwość, sprawdź go **sam** przez SSH, nie pytaj Karola.

**Numeracja pytań: kontynuuj od 118.** Ostatnie zadane: 117 (podział pracy przy inwentarzu).

**Pierwsze zdanie do Karola:** krótkie podsumowanie stanu + pytanie, co robimy. Nic więcej.

---

## 1. STAN FRONTÓW — co czeka na kogo

| task | karta | stan (zweryfikowany) | co dalej |
|---|---|---|---|
| **T-138** | Chat - 20 | **WDROŻONE.** md5 PROD == local (`de64aa30069397092441ed84335aa506`). Commit `71a70fd` + status `f8cbf51` | **INSERT EN 250 NIE WYKONANY** — sprawdzone: `divechat_knowledge` nie ma wpisu. Treść gotowa (sekcja 4). Potem: ADR-120 do napisania (architekt), karta do zamknięcia |
| **T-143** | Chat - 28 | **ZAMKNIĘTE** ✅ | — |
| **T-144** | Chat - 30 | **SKOŃCZONE.** Tabela ma **2606** wpisów (nie 2591 — embeddings domknęło 15 produktów z decyzji 95a). 0 bez wektora, 0 bez `embedding_name` | **karta do zamknięcia** — sprawdź raport CC i zamknij |
| **T-145** | Chat - 26 | **NIE RUSZONE.** `embed_target_products.py` bez zmian (`category_name` tylko w `text_desc`, linia ~82) | prompt gotowy w sekcji 5 |
| **T-146** | Chat - 17 | **KOD GOTOWY, NIE WDROŻONY.** md5 lokalny `472701c68dfeb4c8780651333bf9e36c` ≠ PROD `c6309d0f414274c284d5cf1a0cb6fc8b`. Commity `e85131a` + `0180b71` | **czeka na autoryzację „deployuj"** — patrz sekcja 3 (KOREKTA!) |
| **T-147** | Chat - 33 | **ODDANE.** `_docs/44_slownik_pol_i_metryk.md`, 40907 bajtów, commit `59d59f5` | **architekt ma dopisać sekcję PUŁAPKI** (decyzja 117a) — patrz sekcja 6 |

---

## 2. KOREKTA KRYTYCZNA — kto robi deploy

**KAROL NIE ROBI DEPLOYU.** To była moja pomyłka w tej sesji i trafiła do tasku T-146 („deploy robi Karol ręcznym rsync") — **to jest BŁĄD w pliku tasku**, nie powielaj go.

**Deploy robi CC:** backup `_deploy_bak/`, rsync, md5, `php -l` (ea-php84), smoke, **czyszczenie cache**. Wszystko.
**Karol tylko autoryzuje** słowem **„deployuj"** (STOP-gate, ADR-089).

Wyjątek, który Karol musi zrobić sam (bo to sekret): ustawić `PURGE_KEY` w `_ops/newtmp2_root/purge_litespeed.php` (obecnie placeholder `ZMIEN_MNIE_CHAT_T_127`) — patrz sekcja 8.

---

## 3. NASTĘPNY RUCH — T-146 deploy

Kod gotowy i zweryfikowany przez architekta (nie tylko z raportu CC):

| kryterium | wynik |
|---|---|
| `total_paid_real` w kodzie | **0 wystąpień** ✓ |
| `o.valid` jako kryterium płatności | tylko w komentarzu ostrzegawczym ✓ |
| `os.paid` używane | 4 wystąpienia ✓ |
| link „Atrybucja" poza `if ($isAdmin)` | ✓ (106a) |
| Railway PG w zakładce | **0** ✓ |
| liczby na PROD | **5348,80 zamówione / 5036,00 opłacone**, 3 zamówienia ✓ |

**Świat: SHOP+WIDGET (`newtmp2`).** Po rsync **obowiązkowo** `var/cache/prod` + LSCache, inaczej zakładka się nie pokaże.

Prompt do okna frontend po „deployuj" od Karola:
```
deployuj

Świat SHOP+WIDGET. Plik: modules/divezone_chat/controllers/admin/AdminDivezoneChatController.php
→ ~/public_html/newtmp2/modules/divezone_chat/controllers/admin/
rsync port 5739, --exclude config_pl.xml, BEZ --delete.
backup _deploy_bak/ → rsync → md5 local==prod (wypisz) → rm -rf newtmp2/var/cache/prod → flush LSCache.
Potem: otwórz panel, sprawdź że zakładka "Atrybucja" jest widoczna i pokazuje 3 zamówienia
(5348,80 zamówione / 5036,00 opłacone, mediana 13 min).
UWAGA: w pliku tasku jest błąd architekta ("deploy robi Karol") — deploy robisz TY.
```

---

## 4. INSERT EN 250 — treść ZATWIERDZONA, do wykonania

Sprawdzone: **wpisu NIE MA w `divechat_knowledge`**. Treść przeszła trzy rundy poprawek Karola (decyzje 107b, 111a) i jest **finalna**. Nie modyfikuj.

```
question: Do jakiej głębokości jest certyfikowany automat oddechowy? Co to norma EN 250?
category: automaty_oddechowe
is_direct_answer: true
content:
Obowiązująca norma dla automatów do nurkowania na sprężonym powietrzu to EN 250:2014. Określa minimalne wymagania bezpieczeństwa, metody badań i oznakowanie. Norma nie wyznacza dopuszczalnej głębokości nurkowania — opisuje warunki testu: automat bada się przy ciśnieniu 6 barów absolutnych (odpowiednik 50 m) i wentylacji 62,5 l/min, i musi tam utrzymać wymagane parametry oddechowe. To norma sprzętowa, nie zasada nurkowania. Automaty przeznaczone do zimnej wody przechodzą dodatkowy test w około 4°C — odporność na zamarzanie i brak niekontrolowanego freeflow. EN 250A dodaje test oddychania przez dwóch nurków jednocześnie z jednego pierwszego stopnia (nurek + partner na oktopusie). EN 13949 nie zastępuje EN 250 — dokłada wymagania dla automatów do nitroksu i tlenu.
```

**Dlaczego dokładnie tak (nie zmieniaj):**
- **50 m = WARUNEK TESTU, nie limit.** Poprawka Karola, potwierdzona u źródła (ScubaLab: „62,5 rmv @ 165 fsw reprezentuje europejski standard zgodności EN250"). Pierwotna wersja CC mówiła „maksymalna głębokość normy" — to błąd merytoryczny.
- **Bez WOB / J/l / mbar** (111a). Karol: „mało który nurek wie, że istnieje parametr WOB (...) to jak moment obrotowy w silniku — kto potrzebuje, ten wie". `get_expert_knowledge` szuka semantycznie → wpis o automatach BĘDZIE trafiany przy pytaniach o automaty. Im mniej treści, których klient nie pytał, tym mniej jest czym sypać.
- **Bez testów producentów 100-200 m** — sprawdzone u źródeł, niepotwierdzone. Ryzyko fabrykacji.
- **Bez `[Uwaga dla bota]` w `content`** — ograniczenie jest w prompcie (T-138 wdrożone), w bazie wiedzy trafiłoby do embeddingu jako instrukcja zamiast wiedzy.

STOP: CC pokazuje SQL przed wykonaniem. Embedding wpisu robi **instancja embeddings**, nie backend.

---

## 5. T-145 — prompt gotowy (okno embeddings)

```
# CHAT-T-145 — instancja EMBEDDINGS

Zadanie: _instances/embeddings/tasks/CHAT-T-145_embeddings_kategoria-w-embedding-name.md
Decyzja: ADR-125 (commit 3bb3c8b). Przeczytaj OBA przed pierwszą zmianą.

STATUS: EKSPERYMENT. Wynik rozstrzyga brama pomiarowa. Zmiana może zostać ODRZUCONA
i to jest dopuszczalny wynik, nie porażka. Nie naginaj pomiaru pod tezę.
To NIE deploy. Pipeline lokalny, zero rsync, zero zmian w PHP.

KONTEKST (zweryfikowany): build_multivector_texts() ~74-93 buduje text_name =
product_name + " " + brand_name (BEZ kategorii). Kategoria zasila tylko text_desc (~82).
ProductSearch odpytuje 3 tory równolegle. Twoje ustalenie z bramy T-142 było trafne.

PYTANIE: czy dołożenie category_name do text_name poprawia trafność, czy ROZMYWA sygnał?
RYZYKO: text_name jest dziś najcelniejszy (0.9137 dla "Shearwater Perdix 3"), krótki,
więc każde słowo waży dużo. 4 nazwy kategorii mogą go utopić.

KROK 0. git pull origin main. Przeczytaj ADR-125 + task.
KROK 1. pg_dump PRZED czymkolwiek. Ścieżka + rozmiar w raporcie.
KROK 2. W build_multivector_texts(): dołóż category_name do name_parts PO marce.
        Separator bez zmian (spacja). NIE obcinaj — już obcięte do 4 w extract_products.
        NIE RUSZAJ text_jargon (KONTROLA!) ani text_desc.
KROK 3. Próbka 30-50, obowiązkowo 7641, 7648, 7647, 2369, 7545 + kilka jednokategoryjnych.
        Pomiar PRZED. Liczby z bramy T-142:
          "Shearwater Perdix 3" -> 7641:  name 0.9137, jargon 0.9137
          "Scubapro MK17 zestaw" -> 7648: name 0.8612, jargon 0.8600
          "zestaw automatu Apeks MTX-RC" -> 7647: name 0.7737, jargon 0.7686
        Nie zgadza się PRZED → STOP, zaraportuj.
KROK 4. Re-embed TYLKO próbki (sync). Pomiar PO. Mierz OBA tory: name i jargon (Δ=0.0000).
KROK 5. STOP. BRAMA. Tabela PRZED/PO. Czekaj na decyzję Karola.
        name rośnie/bez zmian → zielone światło. name SPADA → odrzucamy, przywracamy z dumpa.
KROK 6. Pełny katalog (2606) TYLKO po jawnej zgodzie. Batch API dozwolone.
KROK 7. git pull --rebase → add per ścieżka:
          embeddings/embed_target_products.py
          _instances/embeddings/tasks/CHAT-T-145_embeddings_kategoria-w-embedding-name.md
        commit: CHAT-T-145 embeddings: kategoria w embedding_name (ADR-125)
KROK 8. Status — dopisz NA GÓRZE. Raport: dump, tabela PRZED/PO, wniosek, koszt.
```

---

## 6. ROBOTA ARCHITEKTA — sekcja PUŁAPKI do `_docs/44`

CC oddało inwentarz (40907 bajtów, commit `59d59f5`). **Przeczytaj go**, a potem dopisz sekcję „PUŁAPKI" — to decyzja 117a. CC celowo jej nie pisało, bo tego nie da się wyczytać z kodu.

**Sześć pułapek do udokumentowania** (wszystkie z tej sesji, wszystkie kosztowały czas Karola):

| założenie | stan faktyczny | źródło |
|---|---|---|
| `visibility='none'` = ukryty | wyszukiwarką sklepu jest **Luigi's Box**, ignoruje to pole. Karol pokazał zrzut: `divezone.pl/szukaj?s=Torba MARES Cruise` → produkt 3920 (`vis=none`) JEST w wynikach | ADR-123 nota 93a |
| `pr_orders.valid=0` = niezapłacone | flaga **księgowa**. Zapłata = `current_state` → `pr_order_state.paid=1`. Dowód: QETUBCWYS `valid=0`, a jest „Zapłacone" | ADR-124 |
| `total_paid_real` = ile wpłynęło | **2× zawyżone** dla 1246/1259 Tpay. Inne bramki (Przelewy24, Revolut, PayPal, PayU = 624 zam.) mają **zero** podwojeń | karta Sklep - 31 |
| `pr_stock_available.quantity` = stan | zaślepki (9999999, 29998). Źródło prawdy: **Subiekt ERP** | — |
| `similarity` w tool_result = cosine | to **`rrf_score`**, skala ~0-0.065 przy 4 torach. Dowód: conv 636, produkt 5560 miał `name_rank=1, jargon_rank=1, trigram_rank=1` → `rrf_score 0.0713` = **najlepszy możliwy wynik** | ADR-125, T-147 |
| „sprzedaż" = kiedykolwiek | pytanie było o „ostatnio" — 344/483 `vis=none` nie sprzedane od roku | — |

**Wzór wpisu** (krótki, operacyjny, nie wykład):
> `similarity` w `search_products` = **`rrf_score`**, skala 0-0,065 (sufit przy 4 torach), NIE cosine 0-1. Nie porównuj z progami cosine. `get_expert_knowledge` zwraca prawdziwy cosine — to dlatego próg 0.5 tam działa, a tu nie.

---

## 7. ZNALEZISKO DO NAPRAWY — `knowledge_gap` (decyzja 115a)

**Zdiagnozowane, task jeszcze NIE napisany.** Naprawa czeka na raport z T-147 (CC miało ustalić, **kto** używa flagi).

- `ChatService::buildSearchDiagnostic()` ~447: `$gap = empty($items) || ($maxSim < $threshold)`
- próg `knowledge_gap_threshold` = **0.5** (~418) — skala **cosine**
- `search_products` zwraca `rrf_score` (sufit ~0.065) → **`maxSim < 0.5` ZAWSZE prawda**
- `ConversationStore` ~189: `knowledge_gap = (? OR COALESCE(knowledge_gap, false))` — **sticky**
- PROD 30 dni: **126 true / 91 false**. `false` = rozmowy bez `search_products`
- Skutek: **każda rozmowa, w której bot cokolwiek wyszukał, ma flagę zapaloną** — niezależnie od jakości

Przed napisaniem tasku: przeczytaj odpowiedź CC z T-147 na pytanie „kto używa flagi". Jeśli nikt istotny — rozważ wariant „usunąć" zamiast „naprawić".

---

## 8. `purge_litespeed.php` (T-127) — czeka na Karola

Skrypt gotowy w `_ops/newtmp2_root/purge_litespeed.php`, ADR-111. **Blokada: `PURGE_KEY` to placeholder `ZMIEN_MNIE_CHAT_T_127`.**

Sekwencja (tylko Karol może zrobić krok 1):
1. Karol ustawia `PURGE_KEY` na własny sekret
2. rsync → `~/public_html/newtmp2/purge_litespeed.php` (Świat 2, STOP-gate)
3. Weryfikacja: `curl -s -D - -o /dev/null "https://divezone.pl/purge_litespeed.php?k=<KLUCZ>&product=123"` → nagłówek ma być `X-Litespeed-Purge2: tag=PSd6615_P123`
4. Sprawdzić, że tylko produkt 123 idzie w miss, reszta zostaje hit

Inny prefiks niż `PSd6615` = root sklepu pod inną ścieżką fizyczną, skrypt sam użył właściwej wartości, **nic nie poprawiać**.

---

## 9. ATRYBUCJA — największe odkrycie sesji

Mechanizm z T-136 **działa i zbiera pieniądze**, bez żadnego testu. Stan na 2026-07-16 09:00:

| zamówienie | kwota | rozmowa → zakup | stan |
|---|---|---|---|
| NRRYNFJYB | 1417,00 zł | **13 min** | Zapłacone (Tpay) |
| QETUBCWYS | 3619,00 zł | **22 min** | Zapłacone (Tpay) |
| UDRDJMBTG | 312,80 zł | **4 min** | Oczekiwanie na przelew |

**5348,80 zł zamówione / 5036,00 zł opłacone w niecałą dobę.** Mediana 13 minut.

Strumień **GA4 (ADR-119, dataLayer + GTM) NIE jest zaimplementowany** — to druga połowa architektury, osobny task, jeszcze bez karty.

---

## 10. TABLICA TRELLO

**Board:** „Projekty 2026" `6a55e07bc2193b7dfc53297e`
**Listy:** Backlog `6a55f95c5437f80d52e1daf7` | W trakcie `6a55f9604c618d67699affa8` | Do weryfikacji `6a55f969dfc51f978fa0ef0d` | Zrobione `6a55f96cfcd658033cee1a82`

**KONWENCJA NAZW (ustalona w tej sesji):** `Prefiks - NN - opis [T-NNN]`, gdzie **NN = numer karty widoczny dla Karola** (`idShort`). Karol NIE widzi ID technicznych — zawsze mów „Chat - 28", nigdy „karta 6a57ae9f...".

**W trakcie:** Chat - 20 (T-138, INSERT+ADR-120), Chat - 30 (T-144, do zamknięcia), Chat - 33 (T-147, sekcja pułapek)
**Do weryfikacji:** Chat - 17 (T-146, czeka na deploy)
**Backlog Chat:** 7 (dostawa zagraniczna), 8 (kombinacje/kolory — czeka na `nazwa_pl`), 9 (rozmiarówki butów), 14 (kat. 416), 23 (automatyzacja embeddingów), 26 (T-145)
**Backlog inne:** Sklep 2, 11, 29, 31 | Klaviyo 12 | Security 19, 27 | Encyklopedia 32
**Zrobione dziś:** 10, 15, 16, 21, 22, 24, 28

---

## 11. OTWARTE PROBLEMY CZATU — 6 rekordów, ŻADEN to błąd bota

Sprawdzone przez architekta, wszystkie przeczytane:

| conv | co | czeka na |
|---|---|---|
| 638 | koszty dostawy zagranicznej | stawki od Karola → **Chat - 7** |
| 606, 593 | produkty z kombinacjami (kolor) | `nazwa_pl` → **Chat - 8** |
| 600 | „dopytać o produkty" | ocenę Karola |
| 589 | 100 zł różnicy w cenie | **zadanie Karola**, nie bota |
| 541 | rozmiarówki butów | dane → **Chat - 9** |

**Zamknięte w tej sesji:** conv 598 (fix T-128), conv 636 (nie był błędem — RRF vs similarity + nudge, nie chipy).

**Chipy działają** — 20-25% rozmów od 2 lipca. `chip_path` = drzewo chipów, `nudge_sid` = zaczepka proaktywna. To dwa różne mechanizmy.

---

## 12. ZASADY PRACY Z KAROLEM — bez tego zrobisz mu krzywdę

**Diagnoza z realnych danych, ZAWSZE.** Nie zgaduj, nie zakładaj. Masz SSH — użyj go. Ta sesja to sześć błędów z wnioskowania z nazwy pola.

**Gdy weryfikacja obala Twoją tezę — powiedz to WPROST, osobnym zdaniem, ZANIM pójdziesz dalej.** Format: „Myliłem się. Twierdziłem X. Jest Y, źródło Z." Nie chowaj korekty w środku dokumentu.

**Brzytwa Ockhama po deployu widgetu/modułu:** najpierw cache (przeglądarka + sklep), dopiero potem kod.

**Dwa światy wdrożeniowe — nie pomyl:**
- **BACKEND:** `chat.divezone.pl`, PHP `ea-php84`, kod w `~/public_html/chat.divezone.pl/src` (BEZ prefiksu `standalone/` — ten jest tylko w repo)
- **SHOP+WIDGET:** `~/public_html/newtmp2/` — **to PRODUKCJA sklepu**, mimo nazwy „tmp"

**Pytania:** numerowane ciągle (od 118), opcje literowane, **ZAWSZE własna rekomendacja z uzasadnieniem** opartym na sprawdzonym fakcie. Bez półpauz, bez średników. Zwięźle.

**Karol martwi się bilansem kart** — dziś otworzyliśmy 5, zamknęliśmy 4. Chce zamykać więcej niż otwierać. Nie mnóż kart bez potrzeby.

---

## 13. TECHNIKALIA — oszczędzą Ci czasu

**SSH:** `ssh -p 5739 divezone@divezonededyk.smarthost.pl`. Domyślny PHP po SSH = 8.3, do 8.4 użyj `ea-php84`.

**Zapytania do PG przez SSH:** apostrofy giną w łańcuchu SSH→zsh→bash→psql. Zapisuj SQL do pliku:
```bash
ssh ... 'cat > /tmp/q.sql << "EOF"
SELECT ... WHERE x = (chr(97)||chr(98));   -- literały przez chr()||
EOF
cd ~/public_html/chat.divezone.pl && export PGURL=$(grep -m1 "^DATABASE_URL=" .env | cut -d= -f2- | tr -d "\"") && psql "$PGURL" -f /tmp/q.sql'
```
`interval` NIE przyjmuje wyrażeń — użyj `make_interval(days => 30)`.

**MySQL:** hasło w `.env` jest **w apostrofach**. `tr -d '"'` ich NIE usuwa → `Access denied`. Najprościej: skrypt PHP w katalogu aplikacji (`require vendor/autoload` + `Dotenv` + `PDO`). `__DIR__` musi wskazywać katalog aplikacji, nie `/tmp`.

**Namespace enrichmentu:** `DiveChat\Shop` (NIE `Divezone\Chat\Shop`).

**Kolumny, które mylą:** `divechat_conversations.started_at` (NIE `created_at`), `divechat_product_embeddings.ps_product_id` (NIE `product_id`). Recenzje są w OSOBNEJ tabeli `divechat_conversation_review`.

**Desktop Commander na SMB:** ścieżka alternuje `/Users/karol/...` ↔ `/Volumes/karol/...`. `present_files` NIE działa na SMB. `edit_block` dla plików ze strzałkami.

**Skrypt do rozmów:** `python3 _diag_local/chat_verification/show_conversation.py {id}`

**ADR-y commituj NATYCHMIAST po zapisie** — ADR-122 raz przepadł, bo CC zrobiło `git pull` i nadpisało niezacommitowany plik.

---

## 14. CO ARCHITEKT MA ZROBIĆ SAM (nie zlecać CC)

1. **ADR-120** — zarezerwowany przez T-138, wciąż **PUSTY**. Sprawdzone: `grep -c "^### ADR-120"` = 0. Napisać na bazie wdrożonego diffu (decyzje 42c, 43b, 44a, 45a).
2. **Sekcja PUŁAPKI** w `_docs/44` (sekcja 6 tego dokumentu).
3. **Zamknąć Chat - 30** (T-144 skończone, 2606 wpisów).
4. **Task na naprawę `knowledge_gap`** — po przeczytaniu raportu T-147.

---

## 15. OSTATNI ADR I TASK

- **Ostatni ADR:** 125 (`3bb3c8b`). Następny wolny: **126**.
- **Ostatni task:** CHAT-T-147. Następny wolny: **CHAT-T-148**.
- **ADR-120:** zarezerwowany, pusty, do napisania.

---

# AKTUALIZACJA — 2026-07-16, ~19:00 (koniec sesji)

Sekcje 1-15 wyżej były pisane rano. **Poniżej stan faktyczny na koniec dnia.** Gdzie się różni — obowiązuje ta aktualizacja.

## Co się zmieniło

| task | było rano | JEST TERAZ |
|---|---|---|
| **T-138** (Chat - 20) | „INSERT EN 250 niewykonany" | **ZROBIONE W CAŁOŚCI. Karta zamknięta.** Wpis EN 250 jest na PROD — ale w `encyclopedia_chunks` id 19, nie w `divechat_knowledge`. ADR-120 zapisany + nota architekta (`cc36605`) |
| **T-146** (Chat - 17) | „czeka na deploy" | **WDROŻONE. Karta zamknięta.** md5 `472701c6`, 3818 linii, cache+LSCache czyszczone. Commity `e85131a`, `4cef4dd` |
| **T-144** (Chat - 30) | „do zamknięcia" | **ZAMKNIĘTA.** Zweryfikowane: 2606 wpisów, 0 braków we wszystkich 4 kolumnach wektorowych, 2194 wielokategoryjnych, kontrolki T-142 nietknięte |
| **T-147** (Chat - 33) | „czeka na sekcję PUŁAPKI" | bez zmian — **nadal czeka** |
| **T-145** (Chat - 26) | „nie ruszone" | bez zmian — **nadal nie ruszone**, prompt w sekcji 5 |

## ODKRYCIE DNIA — `divechat_knowledge` JEST MARTWA

**To unieważnia sekcję 4 tego handoffu.** Nie wykonuj tamtego INSERT-u.

Zweryfikowane 2026-07-16:
- `grep -rn "divechat_knowledge" standalone/src standalone/config` → **ZERO trafień**
- `ExpertKnowledge.php:105` → `FROM encyclopedia_chunks` — `get_expert_knowledge` czyta **wyłącznie** stąd
- `divechat_knowledge`: 37 wpisów, max `id` 42, najnowszy **2026-02-19** — martwa od miesięcy
- Wpis EN 250 **żyje** w `encyclopedia_chunks` id **19**, `concept_key='AUTOMAT_ODDECHOWY'`, `chunk_type='faq'`, 3072 wymiary, test PROD conv 715

**Architekt przez całe przedpołudnie twierdził „INSERT niewykonany, bot nie ma czym odpowiedzieć" — mierzył martwą tabelę.** To **siódmy** przypadek w tej sesji wzięcia nazwy obiektu za jego znaczenie. CC miało rację, architekt nie.

**Dobra wiadomość:** `_docs/44` już to zawiera — linia 158 + **rozjazd R-2** („`02_schemat_bazy.md:65-95` i `CLAUDE.md` opisują `divechat_knowledge` jako bazę wiedzy eksperta. Kod: 0 trafień."). Inwentarz CC jest aktualniejszy, niż zakładano rano.

## PROBLEM PROCESOWY — dwa rozjechane strumienie numeracji

CC napisało i zacommitowało **ADR-120** (`99bc8be`), opisując go jako „napisany przez architekta, niezacommitowany". **Architekt go nie napisał.** Naruszona reguła: ADR-y pisze architekt.

ADR powołuje decyzje **122a** i **125a**. Numeracja architekta skończyła się na **120**. Te numery pochodzą z **okna CC**, które numerowało własne pytania do Karola niezależnie. Karol potwierdza, że CC mogło go pytać bezpośrednio — więc decyzje są realne, ale **numery nieporównywalne**.

**Do ustalenia z Karolem (nierozstrzygnięte):** jedna numeracja decyzji na projekt, czy dopuszczamy osobne strumienie z jawnym prefiksem? Dopóki nie ustalone — odwołania „decyzja NNNa" w ADR-ach pisanych przez CC czytaj jako „decyzja z okna CC".

Wszystko odnotowane w **ADR-120, nota nr 1** (commit `cc36605`).

## Numeracja pytań — KOREKTA

**Kontynuuj od 121**, nie od 118. W tej sesji zadano jeszcze: 118 (zamknięcie Chat - 30), 119 (czy `divechat_knowledge` martwa → 119a), 120 (co z ADR-120 napisanym przez CC → 120a).

## BILANS DNIA — 8 kart zamkniętych

**10** (bezprzedmiotowa), **15**, **16**, **17**, **20**, **21**, **28**, **30**.
Otwarte: **29** (858 usuniętych produktów, odłożone), **31** (Tpay 2×), **32** (encyklopedia EN 250), **33** (T-147), **26** (T-145).

Karol zwracał uwagę, że otwieramy więcej niż zamykamy — na koniec dnia bilans wyszedł **na plus**. Utrzymaj to.

## CO ZOSTAŁO — priorytety

1. **Sekcja PUŁAPKI w `_docs/44`** (decyzja 117a) — robota architekta. **Dopisz ósmą pułapkę: `divechat_knowledge` martwa.** Reszta w sekcji 6.
2. **T-145** — prompt gotowy (sekcja 5), okno embeddings nie ruszyło.
3. **Task na naprawę `knowledge_gap`** (115a) — przeczytaj najpierw odpowiedź CC z T-147 na „kto używa flagi".
4. **`PURGE_KEY`** (sekcja 8) — czeka na Karola. **Uwaga:** `_ops/newtmp2_root/purge_litespeed.php` jest zmodyfikowany lokalnie i niezacommitowany — prawdopodobnie Karol już ustawił klucz. **Nie commituj tego pliku** (zawiera sekret).

## Ostatnie numery — AKTUALNE

- **Ostatni ADR:** 125. Następny wolny: **126**
- **Ostatni task:** CHAT-T-147. Następny wolny: **CHAT-T-148**
- **Ostatnie pytanie:** 120. Następne: **121**
- **HEAD:** `cc36605`

---

# TRELLO — CZYTAJ TO ZANIM COKOLWIEK ZAPROPONUJESZ

**Board:** „Projekty 2026" `6a55e07bc2193b7dfc53297e`
**Listy:** Backlog `6a55f95c5437f80d52e1daf7` | W trakcie `6a55f9604c618d67699affa8` | Do weryfikacji `6a55f969dfc51f978fa0ef0d` | Zrobione `6a55f96cfcd658033cee1a82`

## Pierwsza czynność w nowym oknie

**Przejrzyj Backlog i „W trakcie" ZANIM zaproponujesz jakąkolwiek pracę.** Nie z grzeczności — bo tablica jest **jedynym miejscem**, gdzie widać całość, a ona żyje poza tym oknem.

**Tablica jest współdzielona z innymi sesjami.** Karty zakłada nie tylko architekt czatu: `Sklep - 34` przyszła z sesji Atrybuty_produktow_2026, `Security - 36` z sesji bezpieczeństwa, `Chat - 37` wydzielono z Chat - 8 w innym oknie. **Zawsze pobierz aktualny stan, nigdy nie ufaj liście z handoffu** — ten dokument ma kilka godzin i już jest niepełny.

## KONWENCJA NAZW — obowiązkowa

`Prefiks - NN - opis [T-NNN]`, gdzie **NN = `idShort`, numer widoczny dla Karola.**

**Karol NIE WIDZI ID technicznych.** Mówienie „karta 6a57ae9f..." jest dla niego bezużyteczne — sprawdzone boleśnie w tej sesji: architekt przez pół dnia używał numerów, których Karol nie miał jak sprawdzić. Zawsze **„Chat - 28"**, nigdy ID.

Każda nowa karta **musi** dostać numer w nazwie od razu.

## Stan Backlogu na 2026-07-16 19:00 (16 kart — POBIERZ AKTUALNY)

**Czat — czeka na dane od Karola (nie ruszać bez nich):**
- **Chat - 7** — koszty wysyłki zagranicznej (conv 638 Austria, conv 649 Hiszpania). Karol: stawki SĄ, zależą od wagi. Potrzebne źródło stawek wg wagi/strefy.
- **Chat - 9** — rozmiarówki (buty do suchego, neopren, zestawy dziecięce). Obszar Atrybuty_produktow_2026, ~25 marek.
- **Chat - 14** — po porządku w kat. 416: przełączyć dobór zestawu na filtr kategorii + re-embed.

**Czat — gotowe do wzięcia:**
- **Chat - 26** — kategoria w `embedding_name` (**T-145**, prompt w sekcji 5). Jedyna bez zewnętrznej zależności.
- **Chat - 23** — automatyzacja embeddingów. **Duża robota:** refaktor połączenia MySQL (dziś tunel SSH z laptopa — cron tunelowałby sam do siebie), deploy pipeline'u, venv python3.12, cron, alerty. Pipeline **nie jest cronowany** — nowy produkt = bot go nie zna, aż ktoś ręcznie odpali skrypt.
- **Chat - 37** — warianty: procedura naprawy koszyka + conv 593 + conv 636. **Wydzielone z Chat - 8** (tamta ZROBIONA — ATTR-T-052 na PROD). Uwaga: conv 636 prawdopodobnie należy do wątku `knowledge_gap`, nie wariantów — sprawdzić PO raporcie T-147.
- **Chat - 38** — **instrukcja i narzędzia do analizy rozmów.** Cytat Karola: *„sprawdzanie czatów to coś, co robi się na co dzień (...) żebyś za każdym razem nie szukał wszystkiego od początku (...) jak następnym razem trzeba sprawdzić czat, to nie piszesz, że nie masz dostępu do Postgresa, a potem szukasz .env na serwerze, a potem wysypujesz się na ssh — **zrobiłeś tę procedurę już kilkadziesiąt razy!**"* — **W TEJ SESJI ARCHITEKT ZROBIŁ TO CZTERY RAZY.** Patrz sekcja 13 (technikalia) — tam są gotowe wzorce. Ta karta ma je utrwalić.

**Sklep:**
- **Sklep - 29** — 858 produktów fizycznie usuniętych z bazy (9 w 2026, trwa). **Karol świadomie odłożył** („za pół roku"). URL 404, nieodwracalne. Nie mylić z `visibility=none`.
- **Sklep - 31** — Tpay zapisuje płatność 2× (1246/1259). Tylko ustalenie faktów (103a). Sprawdzić **Sklep - 11** (nowa wersja TPay z DEV) przed grzebaniem w kodzie.
- **Sklep - 34** — 208/299 kolorów pokazuje klientowi angielski tooltip. **Praca zrobiona, niewdrożona:** 256 kolorów ma gotową polską nazwę w `divezone_attr_color_lang`. HARD STOP: `pr_attribute_lang` to rdzeń PS, widzi go Subiekt/SLINK.
- **Sklep - 2** — Apple Pay. **Sklep - 11** — nowa wersja TPay z DEV.

**Security (wykonawca: Gemini/Antigravity, nie Claude):**
- **Security - 19** — weryfikacja Fazy 2 (termin 22.07, czeka na reset limitu Gemini)
- **Security - 27** — audyt `triage.py` (termin 23.07)
- **Security - 36** — **CSP report-only.** Sklep przeszedł **14.07 włamanie ze skimmerem kart** (p6dX9.js, exfil przez XHR). Cztery iteracje detekcji statycznej pokazały granicę — CSP odwraca sytuację. Osobna sesja, dotyka checkoutu PROD.

**Inne:** **Klaviyo - 12** (custom product block), **Encyklopedia - 32** (artykuł EN 250, bez tabeli WOB).

## Czego NIE robić

- **Nie mnóż kart.** Karol wprost: *„zamiast zamykania to otwieramy cały czas nowe"*. Dziś otwarto 5, zamknięto 8 — bilans na plus, **utrzymaj to**.
- **Nie zamykaj bez weryfikacji.** Standard: SSH, md5, zapytanie do bazy — nie raport CC. W tej sesji CC dwa razy zaraportowało coś, co wymagało korekty.
- **Nie pytaj o karty, które są już w „Zrobione".** Architekt zrobił to dziś z Chat - 17 — CC napisało „kartę zostawiam Tobie", a architekt nie sprawdził i zapytał Karola o rzecz dawno zamkniętą.
